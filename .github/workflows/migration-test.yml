name: Migration SQL Test

# 目的: マイグレーション（Python スクリプト + 生 SQL）を本番適用前に実際の PostgreSQL で検証する
#
# 検証内容:
#   1. 既存データが入った状態でマイグレーションを実行（本番を模擬）
#   2. 同じマイグレーションをもう一度実行（冪等性チェック）
#   3. 追加されたカラムに既存行の値が正しく入っているか確認
#
# 対象:
#   - scripts/migrate_*.py  — Python マイグレーションスクリプト
#   - migrations/[0-9]*.sql — 生 SQL マイグレーション（{schema} テンプレートは除外）
#
# これにより防げること:
#   - SQL 構文エラー（デプロイ前に発覚）
#   - DEFAULT 指定漏れ（既存行が NULL になるバグ）
#   - ADD COLUMN IF NOT EXISTS 書き忘れ（再実行でエラーになるバグ）
#   - PL/pgSQL DO ブロックのロジックエラー（冪等性破壊）
#
# 最適化（2026-06-01）: detect-changes → migration-test-run → aggregator パターンを導入。
#   migration 変更がない PR では PostgreSQL コンテナ起動 + pip install を省略（約2〜3分短縮）。
#   test.yml・schema-check.yml と同じ設計パターン。

on:
  pull_request:
    branches: [main, develop]

jobs:
  # マイグレーション関連ファイルの変更有無を判定
  detect-changes:
    name: マイグレーション変更検出
    runs-on: ubuntu-latest
    outputs:
      migrations: ${{ steps.filter.outputs.migrations }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            migrations:
              - 'migrations/**'
              - 'scripts/migrate_*.py'
              - '.github/workflows/migration-test.yml'

  # マイグレーション変更がある場合のみ PostgreSQL を起動して実行
  migration-test-run:
    name: マイグレーションSQL 実行（内部）
    needs: detect-changes
    if: needs.detect-changes.outputs.migrations == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: jarvis
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: jarvis_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U jarvis -d jarvis_db"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://jarvis:testpass@localhost:5432/jarvis_db
      PGPASSWORD: testpass

    steps:
      # fetch-depth: 0 必須。後段の detect step が PR base..head の git diff で
      # 変更 SQL を特定するため、base SHA を含む全履歴が必要（shallow=1 だと
      # `fatal: bad object` で diff が失敗し、変更検出が空＝SQL未実行の偽 green になる）。
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: backend/requirements.txt

      - name: Install dependencies
        working-directory: backend
        run: uv pip install --system -r requirements.txt

      - name: PostgreSQL 待機
        run: |
          for i in $(seq 1 10); do
            pg_isready -h localhost -U jarvis -d jarvis_db && exit 0
            sleep 2
          done
          echo "PostgreSQL が起動しませんでした" && exit 1

      # ── Step 1: 本番を模擬した初期スキーマ（既存データあり）を作る ──────────────
      #
      # 【段階的拡充ルール】
      # 新しい migration が操作するテーブルがここにない場合、その migration の SQL が
      # PostgreSQL 上で正しく実行できるか検証できません。
      #
      # やること: 新規 migration を追加する PR では、そのテーブルの最小定義をここに追加してください。
      #   例) ALTER TABLE public.foo ADD COLUMN bar INTEGER;  を書く場合
      #       → public.foo の CREATE TABLE をここに追加する
      #
      # コスト: 1テーブル追加するだけ。migration 変更がない PR ではこのジョブ自体が起動しないため
      #         テスト速度への影響はゼロです。
      #
      # 現在登録済みのテーブル:
      #   public:    tenants / users / permissions / tenant_discord_config / inventory
      #   tenant_001: role_permissions / staff / leads / deals
      #   ※ public.products は migrate_inventory_sprint1.py が作成（Sprint 1 ステップ）
      #   ※ public.inventory は PUBLIC_SQL セクションに最小定義を登録（migration 081 相当）
      #   ※ tenant_001.own_inventory は 20260604_140000_create_own_inventory.sql が CREATE（移行自体が作成）
      # ──────────────────────────────────────────────────────────────────────
      - name: 既存データ付きの公開スキーマを作成
        run: |
          psql -h localhost -U jarvis -d jarvis_db -v ON_ERROR_STOP=1 <<'SQL'
          -- public.tenants
          CREATE TABLE IF NOT EXISTS public.tenants (
              id          SERIAL PRIMARY KEY,
              tenant_name VARCHAR(255) NOT NULL UNIQUE,
              tenant_code VARCHAR(50)  NOT NULL UNIQUE,
              is_active   BOOLEAN      NOT NULL DEFAULT TRUE,
              settings    JSONB                 DEFAULT '{}',
              created_at  TIMESTAMPTZ           DEFAULT NOW(),
              updated_at  TIMESTAMPTZ           DEFAULT NOW()
          );

          -- public.users（is_super_admin は意図的に含めない = migration 前の状態）
          CREATE TABLE IF NOT EXISTS public.users (
              id            SERIAL PRIMARY KEY,
              tenant_id     INTEGER      NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
              email         VARCHAR(255) NOT NULL UNIQUE,
              password_hash VARCHAR(255) NOT NULL DEFAULT '',
              full_name     VARCHAR(255),
              role          VARCHAR(50)           DEFAULT 'user',
              is_active     BOOLEAN               DEFAULT TRUE,
              locale        VARCHAR(10)  NOT NULL DEFAULT 'ja',
              theme         VARCHAR(10)  NOT NULL DEFAULT 'light',
              created_at    TIMESTAMPTZ           DEFAULT NOW(),
              updated_at    TIMESTAMPTZ           DEFAULT NOW(),
              last_login    TIMESTAMPTZ
          );

          -- public.permissions
          CREATE TABLE IF NOT EXISTS public.permissions (
              id          SERIAL PRIMARY KEY,
              key         VARCHAR(100) NOT NULL UNIQUE,
              resource    VARCHAR(50)  NOT NULL,
              action      VARCHAR(50)  NOT NULL,
              description VARCHAR(255) NOT NULL DEFAULT '',
              category    VARCHAR(50)  NOT NULL DEFAULT ''
          );

          -- 既存データを挿入（本番の既存ユーザーを模擬）
          INSERT INTO public.tenants (tenant_name, tenant_code)
          VALUES ('テストテナント', 'test-tenant') ON CONFLICT DO NOTHING;

          INSERT INTO public.users (tenant_id, email, full_name)
          VALUES (1, 'existing@example.com', '既存ユーザー') ON CONFLICT DO NOTHING;
          SQL

          # テナントスキーマ（PL/pgSQL DO ブロック型 SQL 用）
          psql -h localhost -U jarvis -d jarvis_db -v ON_ERROR_STOP=1 <<'TENANT_SQL'
          CREATE SCHEMA IF NOT EXISTS tenant_001;

          -- role_permissions: 078 の DO ブロックがスキーマ判定に使用するテーブル
          CREATE TABLE IF NOT EXISTS tenant_001.role_permissions (
              id         SERIAL PRIMARY KEY,
              role       VARCHAR(50) NOT NULL,
              permission VARCHAR(100) NOT NULL
          );

          -- staff: テナント個別スキーマにのみ存在（public.staff は存在しない）
          -- ここで定義しておくことで「REFERENCES public.staff」を書いた migration が
          -- PostgreSQL のエラーで検出できる（public.staff が見つからない → ERROR）
          CREATE TABLE IF NOT EXISTS tenant_001.staff (
              id         SERIAL PRIMARY KEY,
              tenant_id  INTEGER NOT NULL,
              user_id    INTEGER,
              full_name  VARCHAR(255),
              is_active  BOOLEAN NOT NULL DEFAULT TRUE,
              created_at TIMESTAMPTZ DEFAULT NOW(),
              updated_at TIMESTAMPTZ DEFAULT NOW()
          );

          -- leads: テナント個別スキーマにのみ存在
          CREATE TABLE IF NOT EXISTS tenant_001.leads (
              id         SERIAL PRIMARY KEY,
              tenant_id  INTEGER NOT NULL,
              full_name  VARCHAR(255),
              created_at TIMESTAMPTZ DEFAULT NOW(),
              updated_at TIMESTAMPTZ DEFAULT NOW()
          );

          -- deals: テナント個別スキーマにのみ存在
          CREATE TABLE IF NOT EXISTS tenant_001.deals (
              id         SERIAL PRIMARY KEY,
              tenant_id  INTEGER NOT NULL,
              title      VARCHAR(255),
              company_id INTEGER,
              created_at TIMESTAMPTZ DEFAULT NOW(),
              updated_at TIMESTAMPTZ DEFAULT NOW()
          );

          -- companies: テナント個別スキーマにのみ存在（v_company_stats ビューの参照先）
          CREATE TABLE IF NOT EXISTS tenant_001.companies (
              id         SERIAL PRIMARY KEY,
              tenant_id  INTEGER NOT NULL,
              name       VARCHAR(255) NOT NULL,
              created_at TIMESTAMPTZ DEFAULT NOW(),
              updated_at TIMESTAMPTZ DEFAULT NOW()
          );

          -- conversation_logs (for PR#4 migration test)
          CREATE TABLE IF NOT EXISTS tenant_001.conversation_logs (
              id                  SERIAL PRIMARY KEY,
              tenant_id           INTEGER NOT NULL,
              lead_id             INTEGER,
              contact_id          INTEGER,
              company_id          INTEGER,
              deal_id             INTEGER,
              channel_type        VARCHAR(30) NOT NULL,
              channel_identity    VARCHAR(255),
              direction           VARCHAR(10) NOT NULL CHECK (direction IN ('inbound', 'outbound')),
              sender              VARCHAR(100),
              content_text        TEXT,
              original_language   VARCHAR(10),
              external_message_id VARCHAR(255) UNIQUE,
              raw_payload         JSONB,
              status              VARCHAR(20) DEFAULT 'sent',
              translated_text     TEXT,
              analysis            JSONB,
              occurred_at         TIMESTAMPTZ NOT NULL,
              created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
          );

          -- quotes: 見積書テーブル（C-6 スナップショット migration が ALTER する）
          CREATE TABLE IF NOT EXISTS tenant_001.quotes (
              id              SERIAL PRIMARY KEY,
              tenant_id       INTEGER NOT NULL,
              quote_code      VARCHAR(50),
              company_id      INTEGER,
              contact_id      INTEGER,
              currency        VARCHAR(10) NOT NULL DEFAULT 'JPY',
              subtotal        NUMERIC(15,2),
              shipping_fee    NUMERIC(15,2),
              tax_amount      NUMERIC(15,2),
              total_amount    NUMERIC(15,2),
              status          VARCHAR(30) NOT NULL DEFAULT 'draft',
              validity_date   DATE,
              notes           TEXT,
              created_at      TIMESTAMPTZ DEFAULT NOW(),
              updated_at      TIMESTAMPTZ DEFAULT NOW()
          );

          -- quote_items: 見積明細テーブル（C-6: hs_code 列追加 migration が ALTER する）
          CREATE TABLE IF NOT EXISTS tenant_001.quote_items (
              id              SERIAL PRIMARY KEY,
              quote_id        INTEGER NOT NULL,
              product_id      INTEGER,
              product_name    VARCHAR(255),
              name_en         VARCHAR(255),
              condition       VARCHAR(50),
              unit            VARCHAR(50),
              quantity        INTEGER NOT NULL DEFAULT 1,
              unit_price      NUMERIC(15,2) NOT NULL DEFAULT 0,
              weight          NUMERIC(10,3),
              subtotal        NUMERIC(15,2),
              sort_order      INTEGER NOT NULL DEFAULT 0
          );

          -- invoices: 請求書テーブル（C-6/C-7 スナップショット migration が ALTER する）
          CREATE TABLE IF NOT EXISTS tenant_001.invoices (
              id                  SERIAL PRIMARY KEY,
              tenant_id           INTEGER NOT NULL,
              invoice_number      VARCHAR(50),
              quote_id            INTEGER,
              company_id          INTEGER,
              contact_id          INTEGER,
              currency            VARCHAR(10) NOT NULL DEFAULT 'JPY',
              subtotal            NUMERIC(15,2),
              shipping_fee        NUMERIC(15,2),
              tax_amount          NUMERIC(15,2),
              total_amount        NUMERIC(15,2),
              exchange_rate_jpy   NUMERIC(15,4),
              exchange_rate_usd   NUMERIC(15,4),
              amount_jpy          NUMERIC(15,2),
              amount_usd          NUMERIC(15,2),
              payment_method      VARCHAR(50),
              status              VARCHAR(30) NOT NULL DEFAULT 'draft',
              branch_number       INTEGER NOT NULL DEFAULT 1,
              pdf_url             TEXT,
              erp_key             VARCHAR(20),
              issued_at           TIMESTAMPTZ,
              due_date            DATE,
              paid_at             TIMESTAMPTZ,
              voided_at           TIMESTAMPTZ,
              void_reason         TEXT,
              notes               TEXT,
              created_by          INTEGER,
              created_at          TIMESTAMPTZ DEFAULT NOW(),
              updated_at          TIMESTAMPTZ DEFAULT NOW()
          );

          -- invoice_items: 請求明細テーブル（C-6: hs_code 列追加 migration が ALTER する）
          CREATE TABLE IF NOT EXISTS tenant_001.invoice_items (
              id              SERIAL PRIMARY KEY,
              invoice_id      INTEGER NOT NULL,
              product_id      INTEGER,
              product_name    VARCHAR(255),
              name_en         VARCHAR(255),
              condition       VARCHAR(50),
              unit            VARCHAR(50),
              quantity        INTEGER NOT NULL DEFAULT 1,
              unit_price      NUMERIC(15,2) NOT NULL DEFAULT 0,
              weight          NUMERIC(10,3),
              subtotal        NUMERIC(15,2),
              sort_order      INTEGER NOT NULL DEFAULT 0
          );
          TENANT_SQL

          # public スキーマの追加テーブル（最近の migration が ALTER/参照するテーブル）
          psql -h localhost -U jarvis -d jarvis_db -v ON_ERROR_STOP=1 <<'PUBLIC_SQL'
          -- tenant_discord_config: discord連携設定（ALTER ADD COLUMN が多い）
          CREATE TABLE IF NOT EXISTS public.tenant_discord_config (
              id         SERIAL PRIMARY KEY,
              tenant_id  INTEGER NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
              guild_id   VARCHAR(32),
              created_at TIMESTAMPTZ DEFAULT NOW(),
              updated_at TIMESTAMPTZ DEFAULT NOW()
          );

          -- public.tenant_settings: ADR-106 テナントポリシー列が ALTER ADD COLUMN で操作する
          CREATE TABLE IF NOT EXISTS public.tenant_settings (
              tenant_id         INTEGER PRIMARY KEY REFERENCES public.tenants(id) ON DELETE CASCADE,
              spreadsheet_phase TEXT NOT NULL DEFAULT 'A',
              created_at        TIMESTAMPTZ DEFAULT NOW(),
              updated_at        TIMESTAMPTZ DEFAULT NOW()
          );
          INSERT INTO public.tenant_settings (tenant_id) VALUES (1) ON CONFLICT DO NOTHING;

          -- NOTE: public.suppliers / public.products は migrate_inventory_sprint1.py が
          --       CREATE TABLE するため、ここでは作成しない。
          --       スクリプト実行前に列が欠けた状態で存在するとエラーになる。

          -- public.inventory: migration 081 で作成。ADR SA-05 の source_kind 追加 migration が
          --   ALTER TABLE public.inventory を実行するため、前提として最小定義を登録する。
          --   （migrate_inventory_sprint1.py は 056-063 のみで 081 を含まない）
          CREATE TABLE IF NOT EXISTS public.inventory (
              id          SERIAL PRIMARY KEY,
              supplier_id INTEGER NOT NULL,
              product_id  INTEGER NOT NULL,
              condition   VARCHAR(50) NOT NULL,
              quantity    INTEGER NOT NULL DEFAULT 0,
              unit_price  INTEGER NOT NULL DEFAULT 0,
              status      VARCHAR(20) NOT NULL DEFAULT 'in_stock',
              notes_ja    TEXT,
              notes_en    TEXT,
              offered_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
              expires_at  TIMESTAMPTZ,
              source      VARCHAR(50) NOT NULL DEFAULT 'manual',
              created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
              updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
              UNIQUE (supplier_id, product_id, condition)
          );

          -- ADR SA-06: ingestion_jobs / parse_logs は Sprint 1 (suppliers/products) 依存のため
          --   ここでは定義せず、Sprint 1 実行後に migration SQL として適用される。
          --   ただし v_supplier_parse_stats VIEW が parse_logs を参照するため、
          --   migration 実行順（20260604_110000 → 20260604_120000 → 20260604_130000）が
          --   CI の changed_sql ソート順と一致することを確認済み。

          -- tenant_carrier_credentials: ADR-125 RLS + account_number migration の前提テーブル
          --   20260608_080000_add_carrier_credentials.sql で本番作成済み。
          CREATE TABLE IF NOT EXISTS public.tenant_carrier_credentials (
              id                      SERIAL PRIMARY KEY,
              tenant_id               INTEGER NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
              carrier                 TEXT    NOT NULL,
              client_id_encrypted     TEXT    NOT NULL DEFAULT '',
              client_secret_encrypted TEXT    NOT NULL DEFAULT '',
              environment             TEXT    NOT NULL DEFAULT 'sandbox',
              updated_by_user_id      INTEGER REFERENCES public.users(id) ON DELETE SET NULL,
              created_at              TIMESTAMPTZ DEFAULT NOW(),
              updated_at              TIMESTAMPTZ DEFAULT NOW(),
              UNIQUE (tenant_id, carrier)
          );

          -- ADR-SA-03: registration_tokens — ADR-127 の ALTER TABLE（CHECK制約拡張）の前提テーブル
          --   20260604_080000_create_registration_tokens.sql で本番作成済み。
          CREATE TABLE IF NOT EXISTS public.registration_tokens (
              id           UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
              tenant_id    INTEGER     NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
              lead_id      INTEGER     NOT NULL,
              token_hash   VARCHAR(64) UNIQUE NOT NULL,
              type         VARCHAR(20) NOT NULL CHECK (type IN ('register', 'add_address')),
              expires_at   TIMESTAMPTZ NOT NULL,
              used_at      TIMESTAMPTZ,
              created_by   INTEGER     NOT NULL,
              created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
          );
          PUBLIC_SQL

      # ── Step 2: Sprint 1 を適用（Sprint 2 の前提条件）──────────────────────────
      - name: Sprint 1 マイグレーションを適用
        env:
          PYTHONPATH: ${{ github.workspace }}/backend
        run: |
          if [ -f scripts/migrate_inventory_sprint1.py ]; then
            python scripts/migrate_inventory_sprint1.py
            echo "✅ Sprint 1 完了"
          else
            echo "ℹ️  Sprint 1 スクリプトなし — スキップ"
          fi

      # ── Step 3: PR で変更されたマイグレーションスクリプトを実行 ──────────────────
      - name: 変更されたマイグレーションを検出
        id: detect
        run: |
          BASE=${{ github.event.pull_request.base.sha }}
          HEAD=${{ github.event.pull_request.head.sha }}

          # fail-closed: base..head の diff を 1 回だけ取得し、失敗したらジョブを落とす。
          # shallow checkout 等で base SHA が無いと git diff は `fatal: bad object` になる。
          # これを `|| true` で握りつぶすと「変更なし」と誤判定し、migration を実際には
          # 実行しないまま緑（false-positive）になるため、明示的に exit 1 する。
          if ! ALL_CHANGED=$(git diff --name-only "$BASE" "$HEAD"); then
            echo "::error::git diff $BASE..$HEAD に失敗（base SHA 不在の可能性）。fail-closed でジョブを失敗させます。checkout の fetch-depth: 0 を確認してください。"
            exit 1
          fi

          # Python スクリプト（上で取得済みの diff から抽出）
          CHANGED=$(echo "$ALL_CHANGED" | grep '^scripts/migrate_' | sort || true)

          echo "changed_scripts<<EOF" >> "$GITHUB_OUTPUT"
          echo "$CHANGED" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          if [ -z "$CHANGED" ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            echo "ℹ️  マイグレーションスクリプトの変更なし"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            echo "🔍 対象スクリプト:"
            echo "$CHANGED"
          fi

          # 生 SQL ファイル（{schema} テンプレートは psql 直接実行不可のため除外）
          # xargs -r: 入力が空なら grep を実行しない（GNU xargs）
          # grep -L (not -rL): ディレクトリ再帰を行わない
          CHANGED_SQL=$(echo "$ALL_CHANGED" \
            | grep '^migrations/[0-9][0-9][0-9].*\.sql$' \
            | xargs -r grep -L '{schema}' 2>/dev/null | sort || true)

          echo "changed_sql<<EOF" >> "$GITHUB_OUTPUT"
          echo "$CHANGED_SQL" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          if [ -z "$CHANGED_SQL" ]; then
            echo "has_sql_changes=false" >> "$GITHUB_OUTPUT"
            echo "ℹ️  生 SQL マイグレーションの変更なし"
          else
            echo "has_sql_changes=true" >> "$GITHUB_OUTPUT"
            echo "🔍 対象 SQL ファイル:"
            echo "$CHANGED_SQL"
          fi

      - name: 変更スクリプトを 1回目実行（既存データへの適用テスト）
        if: steps.detect.outputs.has_changes == 'true'
        env:
          PYTHONPATH: ${{ github.workspace }}/backend
        run: |
          echo "${{ steps.detect.outputs.changed_scripts }}" | while read script; do
            [ -z "$script" ] && continue
            echo "▶ $script を実行中..."
            python "$script"
            echo "✅ $script 成功"
          done

      - name: 変更スクリプトを 2回目実行（冪等性テスト）
        if: steps.detect.outputs.has_changes == 'true'
        env:
          PYTHONPATH: ${{ github.workspace }}/backend
        run: |
          echo "🔄 冪等性チェック（同じスクリプトを再実行）"
          echo "${{ steps.detect.outputs.changed_scripts }}" | while read script; do
            [ -z "$script" ] && continue
            echo "▶ $script を再実行中..."
            python "$script"
            echo "✅ $script 再実行も成功（冪等性 OK）"
          done

      # ── Step 4: 既存データに DEFAULT 値が正しく入っているか確認 ─────────────────
      - name: 既存行に DEFAULT 値が正しく適用されているか検証
        if: steps.detect.outputs.has_changes == 'true'
        run: |
          echo "🔍 既存ユーザー行の状態を確認:"
          psql -h localhost -U jarvis -d jarvis_db -c \
            "SELECT id, email, full_name FROM public.users;" \
            -v ON_ERROR_STOP=1

          # is_super_admin が追加された場合、既存行が NULL でないことを確認
          HAS_COL=$(psql -h localhost -U jarvis -d jarvis_db -tAc \
            "SELECT COUNT(*) FROM information_schema.columns
             WHERE table_schema='public' AND table_name='users'
             AND column_name='is_super_admin';" 2>/dev/null || echo "0")

          if [ "$HAS_COL" = "1" ]; then
            NULL_COUNT=$(psql -h localhost -U jarvis -d jarvis_db -tAc \
              "SELECT COUNT(*) FROM public.users WHERE is_super_admin IS NULL;")
            if [ "$NULL_COUNT" != "0" ]; then
              echo ""
              echo "❌ 既存ユーザー ${NULL_COUNT}件 の is_super_admin が NULL です"
              echo "   migration SQL に DEFAULT 値が指定されているか確認してください"
              exit 1
            fi
            echo "✅ 既存ユーザーの is_super_admin: NULL なし（DEFAULT が正しく適用された）"
          fi

          echo "✅ 全検証パス"
      # ── Step 5: PR で変更された生 SQL マイグレーションを実行 ─────────────────────
      # {schema} プレースホルダ含む旧テンプレート（018, 019 等）は検出時に除外済み
      - name: 変更 SQL を 1回目実行（実行可否・構文チェック）
        if: steps.detect.outputs.has_sql_changes == 'true'
        run: |
          echo "${{ steps.detect.outputs.changed_sql }}" | while read sqlfile; do
            [ -z "$sqlfile" ] && continue
            echo "▶ $sqlfile を実行中..."
            psql -h localhost -U jarvis -d jarvis_db -v ON_ERROR_STOP=1 -f "$sqlfile"
            echo "✅ $sqlfile 成功"
          done

      - name: 変更 SQL を 2回目実行（冪等性テスト）
        if: steps.detect.outputs.has_sql_changes == 'true'
        run: |
          echo "🔄 SQL 冪等性チェック（同じファイルを再実行）"
          echo "${{ steps.detect.outputs.changed_sql }}" | while read sqlfile; do
            [ -z "$sqlfile" ] && continue
            echo "▶ $sqlfile を再実行中..."
            psql -h localhost -U jarvis -d jarvis_db -v ON_ERROR_STOP=1 -f "$sqlfile"
            echo "✅ $sqlfile 再実行成功（冪等性 OK）"
          done

  # ルールセット必須 Check の名前と一致させる。always() で常に実行し、
  # - migration 変更あり: migration-test-run の結果を報告
  # - migration 変更なし: migration-test-run が skipped → 成功として報告
  migration-test:
    name: マイグレーションSQL 実行テスト（実DB）
    needs: [detect-changes, migration-test-run]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Report results
        run: |
          r="${{ needs.migration-test-run.result }}"
          echo "migration-test-run: $r"
          if [[ "$r" == "success" || "$r" == "skipped" ]]; then
            echo "OK: migration test passed or no migration changes"
            exit 0
          else
            echo "FAIL: migration-test-run=$r"
            exit 1
          fi
