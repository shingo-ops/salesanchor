name: Backend Tests

on:
  pull_request:
    branches:
      - main
      - develop
    # paths フィルタを PR トリガーに置かない。
    # ルールセットが "pytest (SQLite + PostgreSQL RLS)" を必須 Check としているため、
    # ワークフロー未実行（skipped）は「未通過」扱いになり frontend-only PR がブロックされる。
    # 代わりに detect-changes ジョブで変更範囲を判定し、
    # backend 変更がない場合は pytest ジョブを即時成功終了させる。
  push:
    branches:
      - develop
      - main
    paths:
      - 'backend/**'
      - 'migrations/**'
      - 'scripts/**'
      - 'pyproject.toml'
      - 'docker-compose.yml'
      - 'docker-compose.test.yml'
      - '.github/workflows/test.yml'
      - 'backend/pyproject.toml'
      - 'backend/Makefile'
  # schedule 削除（2026-05-28）: PR + push トリガーで十分にカバーされるため冗長。
  # 依存パッケージの週次フルスキャンが必要な場合は workflow_dispatch で手動実行。

jobs:
  # backend 変更有無を判定して後続ジョブに伝える
  detect-changes:
    name: Detect backend changes
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
    steps:
      - uses: actions/checkout@v5
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'migrations/**'
              - 'scripts/**'
              - 'pyproject.toml'
              - 'docker-compose.yml'
              - 'docker-compose.test.yml'
              - '.github/workflows/test.yml'

  # 静的解析（ruff/bandit/mypy）— pytest-run と並列実行
  # 設定: backend/pyproject.toml（ruff/mypy/bandit/pytest の SSoT）
  # ローカル確認: cd backend && make lint（strict）/ make check（CI 相当）
  #
  # bandit: HIGH/CRITICAL 0件確認済み → ブロック（--exit-zero なし）
  # ruff:   708件の auto-fixable 違反あり → `make fix-lint` 後にブロック化
  # mypy:   型アノテーション整備中 → 警告のみ（|| true）
  lint-backend:
    name: lint-backend-internal
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: backend/requirements-dev.txt

      - name: Install lint dependencies
        working-directory: backend
        run: uv pip install --system -r requirements-dev.txt

      - name: Run ruff linting（ブロック）
        working-directory: backend
        run: ruff check app/

      - name: Run bandit security scan（HIGH/CRITICAL 0件・ブロック）
        working-directory: backend
        run: bandit -r app/ -lll

      - name: SA-18 Phase2 ポカヨケ — 生の SET search_path 検出（app.tenant_id 未セット防止）
        working-directory: backend
        run: |
          # set_tenant_context / set_tenant_context_sync を経由しない生の SET search_path を検出する。
          # 唯一の正規経路: app/auth/dependencies.py 内の set_tenant_context* 本体のみ許可。
          # `.execute(` を伴う呼び出しのみ検出（コメント・docstring・SQL文字列定数を除外）
          VIOLATIONS=$(grep -rn "execute.*SET search_path" app/ \
            --include="*.py" \
            | grep -v "^app/auth/dependencies.py:" \
            || true)
          if [ -n "$VIOLATIONS" ]; then
            echo "❌ 生の 'SET search_path' を検出しました。set_tenant_context() / set_tenant_context_sync() を使ってください。"
            echo "$VIOLATIONS"
            exit 1
          fi
          echo "✅ 生の SET search_path なし"

      - name: Run mypy type checking（型アノテーション整備中・警告のみ）
        working-directory: backend
        run: mypy app/ --no-error-summary || true

  # 実テスト。backend 変更がある場合のみ services を使って実行する。
  pytest-run:
    name: pytest-run-internal
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: jarvis
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: jarvis_test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U jarvis -d jarvis_test_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: |
            backend/requirements.txt
            backend/requirements-dev.txt

      - name: Install dependencies
        working-directory: backend
        run: uv pip install --system -r requirements.txt -r requirements-dev.txt

      - name: Generate Fernet key for tests
        id: fernet
        run: |
          KEY=$(python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
          echo "key=$KEY" >> "$GITHUB_OUTPUT"

      # NOTE: "Wait for PostgreSQL" 手動ループ削除（2026-05-28）。
      # services の health-check（--health-cmd "pg_isready"）が通過した時点で
      # PostgreSQL は確実に起動済み。二重確認は冗長。

      # SA-19: salesanchor_app（最小権限ロール）を作成。
      # CI role = 本番 role の不変条件: DML のみ、CREATE は付与しない。
      # テスト用テーブルの DDL は管理者接続（jarvis）が担当（test_rls_*.py で admin_engine 使用）。
      - name: Create non-superuser role for RLS tests (SA-19 — salesanchor_app)
        env:
          PGPASSWORD: testpass
        run: |
          psql -h localhost -U jarvis -d jarvis_test_db -v ON_ERROR_STOP=1 <<'SQL'
          DO $$ BEGIN
            CREATE ROLE salesanchor_app
              WITH LOGIN PASSWORD 'apppass'
              NOSUPERUSER NOCREATEDB NOBYPASSRLS;
          EXCEPTION WHEN duplicate_object THEN
            RAISE NOTICE 'salesanchor_app already exists, skipping';
          END $$;
          GRANT CONNECT ON DATABASE jarvis_test_db TO salesanchor_app;
          -- DML のみ（CREATE は付与しない。CI role = 本番 role の契約）
          GRANT USAGE ON SCHEMA public TO salesanchor_app;
          GRANT SELECT, INSERT, UPDATE, DELETE
            ON ALL TABLES IN SCHEMA public TO salesanchor_app;
          GRANT USAGE, SELECT
            ON ALL SEQUENCES IN SCHEMA public TO salesanchor_app;
          -- 将来テーブル（migration 製テーブル等）も自動付与（所有者 = jarvis 確認済）
          ALTER DEFAULT PRIVILEGES FOR ROLE jarvis IN SCHEMA public
            GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO salesanchor_app;
          ALTER DEFAULT PRIVILEGES FOR ROLE jarvis IN SCHEMA public
            GRANT USAGE, SELECT ON SEQUENCES TO salesanchor_app;
          SQL

      - name: Run pytest (full suite + PostgreSQL RLS)
        working-directory: backend
        env:
          # Phase 1-D Sprint 2 で導入した Fernet 鍵
          METADATA_FERNET_KEY: ${{ steps.fernet.outputs.key }}
          # Webhook / OAuth 周辺で必要な環境変数（テスト用ダミー値）
          META_VERIFY_TOKEN: ci-test-verify-token
          META_APP_SECRET: ci-test-app-secret
          META_APP_ID: ci-test-app-id
          META_OAUTH_REDIRECT_URI: https://test.example.com/channels/oauth/callback
          # ENFORCE_METADATA_FERNET_KEY=1 で lifespan の fail-fast を有効化
          ENFORCE_METADATA_FERNET_KEY: "1"
          # SA-19: salesanchor_app（NOSUPERUSER NOBYPASSRLS）で接続。
          # RLS は SUPERUSER に対して FORCE でもバイパスされるため、専用の最小権限ロールで接続する。
          # DDL（CREATE TABLE / ALTER TABLE / CREATE POLICY）は RLS_ADMIN_DATABASE_URL（jarvis）が担当。
          # CI role = 本番 role の契約: salesanchor_app は DML のみ（CREATE は付与しない）。
          RLS_TEST_DATABASE_URL: postgresql+asyncpg://salesanchor_app:apppass@localhost:5432/jarvis_test_db
          # 管理者接続（jarvis）— テスト用テーブルの DDL と種データ投入専用
          RLS_ADMIN_DATABASE_URL: postgresql+asyncpg://jarvis:testpass@localhost:5432/jarvis_test_db
          # Sprint 2 Reviewer Out-of-scope #1 (PR #510 follow-up) は別 Issue で起票推奨。
          # 本 PR では各テスト側 (test_super_admin_*.py / test_tenant_admin_*.py 等) で
          #   os.getenv("TEST_PG_URL") or os.getenv("RLS_TEST_DATABASE_URL")
          # の alias パターンに統一済み + conftest.py で env 補完済み。
          # CI workflow に TEST_PG_URL を明示 export すると、以前は skip されていた
          # inventory 系テストが PG migration / seed 未投入で大量に失敗するため、
          # CI workflow 側の export は別 PR (CI 環境への migration 適用とセット) で扱う。
          # Redis（既存 OAuth state ストレージ等で利用）
          REDIS_URL: redis://localhost:6379/0
          # FastAPI lifespan 内でアクセスするため
          ENVIRONMENT: test
          # Sprint 4 (F4): Gemini 2.5 Flash 実 API テスト用 (AC4.1)。
          # GitHub Secrets に未登録だと test_inventory_parser_llm_real_api.py が SKIP される
          # （課金抑制のため）。SKIP_REAL_LLM_TESTS=1 を指定すれば常に SKIP 可能。
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          pytest -q --tb=short

      # Phase 1-E F3-S2 v2 で test_rls_tenant_meta_config.py の fixture を修正後、
      # RLS テストを有効化する（services.postgres は workflow に既に組み込み済）。
      # 現在は SQLite skip で skip 件数 3 件のまま。

  # ルールセット必須 Check の名前と一致させる。always() で常に実行し、
  # - backend 変更あり: pytest-run と lint-backend 両方の結果を集約
  # - backend 変更なし（frontend-only）: 両ジョブが skipped → 成功として報告
  pytest:
    name: pytest (SQLite + PostgreSQL RLS)
    needs: [detect-changes, pytest-run, lint-backend]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Report results
        run: |
          pr="${{ needs.pytest-run.result }}"
          lint="${{ needs.lint-backend.result }}"
          echo "pytest-run: $pr / lint-backend: $lint"
          if [[ ("$pr" == "success" || "$pr" == "skipped") && \
                ("$lint" == "success" || "$lint" == "skipped") ]]; then
            echo "OK: all checks passed or backend not changed"
            exit 0
          else
            echo "FAIL: pytest-run=$pr lint-backend=$lint"
            exit 1
          fi
