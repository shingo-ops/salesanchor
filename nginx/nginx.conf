# Nginxバージョン情報を隠蔽（攻撃者にヒントを与えない）
server_tokens off;

# レート制限ゾーン定義
# ログインAPI: 1分間に10リクエストまで（ブルートフォース攻撃防止）
limit_req_zone $binary_remote_addr zone=login:10m rate=10r/m;
# 一般API: 1秒間に30リクエストまで（API乱用防止）
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;

# HTTP設定 - HTTPSへリダイレクト
server {
    listen 80;
    server_name jarvis-claude.uk;

    # Let's Encrypt証明書検証用
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # その他のリクエストはHTTPSにリダイレクト
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS設定 - リダイレクト専用（Phase 5 移行完了・2026-06-10）
# backend/frontend コンテナへの直接アクセスを廃止し、全パスを app.salesanchor.jp へ転送する。
# SSL 証明書と DNS は引き続き維持（リダイレクトが機能するために必要）。
server {
    listen 443 ssl;
    server_name jarvis-claude.uk;

    ssl_certificate /etc/letsencrypt/live/jarvis-claude.uk/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jarvis-claude.uk/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # HSTS（既存ブラウザキャッシュとの整合性維持）
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 全パスを app.salesanchor.jp へ 301 永続転送
    location / {
        return 301 https://app.salesanchor.jp$request_uri;
    }
}

# ============================================================
# Phase 5: app.salesanchor.jp - 本番アプリ（jarvis-claude.uk と並行稼働）
# 既存 jarvis-claude.uk 設定をミラー。Phase 5 完了後の段階的切替で利用
# ============================================================
server {
    listen 80;
    listen [::]:80;
    server_name app.salesanchor.jp;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name app.salesanchor.jp;

    ssl_certificate     /etc/letsencrypt/live/app.salesanchor.jp/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.salesanchor.jp/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # セキュリティヘッダー（jarvis-claude.uk と同一）
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' https://www.gstatic.com; connect-src 'self' https://*.googleapis.com https://*.firebaseio.com; style-src 'self' 'unsafe-inline'; img-src 'self' data:" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    add_header X-XSS-Protection "1; mode=block" always;

    client_max_body_size 10m;
    # ADR-133: Docker 内部 DNS（127.0.0.11）を valid=5s で動的再解決
    # → backend/frontend 再起動後に nginx 操作なしで自動復帰する恒久解
    resolver 127.0.0.11 valid=5s;
    set $backend backend;
    set $frontend frontend;

    location /api/v1/auth/ {
        limit_req zone=login burst=5 nodelay;
        limit_req_status 429;

        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;
        proxy_connect_timeout 10s;
        proxy_send_timeout    60s;
        proxy_read_timeout    120s;
    }

    # Phase 2 SSE: conversations/stream 専用（/api/ より先に評価される完全一致）
    location = /api/v1/conversations/stream {
        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;

        # SSE 必須: バッファリング無効 + 長時間タイムアウト
        proxy_buffering           off;
        proxy_cache               off;
        proxy_read_timeout        3600s;
        proxy_send_timeout        3600s;
        proxy_connect_timeout     10s;
        proxy_http_version        1.1;
        chunked_transfer_encoding on;
        # limit_req は SSE 長時間接続には不適切（バックエンド側で接続数制限）
    }

    # Phase 3 SSE: leads/stream 専用（/api/ より先に評価される完全一致）
    location = /api/v1/leads/stream {
        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;

        # SSE 必須: バッファリング無効 + 長時間タイムアウト
        proxy_buffering           off;
        proxy_cache               off;
        proxy_read_timeout        3600s;
        proxy_send_timeout        3600s;
        proxy_connect_timeout     10s;
        proxy_http_version        1.1;
        chunked_transfer_encoding on;
        # limit_req は SSE 長時間接続には不適切（バックエンド側で接続数制限）
    }

    location /api/ {
        limit_req zone=api burst=50;
        limit_req_status 429;

        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;
        proxy_connect_timeout 10s;
        proxy_send_timeout    60s;
        proxy_read_timeout    120s;
    }

    location /grafana/ {
        proxy_pass http://49.212.160.98:3000/grafana/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        # GrafanaはReact/Angularバンドルに'unsafe-eval'が必要（グローバルCSPを上書き）
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline'; connect-src 'self' ws: wss:; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:;" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    }

    location /status/ {
        proxy_pass http://49.212.160.98:3001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # ================================================================
    # SA設計図書サイト（ADR-134）— Basic認証付き静的配信
    # デフォルトdeny: htpasswdファイルが読めない/消えた場合nginxは500を返す
    # （htpasswdが消えた場合に401ではなく500になる = fail-closed 構造）
    # ================================================================
    # /design（末尾スラッシュなし）→ /design/ へ 301 リダイレクト（ADR-134）
    # location /（SPA catch-all）に落ちてアプリ画面を返す挙動を防止する
    location = /design {
        return 301 /design/;
    }

    location /design/ {
        alias /var/www/design-site/;
        auth_basic "SA Design Site";
        auth_basic_user_file /etc/nginx/htpasswd.d/design-site;
        try_files $uri $uri/ =404;

        add_header X-Frame-Options "DENY" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Cache-Control "no-store, no-cache" always;
    }

    location = /metrics {
        allow 49.212.160.98;
        deny all;
        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }

    location / {
        proxy_pass http://$frontend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================================
# Phase 5: api.salesanchor.jp - API 専用サブドメイン
# Meta Webhook (Phase 5 で C3 切替) / Data Deletion Callback (B 実装後) で使用
# フロントエンド・管理画面は提供しない（API only）
# ============================================================
server {
    listen 80;
    listen [::]:80;
    server_name api.salesanchor.jp;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name api.salesanchor.jp;

    ssl_certificate     /etc/letsencrypt/live/api.salesanchor.jp/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.salesanchor.jp/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # API ドメインは JSON 返却前提だが、502/504 等の nginx error page が HTML で
    # 返る defense-in-depth として CSP を最小権限で固定
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Content-Security-Policy "default-src 'none'; frame-ancestors 'none'" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    client_max_body_size 10m;
    # ADR-133: Docker 内部 DNS（127.0.0.11）を valid=5s で動的再解決
    # → backend 再起動後に nginx 操作なしで自動復帰する恒久解
    resolver 127.0.0.11 valid=5s;
    set $backend backend;

    location /api/v1/auth/ {
        limit_req zone=login burst=5 nodelay;
        limit_req_status 429;

        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;
        proxy_connect_timeout 10s;
        proxy_send_timeout    60s;
        proxy_read_timeout    120s;
    }

    # Phase 2 SSE: conversations/stream 専用（/api/ より先に評価される完全一致）
    location = /api/v1/conversations/stream {
        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;

        # SSE 必須: バッファリング無効 + 長時間タイムアウト
        proxy_buffering           off;
        proxy_cache               off;
        proxy_read_timeout        3600s;
        proxy_send_timeout        3600s;
        proxy_connect_timeout     10s;
        proxy_http_version        1.1;
        chunked_transfer_encoding on;
        # limit_req は SSE 長時間接続には不適切（バックエンド側で接続数制限）
    }

    # Phase 3 SSE: leads/stream 専用（/api/ より先に評価される完全一致）
    location = /api/v1/leads/stream {
        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;

        # SSE 必須: バッファリング無効 + 長時間タイムアウト
        proxy_buffering           off;
        proxy_cache               off;
        proxy_read_timeout        3600s;
        proxy_send_timeout        3600s;
        proxy_connect_timeout     10s;
        proxy_http_version        1.1;
        chunked_transfer_encoding on;
        # limit_req は SSE 長時間接続には不適切（バックエンド側で接続数制限）
    }

    location /api/ {
        limit_req zone=api burst=50;
        limit_req_status 429;

        proxy_pass http://$backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;
        proxy_connect_timeout 10s;
        proxy_send_timeout    60s;
        proxy_read_timeout    120s;
    }

    # API 以外は 404
    location / {
        return 404;
    }
}

# ============================================================
# monitor.salesanchor.jp - Uptime Kuma 専用監視ダッシュボード
# ADR-069 参照。VPS で初回起動前に certbot での証明書取得が必要:
#   certbot certonly --webroot -w /var/www/certbot -d monitor.salesanchor.jp
# さくらインターネット DNS: monitor.salesanchor.jp A レコード → VPS IP（49.212.137.46）を追加すること
# ============================================================
server {
    listen 80;
    listen [::]:80;
    server_name monitor.salesanchor.jp;

    # Let's Encrypt 証明書検証用
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name monitor.salesanchor.jp;

    ssl_certificate     /etc/letsencrypt/live/monitor.salesanchor.jp/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/monitor.salesanchor.jp/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Uptime Kuma へのリバースプロキシ（WebSocket 必須）
    location / {
        proxy_pass http://49.212.160.98:3001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400s;
    }
}

# ============================================================
# salesanchor.jp - 独立した静的サイトホスティング
# Production-only: ssl_certificate と www/salesanchor 配下のコンテンツは
# VPS でのみ用意される。dev 環境でこの server ブロックを起動するには
# Let's Encrypt 証明書 mock + コンテンツの配置が必要（別途対応）。
# ============================================================
server {
    listen 80;
    listen [::]:80;
    server_name salesanchor.jp www.salesanchor.jp;

    # Let's Encrypt 更新用
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # その他は HTTPS へリダイレクト
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name salesanchor.jp www.salesanchor.jp;

    ssl_certificate     /etc/letsencrypt/live/salesanchor.jp/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/salesanchor.jp/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    root /var/www/salesanchor;
    index index.html;

    # クリーンURL対応（Meta API 申請等で必須のページ）
    location /privacy {
        try_files $uri $uri/ /privacy/index.html;
    }

    location /terms {
        try_files $uri $uri/ /terms/index.html;
    }

    location /data-deletion {
        try_files $uri $uri/ /data-deletion/index.html;
    }

    location / {
        # Phase 1-E M-DYN-2 (2026-05-02): 存在しないパスは soft 404 (LP HTML 200) ではなく
        # 専用 404 ページを返す。Astro がビルドする /404.html を error_page で返却。
        try_files $uri $uri/ =404;
    }

    # 404 専用ページ（Astro lp/src/pages/404.astro でビルド）
    error_page 404 /404.html;

    # セキュリティヘッダー
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}
