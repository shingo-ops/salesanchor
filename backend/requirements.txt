fastapi==0.136.3
starlette==1.0.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.38
asyncpg==0.31.0
psycopg2-binary==2.9.10
python-dotenv==1.2.2
pydantic[email]==2.10.6
pydantic-settings==2.13.1
bcrypt==4.2.1
phonenumbers==8.13.52
pycountry==24.6.1
firebase-admin==6.6.0
python-jose[cryptography]==3.5.0
httpx==0.28.1
redis[hiredis]>=5.0.0
celery[redis]>=5.4.0
# Meta Data Deletion Callback の Form パース用（B1）
python-multipart==0.0.29
# Discord Gateway worker (ADR-009)
discord.py==2.4.0
# Phase 1-D: Fernet 暗号化（Meta Page Access Token の保管）
# python-jose[cryptography] が transitive に持ち込むが、用途が異なるため直接依存として明示
cryptography==46.0.7
# Sprint 4 (F4): Gemini 2.5 Flash LLM フォールバック（在庫メッセージ解析）
# structured output (response_mime_type=application/json + response_schema) を使うため 0.8.0+
google-generativeai>=0.8.0,<0.9.0
# Sprint 8 (F8): PO PDF レンダリング (発注書 PDF + alias 置換 + 敬称分岐 + テナント名義)
# reportlab: 純 Python (BSD-3-Clause)、商用 OK、TrueType 日本語フォント embedding 対応
# 日本語フォント (NotoSansJP / IPA) は OS 側 or 環境変数 PO_PDF_FONT_PATH で参照
reportlab>=4.0.0,<5.0.0
# ADR-129: pypdf — FedEx 公式 Label Validation カバーシートのフォームフィリング用
pypdf>=4.0.0,<6.0.0
# Google Calendar OAuth 連携（tenant_google_calendar_config）
google-api-python-client>=2.100.0,<3.0.0
google-auth-oauthlib>=1.2.0,<2.0.0
google-auth-httplib2>=0.2.0,<1.0.0
# Sprint 3 (monitoring): FastAPI メトリクスを /metrics エンドポイントで Prometheus に公開
# rate_limit.py / audit.py の _SKIP_PATHS で除外済み
prometheus-client==0.25.0
