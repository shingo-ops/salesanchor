# 設計 — fix-extraction-rules-api-path

**対象ADR**: なし（バグフィックスのみ）
**recon**: docs/handoff/fix-extraction-rules-api-path/recon.md
**日付**: 2026-09-24
**担当**: Planner

---

## 外部・過去事例の参照と我々への応用

該当なし: 今回は `api.get()` / `api.patch()` のベースURL自動付加仕様に起因する APIパス文字列の重複バグ修正であり、設計判断を要しないため外部事例の参照は不要と判断。

---

## 受け入れ基準

| 基準 | 検証方法 |
|------|---------|
| 仕入元一覧が 404 にならず表示される | ブラウザで /super-admin/supplier-extraction-rules にアクセスし、一覧が表示されることを確認 |
| 詳細取得が正常に動作する | 一覧行クリック後に詳細パネルが表示されることを確認 |
| ルール保存が正常に動作する | フォームを編集して保存ボタンを押し、成功メッセージが出ることを確認 |

---

## 技術 How・KPI

- KPI: 3つの API エンドポイントが 404 → 200 に変わる
- 技術選択: パス文字列から `/api/v1` プレフィックスを削除する（3箇所）

---

## 弊害・トレードオフ

- なし: API エンドポイント自体は変わらない。パスの記述方法を正しい形式に修正するだけ。

---

## 計画票

| ステップ | 内容 | 担当 |
|---------|------|------|
| 1 | SupplierExtractionRulesPage.tsx の3箇所のパス文字列から `/api/v1` を削除 | Generator |

---

## 継続

- 完了後の監視: 本番デプロイ後に /super-admin/supplier-extraction-rules の動作確認
- 次フェーズへの引き継ぎ: なし

---

## 維持の仕組み

- 守り手: 人手で守る（api.get()/api.patch() を使う際は /api/v1 を含めないコーディング規約を遵守）
