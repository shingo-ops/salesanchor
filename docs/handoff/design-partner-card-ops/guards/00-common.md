## 0. 共通（どの作業でも読む）

| 止まる形 | 回避形 | 出所 |
|---|---|---|
| コマンドが cd パス で始まらない → $PWD で判定され git commit が止まる | 全コマンドを cd 作業台のフルパス && で始める。変数代入・echo・グループ化を先頭に置かない | 実測・4セッションで再現 |
| 1行に複数コマンドを並べる → 構文エラーで着手前に停止 | 1手順1コマンドにする | 2026-09-07 実測（2回） |
| 検索語にバッククォートを含める → コマンド置換と解釈され構文が壊れる | 別の語で探す | 2026-09-07 実測 |
| 報告ファイルのパスを実行役が誤記 → no such file or directory | パスは一字一句そのまま使う、とカードに明記する | 2026-09-07 実測（2回） |
| ~/.claude を検索対象にする → 全セッションの会話ログが出て打ち切られる。他セッションの内容が流出する | 検索対象にしない | 2026-09-07 実測 |
| 出力に上限が無い → 転記崩れ・ディスク圧迫 | head -N / tail -N を必ず付ける | CARD-BENCH-RECON-01（32GB） |
| 出力ファイルを列挙対象のディレクトリに置く → 自分を自分に書き足す | 出力先は列挙対象の外（/tmp/CC報告ファイル/） | 同上 |
| sleep で待つ → Codex が30秒で打ち切る | 待たない。次便で確認する | 2026-09-07 実測 |
| gh run view / gh run watch | gh-scope-guard が既定拒否。gh pr checks で判定する | 実測 |
| カードにプレースホルダを残す → 実行役が補わず着手前に停止 | 未確定の値は、それを作る手順を先に置く | 2026-09-07 実測 |
| 散文で「〜に本文を書く」と頼む → 実行役は組み立てず、確認だけ実行して停止 | ファイルを作るコマンドを書く | 2026-09-07 実測 |
| カードで頼めることを地の文で頼む | 実行役は正しく拒否する。実装依頼は必ずカード方式にする | 2026-09-08 実測・設計パートナーが2回破った |
| 道具の仕様が分からず推測で切り分ける | 実行役に公式ドキュメントで確認させる。今日は切り分けに5便使ってから聞き、聞いたら1便で解決した | 2026-09-08 実測 |
| grep の Perl 正規表現と日本語の文字範囲 | BSD grep では両方使えない。否定先読みは抜いてから除く2段階で書き、日本語の範囲は ASCII 以外を表す形で書く | 2026-09-08 実測 |
