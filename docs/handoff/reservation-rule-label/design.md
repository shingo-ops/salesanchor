<!-- バッククォート内のパスはリポジトリルートからのフルパスのみ（ゲートが実在確認する）。
     このPRに含まれないファイル・リポジトリ外のパスはバッククォートを付けない。
     守り手: はファイルパスか「人手で守る」のみ -->
# Phase 3 設計 — reservation-rule-label

**対象ADR**: ADR-027  
**recon**: docs/handoff/reservation-rule-label/recon.md  
**日付**: 2026-09-20  
**担当**: Planner

---

## 外部・過去事例の参照と我々への応用

- 該当なし：翻訳ファイルの表示値のみを変更する単純なリネームであり、外部事例を参照する必要はない。i18n管理はADR-027に従いt()経由で既に適切に実装済み。

---

## 受け入れ基準

| 基準 | 検証方法 |
|------|---------|
| サイドバーに「予約ルール」と表示される | ブラウザ確認（解析管理ページ） |
| パネル見出しに「予約ルール」と表示される | ブラウザ確認（解析管理 > 予約ルールパネル） |
| 英語切替時に「Reservation Rules」と表示される | ブラウザで言語切替後確認 |
| 翻訳キーの整合性 | `npx vitest run` でi18n関連テスト確認 |

---

## 技術 How・KPI

- KPI: ja.json と en.json の各2行計4行を変更することで、全画面で表示ラベルが即時反映される
- 技術選択: i18n翻訳ファイルの値変更のみ（ADR-027準拠）。コンポーネント・キー名・APIの変更なし

---

## 弊害・トレードオフ

- リスクなし：表示名の変更のみであり、内部ロジック・DB・APIに影響しない

---

## 計画票

| ステップ | 内容 | 担当 |
|---------|------|------|
| 1 | ja.json 2行変更（sidebar + title） | Generator |
| 2 | en.json 2行変更（sidebar + title） | Generator |
| 3 | grep確認で残存「日付ルール」「Date Rules」がないことを確認 | Generator |
| 4 | vitest run で既存テスト通過確認 | Generator |

---

## 継続

- 完了後の監視: なし（ラベル変更のみ）
- 次フェーズへの引き継ぎ: なし

---

## 維持の仕組み

- 守り手: ADR-027（i18n強制・全UIラベルはt()経由。ESLintとlint-stagedによる自動チェック）
