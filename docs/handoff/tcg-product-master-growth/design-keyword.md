---
mode: handoff
---

# design — TCG商品マスタ キーワード整備

この文書は何か（専門用語なしの1行）: 商品の辞書（キーワード）を、根拠を持って増やし・直し続けるための決め事。

親（設計仕様書）へのリンク: ../../specs/product-master/README.md
現在地（recon）へのリンク: ./recon.md

- 仕事名: tcg-product-master-growth（キーワード整備）
- 日付: 2026-09-08（§0 と §6 を実測で訂正。MULTI 0件の記載を取り消した。初版は 2026-09-07）
- 実測時の origin/main SHA: a5acd7c178a9d5f3375a335e60f70431911eb2b2（前版は 8d6e9bb107ddf8c2996af4dd56e900adbbb9f028、初版は 9a4909b9d46f1455c872cdd439c878cb78123635）
- 対象ADR: ADR-154
- 対象テナント: tenant_004（本番実データ）
- 区分（STANDARD-WORKFLOW 1.8）: 既存の延長・修正

注: 同ディレクトリの design.md は「配信の未完了ガード」の設計であり、本書とは別テーマである。

## 0. 前提の実測値（2026-09-08 実測）

出所: CARD-PMG-KW-RECON-01 / CARD-PMG-KW-DBCHECK-02 / CARD-PMG-KW-COLS-03 / CARD-PMG-KW-REANALYSIS-04。いずれも tenant_004 を読み取り専用（transaction_read_only=on）で実測した。

- analysis_results: 15,978件。解決 13,084件 / 未解決 2,894件（NONE 2,573・MULTI 321）。2026-09-08 05:03 UTC 実測（CARD-PMG-321-DB-01）。
- MULTI: 321件・11種。同日の先行測定で「0件」とした記載は誤りであり、取り消す。pid_basis の実際の値は MULTI という4文字ではなく、MULTI(PM.PM0007/PM.PM0008/PM.PM0009):要確認 のように商品コードと要確認が付いた文字列である。完全一致では数えられない。
- MULTI の内訳（2026-09-08 実測・全11種。count(DISTINCT pid_basis) が 11 であることで網羅を確認した）: PM0007/PM0008/PM0009 が 235件、PM0127/PM0059 が 30件、PM0010/PM0007/PM0008/PM0009 が 17件、PM0267/PM0190 が 13件、PM0064/PM0074 が 11件、PM0049/PM0074 が 7件、PM0161/PM0007/PM0008/PM0009 が 3件、PM0199/PM0230 が 2件、PM0274/PM0273/PM0272 が 2件、PM0272/PM0274/PM0273 が 1件。
- 同日 05:03 より前の測定値。上書きせず残す: analysis_results 15,816件 / 解決 12,951件 / 未解決 2,865件（NONE 2,544・MULTI 321）。当時これを「その他の根拠 321」「MULTI 0件」と書いたが、321 は MULTI である。
- analysis_results と extraction_items を結ぶ列は extraction_item_id（一意制約つき外部キー・ON DELETE CASCADE）。2026-09-08 実測。
- 件数は測った時点の値である。取り込みが常時稼働しており、同一セッション内の測定でも 14,931 から 15,816 まで増加した。
- 再解析: 未実施。updated_at と computed_at の日別分布が一致しており、2026-09-04 から 09-06 に解析された10,542行は以降ひとつも書き換えられていない。
- engine_version: name-first-v2 の1種のみ。
- キーワード整備 migration の本番適用: 適用済み。削除5本が残0本・追加7本が実在・PM0071 の綴りが Golden・PM0146 の is_active が偽・バックアップ表2本が実在。適用はデプロイ経由で自動である（.github/workflows/deploy.yml:458 が scripts/run_all_migrations.sh を呼ぶ）。
- 初版（2026-09-07）の値。上書きせず残す: extraction_items 12,045件 / analysis_results 12,045件 / 解決 9,898件 / 未解決 2,147件（NONE 1,905・MULTI 242）。
- 2026-09-08 に測り直していない値（初版のまま）: tcg_products 296件 / product_search_keywords 630件 / product_exclude_keywords 134件 / 未解決の商品名原文 580種 / 商品あたり検索キーワード数（2026-09-06 時点）: 1本=140商品、2本=55、3本=54、4本=29、5本=10、6本=1、7本=4、9本=2、0本=1

## 1. キーワード登録の原則（PO定義）

検索キーワードは「網」である。取りこぼしを減らすために、正規の商品名の一部と省略呼称（例: ポケモンセンターセット / ポケセン / ポケセンセット）を登録する。網の段階で複数の商品が候補になっても構わない。

除外キーワードは「壁」である。網で捕まえた中から誤解析を防ぐために、類似商品にだけ含まれる語を登録する。類似商品にしか含まれない語を選ぶほど壁は強固になる。

特定版（DX版など）は、識別語を含む形だけを検索語にし、素の名前は入れない。
通常版は、素の名前を検索語にし、識別語を除外語に入れる。
「その語を含まなければ取らない」という設定は不要である。検索キーワードそのものがその役割を果たす。

実例（2026-09-07 実測）: PM0165 は検索語「ブラックボルト DX」「ブラックボルトDX」を持ち素の「ブラックボルト」を持たない。PM0167 / PM0168 は除外語「DX」を持つ。DX を含む原文12種はすべて解決済みである。

## 2. 照合の仕組み（コード実測に基づく）

照合の前に2段の正規化が掛かる。

第1段: tcg_normalization_rules の PRODUCT_NAME ルール22件（tcg_analyzer_svc.py:917 で適用）。装飾記号 ● ◆ ◎ ▪ ▫ ■ □ ★ ☆ ・ ※ ▶ ▷ ▼ ▽ ◉ ⚫ ⭐ と絵文字の異体字セレクタ・ゼロ幅接合子を削除し、全角ハイフン「－」を長音「ー」に置換する。

第2段: normalize_en（tcg_analyzer_svc.py:220）。全角の英数記号（U+FF01–FF60）を半角に変換し、小文字化する。商品名原文とキーワードの両方に掛かる。

照合の順序（match_keyword・tcg_analyzer_svc.py:276）:
1. 除外キーワードを順に照合する。1本でも当たれば、その商品は即座に候補から外れる。検索キーワードは試されない。
2. 検索キーワードが1本も無い商品は候補にしない。
3. 検索キーワードを順に照合し、当たった商品を候補にする。

1キーワードの照合（match_one_kw）:
- 純ASCII語は単語境界で照合する（前後が英小文字でない位置）。
- 日本語混じりは空白で区切ったトークンのAND照合。全トークンが含まれれば一致。

候補の数による分岐:
- 0件 → NONE（未解決）
- 1件 → 解決済み（pid_basis に SK:キーワード が入る）
- 2件以上 → MULTI（未解決）。最長一致の1件が product_id に入るが解決済みにはならない。

## 3. 書き方の決まり

- 記号（◆ ■ ★ ・ ※ など第1段で消える22種）はキーワードに入れない。自動で消えるため無駄になる。
- 全角英数と大文字小文字は区別されない。ＤＸ・DX・dx は同じ語として扱われる。全角スペースも半角スペースになる。
- カタカナ・ひらがな・半角カナの揺れは自動で揃わない。揺れがあるなら両方を登録する。
- 日本語混じりのキーワードは空白で区切ると「すべてを含む」の意味になる。空白を入れた形のほうが広く当たる。
- 純英数のキーワードは単語境界で照合される。

## 4. 守り手（4本）

① 同じキーワードが2商品以上に付いていない。
   2026-09-07 時点の違反: 1種（キーワード「AR」が PM0007 / PM0008 / PM0009 に付いている）。
   理由: 検索キーワードは1本でも当たれば候補になるため、共有キーワードは候補2件以上（MULTI・未解決）を生む。

② 有効な商品は検索キーワードを1本以上持つ。
   2026-09-07 時点の違反: 0件（PM0146 を無効化して解消）。
   理由: 検索キーワードが空の商品は候補にならない（tcg_analyzer_svc.py:308）。何にもヒットしない状態で残り続ける。

③ 除外キーワードが自分自身を弾いていない（自己矛盾の禁止）。
   検査方法: 除外キーワードが、同じ商品の商品名または検索キーワードに文字列として含まれていないか。
   2026-09-07 時点の違反: 0件（5件を是正して解消）。
   理由: 除外語は検索語より先に照合されるため、自分の名前の一部を壁にすると、その語が原文に書かれた瞬間に自滅する。

④ 実データ命中検査。
   キーワードごとに、どの商品名原文に当たり、どの商品コードに解決されたかを数える。
   長さによる制限は設けない。2026-09-07 の実測で、キーワード「151」を含む原文138行が全件解決済み（うち136行が PM0104・根拠 SK:151）であり、短さは危険の指標にならないことが確認されたため。
   検査は本番DBの読み取りとして migration 投入の前に人が回す。CI は本番DBに接続しないため CI では強制できない。

## 5. 是正済みの記録

2026-09-07・PR #3354（マージコミット 9a4909b9）・migration 20260907_140000_tcg_keyword_hygiene_t004.sql

発見の経緯: 原文「PRB-02 THE BEST vol.2」等が NONE になっていた。照合関数を実際の文字列で呼び出したところ、検索語「PRB-02」は単語境界で当たっていた。原因は PM0172「THE BEST vol.2」の除外語「THE BEST」が自分自身を弾いていたことだった。さらに原文「ONE PIECE CARD THE BEST PRB-02」が PM0137（vol.1）へ誤解決していた。

内容:
- 自己矛盾の除外語5本を削除（PM0172「THE BEST」/ PM0063「イーブイヒーローズ」/ PM0072「25th Anniversary Collection」/ PM0159「熱風のアリーナ」/ PM0126「バトルアカデミー」）
- 壁7本を追加（PM0137 に PRB-02・PRB02 / PM0098 に ポケモンセンター・ポケセン・ジムセット / PM0047・PM0048 に プレシャス）
- 綴り誤り1本を修正（PM0071「25th Aniniversary Goleden Box」→「25th Aniniversary Golden Box」）
- PM0146「スタートデッキGenerations（各種）」を無効化（検索キーワード0件・解決実績0件・該当する商品名原文0件）
- 適用前に tenant_004.tcg_products_bak_20260907 と tenant_004.product_exclude_keywords_bak_20260907 を作成

注: migration のマージだけでは本番DBは変わらない。scripts/run_all_migrations.sh の実行が必要。さらに、キーワードを直しても既存の analysis_results は更新されない。効果を出すには再解析が必要である。

## 6. 未着手の課題

- 未解決2,894件（2026-09-08 05:03 UTC 実測。同日の先行測定は2,865件、初版の記載は2,147件）のうち、原因を特定したのは PRB-02 系と MULTI 321件。NONE 2,573件は未調査。
- 未解決のうち MULTI は321件・11種（2026-09-08 実測）。うち255件は PM0007 / PM0008 / PM0009 が絡むもので、守り手①違反（キーワード AR の共有）が原因である。「NONE でも MULTI でもない根拠」という記載は誤りであり、取り消す。
- PM0267 と PM0190 が競合する MULTI が13件ある（2026-09-08 実測）。recon.md 不明点#3 の重複登録の疑いと同じ対象である。未判定。
- PRB-02 を含む商品名原文のうち4行が PM0137 に解決したまま残っている（根拠 SK:ONE PIECE CARD THE BEST・2026-09-08 実測）。再解析で消える古い判定なのか、追加した壁が効いていないのかは未判定。別セッションが対応中。
- 検索キーワード630本の棚卸しは未実施（件数と一部のみ確認）。
- 兄弟商品の検出は「商品名が丸ごと含まれる」29ペアのみ。略称違い・英名と和名の組み合わせは未検出。
- AR の区別。PM0007「AR/CHR ダブりなし」/ PM0008「ダブりあり」/ PM0009「ダブり2枚まで」は、商品名原文に「被りなし」等の記載が無いかぎり機械的に区別できない。2026-09-08 実測で、これら3商品が絡む MULTI は255件（235＋17＋3）であり、守り手①の違反は現在も残っている。「MULTI が0件になったため件数としては無効」という記載は誤りであり、取り消す。
- 未登録商品の登録。未解決のタイトル別内訳（2026-09-06 実測・重複を含む概算）: ONE PIECE 158、遊戯王 122、UNION ARENA 109、ドラゴンボール 99、ガンダム 69、ポケモン 41。
- 守り手④の実装（検査の自動化）。

## 7. 非目標

- 照合ロジックの変更（ADR-154: GAS実行順序の100%再現が決定事項のため）。
- 正規化ルールの追加・変更（同上の制約に触れるため、本書では扱わない）。
- キーワード表への無効フラグ列の追加（別テーマ。照合コードの変更を伴い ADR-154 に触れる）。
- analysis_results への書き込み（別セッションの担当）。
- 「〆」だけの投稿で在庫が全消えする問題（本テーマ外）。

## 8. 未確認として残すもの

- PRODUCT_NAME_MATCH ルール2件の使用箇所（tcg_analyzer_svc.py:917 の呼び出しには現れていない）。
- 半角カナ・ひらがなの揺れが実データに存在するか。
- 商品名原文「マスターボールミラー151のみ」10行を PM0104 と同一商品として扱ってよいか（業務判断が必要）。
- 同ディレクトリの onepiece-catalog.md / sword-shield-catalog.md の内容と、本書の課題6との重複。


## 9. 2026-09-10 追加変更の設計草案 — 作品による候補制限・限定版の分離

状態: DRAFT / mode: handoff（本追加変更）。既存§0–8の完了事項を変更しない。
recon: docs/handoff/tcg-product-master-growth/recon.md
関連ADR: ADR-154 / ADR-113。§7で対象外だった照合変更を含むため、既存のGAS一致契約をそのまま満たすとは扱わない。

### PO依頼と範囲

PO発話（2026-09-10、実装依頼。最終設計の承認とは区別）:
> 実装してくれ
> 通常版にはコロを除外ワードに登録

対象: (A) Geminiの作品情報抽出と作品IDによる商品候補制限、(B) 通常バトルコレクションへの「コロ」除外とコロコロコミックver.／コロちゃおVer.の区別。先行相談のBOX共通フィルター、状態・備考拡張はこの実装依頼へ自動混載しない。再解析・配信・本番DB変更は別の反映手順を要する。本セッションは設計担当のまま。

### Whyと実物

- 保存誤判定20行のうちガンダム2行はEB01によってワンピースへ確定。作品条件を使っていない照合が原因の一つ（recon参照）。
- `migrations/20260902_110000_tcg_classification_masters.sql:94` の作品seedはIP001 Pokemon、IP002 One Piece、IP006 GUNDAM。IP005 Union ArenaとIP007 Weiss Schwarzも同じ作品マスタに含む。現行マスタの「作品」はゲームブランドを含む分類であり、NIKKEというIP名だけを新しい作品IDにする案は採らない。前節reconの「追加軸が必要」は、この既存分類の実体を確認した本観測で具体化した。
- `gemini_extraction_svc.py:31` は分類禁止・7列固定、同:211は列数7以外を拒否。`backend/app/tasks/tcg_extraction.py:163` は作品欄を保存しない。`tcg_analyzer_svc.py:994` は作品を照合に使わない。3箇所を連動して変更する必要がある。
- 通常版PM0200とコロちゃお版PM0285は手元マスタに存在。コロコロコミックver.は手元の296商品には見つからない。現本番での登録有無は未確認。
- コロコロコミックver.の公式根拠: https://www.pokemon-card.com/info/003230.html 。コロちゃお版を含む公式案内: https://www.corocoro.jp/corociao （2026-09-10確認）。2つの限定版を同一商品に統合しない。

### A: 作品抽出の契約案

- Geminiは各明細について、商品名と対応する見出しから作品／ゲームブランドの原文表記とそのLine IDを返す。根拠のない型番から作品を推測しない。原文にないDB IDは返さない。
- サーバーが作品マスタの表示名・別名と照合し、一意に対応する作品IDだけを使う。根拠文字列の存在と位置を原文で検証する。存在しない位置を先頭行へ補正して有効な作品根拠にしない。
- 作品確定時は、検索に一致した候補を同じwork_idに限定する。不一致や作品ID未設定の商品は確定候補から外す。候補ゼロでも他作品へフォールバックしない。
- 作品不明・複数作品・根拠不正の場合、型番だけの一致は要確認とする。人の訂正は本変更の自動処理で上書きしない。
- 既存抽出データの作品情報欠落、新規出力形式、保存カラム、型番のみ判定の定義、既存の訂正保護との接続は、確定前の契約課題。ここを実装役の裁量に残したままカードを発行しない。

### B: 限定版の辞書変更案

- 通常バトルコレクションの除外へ「コロ」を追加する。既存除外語を一括置換しない。
- コロコロコミックver.とコロちゃおVer.は異なる商品ID。検索語にはそれぞれを区別できる表記を使い、共通の「コロ」を限定版側の検索語にしない。
- 「スタートデッキ100 コロ」だけでは限定版2種を区別せず未解決／要確認。通常版には戻さない。
- 現本番のコード・名称・作品ID・区分ID・検索語・除外語を確認してから名指しの変更差分を発行する。コロコロ版が既存なら再利用し、未登録なら公式商品情報と必須分類を揃えて新規登録する。古い通常スタートデッキ100を今回の通常バトルコレクションに統合しない。

### 受入条件案

| 入力／条件 | 期待結果 |
|---|---|
| ガンダム誤判定2行 | ワンピース確定0件。正しい候補なしなら未解決 |
| ガンダム見出し＋EB01 | 見出しの適用範囲を根拠にガンダム候補のみ |
| EB01のみ・作品根拠なし | ワンピースへ型番だけで自動確定しない |
| 複数作品のメッセージ | 他明細の作品を持ち込まない |
| 通常バトルコレクション名 | 通常版の候補維持 |
| コロコロコミックver.／コロちゃおVer. | 通常版候補0件、限定版相互の誤確定0件 |
| スタートデッキ100 コロ | 通常版にも限定版の一方にも自動確定しない |
| 正常商品・旧抽出・人の訂正 | 正解維持件数／要確認化件数／訂正保持を個別報告 |

実行済みの局所検算: 固定SHAの照合関数へ検索語「スタートデッキ100」・除外語「コロ」を渡す4入力。通常名1件を維持し、限定版2種＋曖昧な「コロ」1件の計3件を通常候補から除外。証跡 `/private/tmp/line-coro-exclusion-20260910.json`。Gemini実験・DB統合試験・最新本番マスタの検算は未実施。

### 代替案・影響・維持

- ガンダム等を個別商品の除外に追加する案は局所的だが、作品をまたぐ型番衝突のたびに追加が必要。作品ID条件は共通の誤一致を防ぎやすいため採用方向。
- 作品不明を要確認にすると自動解決数が減り得る。正解率と自動解決率を別に測る。
- 「コロ」除外は備考の雑誌紹介まで拡張すると正しい通常版を止め得る。本Bでは現行の商品名の除外だけを対象とする。
- 実装役が抽出・保存・照合の回帰試験を保守し、POが正解ラベルを確認する。新商品登録時は同型番の別作品と通常／限定版の対照例を受入データに追加する。

### Architect自己審査（独立した第二者レビューではない）

判定: REVISE。

根拠: Bの「コロ」除外の局所動作は4入力で確認できた。Aの作品マスタ実在と現行処理の不足も確認済み。しかしAの保存形式・後方互換・型番のみの定義・訂正保護の接続が未確定で、ADR-154の一致契約の更新案も未完成。Bは最新マスタにおける2限定版の登録状況を確認していない。未解決前提を実装役へ渡さない。

実装依頼は受領済み。再度「実装してよいか」は求めない。最終設計承認・設計合格・実装カード発行は未了。カード未発行のためcard-lintの合格を称さない。次は上記契約の実物照合と最新マスタの読み取り確認を完了し、ADR-113の整合検査と正式カードチェックを通す。


### 2026-09-10 GO受領後の契約具体化（§9の草案を補完）

PO発話: 「GO」。直前の作品抽出・通常版コロ除外の実装依頼への承認として受領。製品実装役への自動切替、サブエージェント起動、本番操作・マージの承認には読み替えない。本節の具体化内容をPOが逐語承認したとは記録しない。

以下は実装役へ渡す前の契約案。§9の「保存形式・後方互換・型番のみ判定」の未確定点を具体化する。

1. 新しい抽出形式は既存7列の末尾に `RAW_WORK_NAME` と `RAW_WORK_SOURCE_LINE_SPAN` を足した9列。既存7列の意味は変更しない。作品欄は原文の作品／ゲームブランド表記のみ、根拠なしは両列空欄。GeminiにUUIDを生成させない。prompt_versionは新しい値 `raw-extraction-v3-work-p1` とし、旧形式と区別する。API・モデルの変更は本件の対象外。
2. v3の出力は9列で検証する。非空の商品明細に不正な列数があれば、そのジョブはerrorにして部分的な成功として保存しない。空の抽出は既存empty経路。旧7列のパーサは過去応答の読み取り用として残すが、新規v3の不正出力を旧形式として救済しない。
3. `extraction_items` にnullable TEXTの `raw_work_name` と `raw_work_source_line_span` を追加する案。既存行はNULLを維持し、再抽出・再解析をmigrationに混ぜない。既存商品名・状態・備考へ作品情報を埋め込まない。抽出タスクのINSERTと解析側SELECTを同時に対応させる。schema追加は既存のTCGテーブルを持つスキーマに限定し、将来のTCGテーブル作成経路への追加も必要。実際の適用経路はカード発行前に検証する。
4. 作品照合には有効な `tcg_series` の `display_name` と `alt_name` を用いる。作品IDはその行のUUID。IP001等はコードであり、UUIDに代入しない。正規化は既存normalize_enを使う。追加の別名辞書が必要な場合は作品マスタ側の正式な管理方法を確定してから別変更にする。未知の表記を推測で既存作品へ寄せない。
5. 作品証拠は原文に当該文字列が存在すること、Line IDが範囲内であることを検証する。商品名内の作品表記か、当該商品より前の独立した作品見出しを根拠とする。独立見出しはtrim後の行が作品の表示名または別名に一致するもの（外側の `【】` または `[]` の一対は除いてよい）。見出し利用は最も近い先行作品見出しのみ。同じ商品に複数作品が成立する場合は不明扱い。途中の商品行にある作品を次の商品へ引き継がない。推測した見出しや範囲外を先頭へ補正しない。
6. 旧7列データは作品欄NULLとして読み、商品名に明示された作品名／別名から一意に解決できる場合だけ作品を付ける。旧データへのGeminiの再呼び出しはしない。取得済みの作品根拠が不正・未知・複数の場合も不明扱い。
7. 型番語の暫定的な実装定義は、normalize_en後にASCII英字と数字の両方を含み、文字全体がASCII英数字・ハイフン・空白・スラッシュだけからなる検索キーワード。作品不明の場合、この種の検索語だけで成立した候補は自動確定から外す。同じ候補に別の通常商品名の検索語も一致すれば、その根拠を使える。先頭の検索語が型番か否かだけで判定せず、一致した検索語をすべて確認する。残った候補0件は未解決、複数は従来の要確認。
8. 作品確定時は候補商品のwork_id一致が必須。不一致／NULLの候補は除外し、空集合から全作品候補へ戻さない。商品区分の既存絞り込みは維持するが、そのフォールバック後にも作品制約を必ず適用する。`pid_basis`の100文字制約を守って作品制約の有無を記録し、engine_versionは `name-first-v3-work` に更新する案。
9. 再解析の訂正保護は既存の別作業と重複実装しない。実装ブランチ開始時に訂正保護の実装状態を固定SHAで確認する。保護がなければ本件の受入・本番反映は「新規抽出行のみ」に限定し、既存判定の一括更新を許可しない。現在のON CONFLICT更新はproduct_idを上書きするため、旧行の安全な一括再解析まで完成したとは称さない。
10. 旧エンジンでのGAS一致は履歴として保持する。ADR-154に「v3では作品根拠により誤一致を拒否するため、商品判定のGAS一致を成功条件にしない」という追加決定案を提出する。数量・価格・単位・状態の既存処理順序は変更しない。

実装予定の接触箇所: `backend/app/services/gemini_extraction_svc.py`、`backend/app/tasks/tcg_extraction.py`、`backend/app/services/tcg_analyzer_svc.py`、`backend/tests/test_tcg_gemini_extraction.py`、`backend/tests/test_tcg_keyword_matching.py`、TCG schema追加migrationと正規の適用経路。今回これらは編集していない。

追加の受入条件: v3不正列は部分保存0件、作品証拠の範囲外は採用0件、旧7列入力の読み取り成功、ガンダム／ワンピース／ポケモン混在で作品の横漏れ0件、作品不明EB01のワンピース自動確定0件、作品確定後に空候補から他作品へ復帰0件。各正常商品の正解維持件数を別表にする。実行結果未取得のテストをPASSとはしない。

残る審査課題: 新規スキーマへの適用経路、実際の作品別名の保持形式と最新マスタ値、コロコロ版の現在の商品ID／分類、ADR追加決定の正式化。数値・IDを創作せず、最新マスタの読み取り情報を得てから実装カードを名指しで発行する。審査状態はREVISEを維持。GO受領を設計上の欠落の解消とは扱わない。


## 10. DB確認後の実装契約案（§9の未確認事項を更新）

mode: handoff。対象ADR: docs/adr/ADR-154-tcg-parity02-gas-python-migration.md の本PR追加決定案／ADR-113。
本節は§9の契約案を以下の差分で具体化する。POの実装依頼・GOは受領済み。本節の文書承認および本番反映は別の状態として管理する。

### 10.1 変更対象と完成条件

- 作品抽出: §9の9列、2つのnullable TEXT追加、v3厳格形式、原文根拠の検証、旧7列互換を採用する。
- 作品マスタの値は最新DBで取得済み。別名は単一TEXTであり、カンマやパイプで勝手に分割しない。IP001/IP002/IP006は日本語名も登録済み。今回の確実な受入対象はポケモン／ワンピース／ガンダム。IP007の日本語別名不足を、この変更で解消済みとはしない。
- 作品が明示されない明細でも、最も近い独立作品見出しをGeminiが抽出し、サーバーがその根拠を検証する。明細自体に作品があれば明細を優先するが、複数作品の根拠が矛盾する場合は不明とする。Geminiが分類ラベルだけを出し、原文に根拠がなければ採用しない。
- 作品IDによる絞り込みと型番語のみの自動確定禁止は§9の定義を使う。§9の暫定定義を具体化し、型番語はnormalize_en後の全体が `[a-z0-9]+(?:\s*[-/]\s*[a-z0-9]+)*` に一致し、ASCII英字・数字を各1文字以上含む検索語とする。通常の英語の商品名を誤って型番扱いしないため、ハイフン／スラッシュ前後以外の語間空白は許さない。英字だけ・数字だけの語も本件の型番語には含めない。これは全ての誤商品の防止ではなく、本件の型番衝突への対策である。
- 除外語の入力を同一明細の商品名・状態・備考へ拡張する。検索語の入口は商品名を維持する。各欄に対し独立に既存match_one_kwを実行し、どれか1欄で除外成立ならその候補を外す。複数語のANDを欄またぎで成立させない。メッセージ全文は除外入力にしない。既存正規化後の商品名／状態／備考を使う。
- この除外入力の拡張は、商品名だけに「コロ」を追加する案では備考のみの実例1行を防げないというDB実測により、本件の必要変更に含める。SAR/AR等の共通除外語追加や英字境界ルールの変更は本便に含めない。
- PM0200の既存除外5語を維持して「コロ」を1語追加する。PM0285のID・検索語を維持する。「コロ」を限定版の検索語にはしない。今回の除外効果で備考のみの1行は未解決となり、PM0285へ自動解決するとは約束しない。
- コロコロ版は最新の全296商品の和英名・検索語でも登録なし。新規登録は本カードでは行わず、未登録商品の一覧へ残す。作品による誤一致防止と新商品登録を分離する。ガンダムEternal Nexusも同様。

### 10.2 スキーマ適用・既存行・訂正

追加migrationは `migrations/20260910_160000_tcg_work_evidence.sql` とする。名前の衝突が実装開始時にあれば、勝手に改名せず設計へ差し戻す。既存migrationは書き換えない。

- `DO` ブロックで `pg_namespace` のtenantスキーマを走査し、`extraction_items` と `extraction_jobs` の双方が実在する場合だけ2列を `ADD COLUMN IF NOT EXISTS` する。テーブル不在のテナントへTCG表を新設しない。tenant_006へTCG表を増やさない。削除・型変更・既存行の再解析なし。
- `scripts/run_all_migrations.sh` の末尾に登録する。同スクリプトは全登録SQLを毎回実行する（run_sql:既存関数確認済み）。したがって初回TCGテーブル作成を経て同ランナーを実行する経路でも後段で2列を追加できる。既存テーブルあり／なし／作成後の再実行／2回実行をPostgreSQLで検証する。
- 辞書の「コロ」追加は別のデータmigration `migrations/20260910_160100_tcg_normal_deck_coro_exclusion.sql` とする。TCG表があるスキーマで、PM0200が存在すればコード＋日本語名＋作品コードIP001を検算してから追加。既存の同一語があれば何もしない。positionは当該商品の最大値＋1。対象商品がなければ何も作らない。名前／作品の不一致は例外で停止し、別商品に投入しない。これも末尾登録し、構造追加の後に置く。
- migrationと新コードの順序は構造追加→コード切替。列がない状態ではv3解析を開始せずエラーにする。旧コードはnullable追加列を無視できるため、コードを戻せても列を削除する必要はない。データmigrationの自動取り消しは行わない。
- 既存抽出・解析データの一括再抽出／再解析はしない。新規行で検証し、旧結果の修復は別カード。
- product_idについて訂正記録がある既存解析行は、本件の再解析入口で行ごとスキップし、件数を `skipped_product_corrections` として報告する。修正済みの商品判定3項目（product_id、pid_resolved、pid_basis）を変えないことをDBテストで確認する。別PRで同等保護が既にあれば重複追加せず、そのテストを本件の受入に含める。これを実装役による仕様変更とはしない。

### 10.2.1 マージ直前の適用順確認と補正（2026-09-10）

観測: `.github/workflows/deploy.yml:160` のコード切替は `:335` でcelery-workerを起動し、DB migrationは後段 `:444–458`。本設計の「構造追加→コード切替」を単一PRの自動反映では満たせない。v3は新列を読み書きするため、列追加前の抽出はエラーになり得る。単体・DB試験はこの本番適用順を検証しておらず、先の実装レビューで見落としていた。マージ未実施の時点で検出した。

補正: 承認済み2本のmigrationとrunner末尾の登録だけを、最新origin/main起点の先行PRへ同一内容で分離する。旧コードで追加列・コロ除外を適用し、先行PRの自動デプロイでバックアップ・両SQLの成功を確認してから #3393へ最新mainを取り込み、既存GOの対象仕様を変えずにコードを反映する。CI/deploy.yml変更や本番DBへの直接書き込みは行わない。先行PRの固有番号に対するGOは別途必要で、#3393のGOを流用しない。

同一AIによる補正の自己審査: APPROVE（適用順の実現方法）。既存コードはnullable列を無視し、2本のSQLは実PostgreSQLで冪等性と同一商品確認を検証済み。先行PRの製品差分は既存承認差分の部分集合。新商品・再解析・機能変更は増やさない。先行適用が確認されるまで #3393 のマージを保留する。

### 10.3 触るファイルと検証

製品の予定差分:
- `backend/app/services/gemini_extraction_svc.py`:9列抽出・パースと原文根拠検証。
- `backend/app/tasks/tcg_extraction.py`:有効作品の表示名／別名を抽出サービスへ渡す。作品原文2列を保存。
- `backend/app/services/tcg_analyzer_svc.py`:作品・原文根拠の読み取り、作品候補制限、型番のみ判定、除外欄拡張、訂正保持。
- `backend/tests/test_tcg_gemini_extraction.py`、`backend/tests/test_tcg_keyword_matching.py`:単体回帰。
- `backend/tests/test_tcg_work_matching_integration.py`:抽出保存→作品付き解析→訂正保持のPostgreSQL統合テスト（新規）。
- 上記2migrationと `scripts/run_all_migrations.sh`。CI・deploy.yml・secrets・UIは対象外。

受入は§9の入力表に加え次を必須とする。

| ケース | 合格条件 |
|---|---|
| 最新DBのガンダム系29保存行相当の判定入力 | 正しい作品を入力した場合のワンピース確定0件。作品なしのEB01型番のみも自動確定0件 |
| コロちゃお備考のみ1行 | PM0200候補0件、未解決かつ要確認 |
| コロちゃお商品名3行 | 最新マスタのPM0285一致を維持 |
| 正常な通常版名 | PM0200一致を維持 |
| 他明細のコロ／PSA10を含むメッセージ | 当該明細に語がなければ誤除外0件 |
| 除外AND語の要素が商品名と備考へ分散 | AND除外は成立しない |
| 作品マスタに対応しない表記、複数・無効作品、空根拠、範囲外根拠 | 自作ID・他作品へのフォールバック0件 |
| 新旧形式・再実行・訂正済み行 | 旧7列の読み取り成功、migration再実行安全、訂正商品3項目の変更0件 |

実際のGemini出力はポケモン／ワンピース／ガンダムの明示、独立見出し、混在、作品不明の小さな匿名化済み標本で別途確認する。分類の正答数／不明数／誤分類数と9列形式失敗数を併記。単体モックだけで抽出精度が改善したとは称さない。本番反映前にこの実測が必要。

通常のBackend品質チェックに加え、既存の解析・配信テストを実行する。DockerなしではPostgreSQL統合PASSとせず、適切なCIまたはQA環境で完了させる。本番DBにテストを書き込まない。

### 10.4 設計審査と引き渡し状態

同一AIがPlannerの後にArchitectとして自己審査。独立レビューではない。

技術契約の判定: APPROVE。根拠は最新DBの296商品・11作品・2区分とNULL 0件、誤判定の有効／無効別集計、最新辞書での候補除外再現、抽出・保存・解析・migration実物の照合。§9のREVISEはこの技術契約に限り解消した。人間の正解率や実装テストの合格は未実測。

ADR-154の追加決定案と本設計は文書レビュー対象。本番反映・既存行修復・新商品登録は別工程。実装カードは本節の文書承認後に有効化する。POの既存GOを再度求めるための条件ではなく、起動指示にある「レビュー済み設計・POカード」を実物として揃える工程である。

## 維持の仕組み

守り手: `backend/tests/test_tcg_keyword_matching.py` / `backend/tests/test_tcg_gemini_extraction.py`（実在する試験基盤に上記受入例を追加する）。新規のPostgreSQL統合試験は実装役が維持する。POは匿名化された正解標本を確認し、新しい作品・別名の追加時に同型番の別作品との対照例を追加する。実装前の段階ではテストによる強制は未成立のため、人手で設計とカードを照合する。


## 外部・過去事例の参照と我々への応用

外部企業の改善率は使わない。本件の根拠は2026-09-10の自システムDB読み取りと同日固定SHAの局所再現であり、対象・時点・件数・適用限界はreconに記録した。GeminiのAPIを新しいAPIへ変更する提案ではない。既存のテキスト抽出形式を拡張し、モデルの分類精度は匿名化標本で実装時に測る。

## 受入基準の検証場所

| 基準 | 検証方法 |
|---|---|
| 別作品への型番単独確定を防ぐ | §10.3の対象入力を単体・PostgreSQL統合で照合 |
| 備考の限定版を通常版へ確定しない | 最新DBで見つかった備考のみ1行の匿名化入力を検証 |
| 文書・カードを実装前に照合できる | card-lint、task-state、差分チェック。実装時のモデル試験は別報告 |


## 11. 誤商品確定を止める辞書3操作（2026-09-10・PO未承認案）

### 11.1 目的・成功条件

POの検索語＝網、除外語＝フィルタの定義に従い、広すぎる網で拾った別商品を自動確定させない。対象標本で誤商品確定10→0、対象外735明細の判定変更0、正式名称293件の判定変更0を成功条件とする。自動確定の件数増加は本便の成功条件にしない。調査・対照検証はPOから依頼済み。この3操作と未確定を許容する事業判断はPO未承認。

### 11.2 変更の全量

| 対象 | 操作 | 変更前→後 |
|---|---|---|
| tenant_004 / PM0230 トライアルデッキ 【推しの子】 / IP007 / PC_SINGLE | 検索語vol.1を完全一致で1語削除 | 検索5→4、その他の語を保持 |
| tenant_004 / PM0104 ポケモンカード151 / IP001 / PC_BOX | 除外語マスターボールミラーを1語追加 | 除外9→10、検索語を保持 |
| tenant_004 / PM0184 スターターセットMEGA メガゲンガーex / IP001 / PC_BOX | 除外語スペシャルデッキセットを1語追加 | 除外1→2、検索語を保持 |

PM0199への検索語追加は採用しない。型番判定・文字境界・Geminiプロンプト・作品見出し・区分判定・UI・価格/数量処理は変更しない。他テナント・他商品の辞書・商品新規登録・過去結果の一括更新・再解析起動・配信は対象外。

### 11.3 実装範囲と適用方法

実装時の変更は `migrations/20260910_170000_tcg_keyword_false_positive_guards.sql`、既存 `scripts/run_all_migrations.sh` の末尾1本登録、既存 `backend/tests/test_tcg_work_matching_integration.py` への当該回帰試験追加、および必要な当該台帳・本設計の検証記録に限定。現時点ではこれらの製品ファイルを変更していない。同名SQLが異なる内容で存在する場合は停止する。

SQLはtenant_004だけを対象に1トランザクションで3操作。tenant_004のTCG表群がない環境は変更0で終了する。対象表群がありながら対象商品が不足、名前・作品・区分が表と不一致なら変更前に例外終了する。全3商品について先に検証してから変更する。vol.1は対象商品との完全一致行だけを削除し、重複が2行以上なら異常として停止。0行は適用済み状態として許容。除外語2つも既存0/1行のみ許容し、未登録の場合だけ既存position最大+1で追加。既存の語・UUID・positionを並べ替えない。

再実行で増減0。他商品・他テナントの行数と内容が不変。初回の期待は検索1行減／除外2行増、再実行は差分0。新規のバックアップテーブルを作らず既存deployのDBバックアップを利用する。既存コードが読む辞書変更なのでDB先行とコード後続の2PR分割は不要。自動deployはマージ後に実行されるため、本番バックアップ・適用確認を含む当該PR固有GOを取得する。

復旧は逆操作を行う別のレビュー済みmigrationとPO承認を必要とする。初案の広い語を無条件に戻すと取り違えが再開するため、自動復旧しない。全体DB復元は他業務へ影響するため本便の復旧手順に含めない。既存解析は辞書反映で書き換わらず、履歴修復は別判断。

### 11.4 代替案と根拠（ADR Whyへ渡す証拠）

- 採用候補: 辞書3操作。実データ745行中10行の誤商品をNONEにし、735行・正式名称293件は判定不変。合成16例合格。既存の除外機構をそのまま使える。
- 初案の検索語追加: REVISE。BASE SHOP vol.10/11等がvol.1商品に誤確定する再現を確認。初回5例では露見せず、境界試験で検出した。
- 全体の文字境界変更: 全商品の既存検索・除外へ影響するため本便では採用しない。別設計と広範な比較が必要。
- BOX共通フィルタ／新規商品: 本便の3操作で止まる10例とは分け、区分・販売単位の定義から設計する。
- 外部事例は不要。既存サービスの辞書変更の成否は当該データと実装で直接測る。

### 11.5 受入検証

| 項目 | 期待・検証方法 | 現在の証拠 |
|---|---|---|
| 反映コードとDB保存判定の再現 | product code / pid_resolved不一致0/745 | 手元再現0、正規化・単位区分・作品根拠を含む |
| 確認済み誤商品 | 匿名化10入力すべてNONE、pid_resolved=false | 手元10/10。実装時は実DB migration後の既存照合で再検証 |
| 対象外 | 比較735入力、正式名称293入力の判定変更0 | 手元0。全入力が正答という意味ではない |
| 正常・状態/備考・版境界 | recon記録の合成16例すべて期待どおり | 手元16/16。実装時の回帰fixtureへ固定 |
| migration境界 | 初回検索-1/除外+2、2回目差分0、対象なし環境変更0、商品同一性不一致・重複で変更前に停止、他商品/tenant不変 | 実装後に使い捨てPostgreSQLで検証する必須条件 |
| 自動チェック | backend静的検査・既存回帰・migration CI成功 | 実装後に実施。Docker不在なら既存CIのPostgreSQLを利用、未実施を成功扱いしない |
| 本番反映 | バックアップ成功→migration成功→辞書差分実物確認→新規解析の対象例を確認 | 個別PR GO後の工程。現在未実施 |

原文全文・顧客情報はテスト/Gitへ入れない。合成と必要最小限の商品表記だけをfixture化する。745行は一時的な実データ全件取得標本であり、永続CIへ顧客データを持ち込まない。商品訂正の保護は既存実装・回帰を維持する。

### 11.6 維持・接触面

担当: POは辞書の採否、実装役は承認済み3操作と回帰、設計担当は原文に基づく結果比較。維持の仕組みは既存migration CIと `backend/tests/test_tcg_work_matching_integration.py`。日々の新しい誤判定・未知の商品は人手の原文照合で監査し、今回の745行試験を全商品の将来保証に使わない。

接触面6面: 人＝未確定10件相当が確認対象になる。エージェント＝新規の実装承認とレビュー済みカードを必要とし、旧GOを流用しない。機械＝runner追加・既存CI、設定変更なし。データ＝tenant_004の3商品だけ、旧解析不変。本番＝既存deployのバックアップとmigrationを経由。外部＝新しい自動確定の誤商品を抑える、API呼び出し仕様・配信変更なし。

### 11.7 Architect自己審査（同一AI）

初案: REVISE。数字が続くVol.10/11の誤一致があり、検索語追加の根拠不足。
修正した3操作案: APPROVE（設計合格、PO承認ではない）。既存v3の名称/状態/備考フィルタと整合し、追加コード不要。実物745行の再現不一致0・対象外変化0・正式名称293件変化0・合成16例で副作用を確認した。tenantと語を限定し、実DB・冪等・同一性検証の完了を実装受入条件に固定。

未決の事業判断: 10例を正しい商品へ自動確定する代わりに未確定へ送る3操作案の採否。技術的実装範囲に未決の補完を任せない。設計案保存済み／自己審査済み／PO承認未了／正式実装カード未発行／実装・本番変更未着手。PO承認後にカードを用意して正式card-lintを行い、実装役の開始条件と照合する。


### 11.8 PO承認と実装・本番反映への移行（2026-09-10）

本セッションでPO原文「承認する、修正から」「本番反映まで実施してくれ、」を順に受領。§11の3操作・未確定許容を承認し、修正・検証・PR・マージ・既存自動deployによる本番反映まで許可された。時分は未取得のため創作しない。過去のGO番号を流用せず、この原文と対象§11を当該PRへ記録する。実装担当は既存委任の同じ1名、rootは文書・読み取りレビュー・承認済みマージと反映確認を担当する。追加サブエージェントなし。機能追加・既存結果一括再解析・配信・安全機構迂回は許可されていない。

§11.3の「TCG表群がない環境」はtenant_004に対象TCG表が1つもない場合の変更0とする。一部だけ存在する場合は不完全な構造として例外停止し、欠落を黙認しない。対象表はtcg_products、tcg_series、tcg_product_categories、product_search_keywords、product_exclude_keywords。実装担当が曖昧さを補完しないため明記した整合補足であり、3操作の変更範囲は同じ。

正式カード: card-keyword-false-positive-guards.md。設計合格の上でPO承認済み、実装・実DB試験・本番確認の実績は別途記録する。


### 11.9 実装時補足と検証（2026-09-10）

同時に辞書を変更する処理との競合を避けるため、対象5表へSHARE ROW EXCLUSIVEのロックを取得してから同一性を検証する。待ち続けないよう、BEGIN→SET LOCAL lock_timeout=5s/statement_timeout=30s→DO→COMMITの単一トランザクションとする。設定を外へ残さない。Context7は利用不可で、実装担当が許可済み代替のPostgreSQL16公式runtime-config-client.htmlを確認し、DO実行前に設定する構成を採用。競合ロックの5秒timeout、全辞書と接続設定の原状保持も実DB試験で確認した。

PR #3400 / 検証HEAD772a09b776eef9f3cfcc729efa7d1be0160a7b6d。Backend run34432985860/job102732312951:2459 passed/93 skipped/302 warnings、88.71秒、coverage61.51%。新規23テストケースはCI使い捨てPostgreSQLで実行、匿名10と合成16を含む。rootもSQL・試験差分・GitHubログを読み取り確認し、追加阻害所見なし。製品ファイル3件は設計どおり。技術チェック成功と本番反映は別の状態。

現時点の停止: process-artifacts gate job102732666119がPO原文「承認する、修正から」「本番反映まで実施してくれ、」を番号付きGOではないとして拒否した。scripts/check-process-artifacts.jsはGO原文中の対象PR番号一致を要求する。包括的なマージ事前承認は受領済みだが、「GO #3400」の原文は未受領。PO原文の代筆、ゲート変更、管理者マージは行わない。安全なファイル編集はapply_patchで成功し、先のshell保存時の誤検知は解消済み。マージ・今回の本番変更・再解析・配信は未実施。

## 12. 配信を止める中断2ジョブの限定復旧（2026-09-10）

### 12.1 目的・承認・根拠

§11は#3400で本番反映・79job/1425明細再解析まで完了（recon末尾）。全3接続への配信は安全装置#8bで停止した。安全装置を維持して中断2件を復旧し、有効な原文1件の抽出・解析完了後に3接続へ配信できる状態にする。

POへ「無効になった旧メッセージ1件は再実行せず、有効な1件だけを復旧する。成功扱いには書き換えない」と提示し、原文「進める」を受領。追加原文「› › 次に進む、また離席するのでPRマージとデプロイまで進めてくれ」を受領。これが復旧・実装・PR・マージ・デプロイの対象承認。日付2026-09-10 JST、時分は未取得。旧PRの番号付きGOは流用しない。

07:38:28ZのSELECTで2jobはrunning、items各0、extracted_at/error_messageはNULL。作成時刻は両方2026-09-10T02:49:21.105805Z。前後のCelery inspectはactive/reserved/scheduled各0。既存taskはpendingだけを処理し、retry_extractionはpending/errorのみ・running/doneをskipする（tcg_extraction.py:116、tcg_diagnostics_svc.py:184）。中断原因は未確認。タイムアウト等と断定しない。

| job ID | source ID | 原文の有効性 | 復旧後 |
|---|---|---|---|
| 6da3ca68-651e-4ff6-8316-1c9135508ad2 | b1b58ee9-0d6a-4ed1-8034-f1d62a72b4b2 | false、superseded_by=3a4633b1-82a6-4ce5-8694-053cd637c5f6 | 中断をerrorとして記録、再実行・再有効化しない |
| bfa07018-9b34-42b6-990a-017e3c1cf140 | afbc08d1-cf3b-43be-87e5-4b7200144b6c | true、superseded_by=NULL | 中断をerrorとして記録、反映後に既存retry_extractionでこの1件だけ再実行 |

有効原文は837文字の商品一覧、無効原文は10文字の終了表記。内容はline-recovery-source.jsonに退避しGitへは含めない。外部導入事例は不要。今回の根拠は固定2件のDB観測、既存サービス、既存復旧migrationの実物（20260907_120000、20260908_130000）である。

### 12.2 実装契約・対象外

製品変更は `migrations/20260910_180000_tcg_interrupted_jobs_recovery_t004.sql`、runner末尾の1登録、既存 `backend/tests/test_tcg_work_matching_integration.py` の追加試験だけ。アプリ・API・Gemini・配信の実装やCI・secrets・他tenant・他job・原文・商品/状態辞書を変更しない。本番直接更新は行わず、退避と既存migration経路を使う。

SQLはBEGIN→SET LOCAL lock_timeout=5s/statement_timeout=30s→DO→COMMIT。対象表source_messages/extraction_jobs/extraction_itemsが全て不在なら変更0。一部欠落は例外。3表へSHARE ROW EXCLUSIVEロック取得後に事前条件を検証し、後続の同時変更と競合させない。

1. 固定job IDが両方不在なら変更0（新規環境）。片方のみなら例外。両方のsource ID・created_atは上表と完全一致が必須。
2. running以外のjobは変更しない。既にdone/empty/error/pendingになったjobを再度中断させない。この条件により後日deployが繰り返しても復旧済みの再実行は生じない。
3. runningのjobについてのみ、上表のis_active/superseded_by、extracted_at=NULL、prompt_version=NULL、error_message=NULL、items0、作成から10分以上を検証。一つでも違えば、2件とも変更せず例外。
4. すべての検証後、固定IDのrunningだけstatus=error、error_message=`LINE-RECOVERY-20260910: interrupted job; PO-approved recovery`へ変更。他の列を保持する。期待変更件数は直前に検証したrunning件数（0〜2）であり、件数不一致ならトランザクション全体を失敗させる。
5. ジョブをdoneへ書き換えない。行削除・抽出item生成・Celery enqueueはmigration内に入れない。SQL再実行は変更0。復旧済みjobが再度runningだが条件が違う場合も例外として二重実行を防ぐ。

lock/statement timeoutの仕様根拠は§11.9で確認済みのPostgreSQL16公式資料を再利用する。新しいライブラリ契約は導入しない。

### 12.3 反映・再実行・配信（root担当）

実装役は実DB試験・PR提出で停止。rootが差分とCIを確認。マージ直前にworkerのactive/reserved/scheduled、2jobと原文・itemsを再確認。対象の実行/予約があれば終了を待つ。応答不能・前提ずれは停止する。PO原文の番号要件が拒否された場合も代筆・迂回しない。

既存deployの直前DBバックアップ成功、当該migration成功、backend healthを確認。その後有効jobの現状を読む。error/pendingかつitems0なら既存retry_extractionをjob_ids=[有効jobの固定ID]、scope=Noneで1回呼ぶ。already done/emptyなら再投入せず結果を検証。予想外の状態・応答不明で盲目的再送しない。

無効sourceはfalseのまま、既存明細は保持。有効sourceの抽出は新規items生成のため、doneかつitems>0と全itemの解析保存を確認し、原文の商品・価格・数量・状態との照合を行う。empty/errorなら商品一覧の抽出未達として配信を停止し原因確認。未完了run/jobが0であることを確認し、既存3シートのタブ内容を退避して既存run_distributionを全targetへ実行する。3件の書込結果と実値を照合。検証不足の新商品/状態ルール追加は混載しない。

### 12.4 受入・自己審査

CI使い捨てPostgreSQLで、2件の初回変更と他列/他job/他tenant保持、再実行0、対象0skip・1件欠落fail、3表全不在skip・一部欠落fail、ID対応/created_at/有効性/後継ID/抽出時刻/prompt/error/itemsの各ずれで全件原状保持、running以外の保持、競合ロックtimeoutと設定復帰を確認する。既存retry_extractionの1件限定enqueueはモデル通信をmockし、有効jobだけpendingになること、無効jobがerrorのまま保たれることを実DBで確認する。既存配信安全装置の試験も維持する。ローカルDockerなしはCIで実行し、未実行を合格としない。

Architect自己審査: APPROVE（同一AIの設計審査、独立レビューではない）。固定2件・既存状態遷移への復帰・全件事前検証・単一トランザクション・再実行0・既存配信停止条件維持により範囲と受入が明確。未解決の中断原因はこの限定復旧で原因修正済みと扱わない。採用しなかった案は、runningを成功にする案（未抽出を隠す）、無効原文も再実行する案（POの対象外）、恒久的な自動中断検知追加（対象拡大）。再発防止の恒久設計は別件。

維持担当: 既存実装担当がSQL/既存試験を保守し、rootが反映・復旧・配信の一次証拠をrecon/evidence/tasksへ保存。新たなrunning残存は既存diagnosticsの10分超検出で観測する。今回の中断終了が将来の発生防止まで保証するとはしない。

### 12.5 実装検証・提出位置

PR #3403 / 製品検証HEAD `be4bf045a1c5dabc62cfaa5a125b4590e66fb432`。Backend run34451813934/job102789066448は2499 passed / 93 skipped / 302 warnings、78.65秒、coverage61.60%。復旧追加32ケースはskipなし。実DBmigration run34451813639を含む技術CI成功。rootもSQL・試験差分と上記ログを直接確認し、設計契約からの逸脱・追加阻害指摘なし。全文ログは/private/tmp/line-interrupted-recovery-execution-report.md。

POのマージ・デプロイ承認は受領済みだが、process-artifacts run34451813776/job102789028541は受領原文が「GO #3403」形式ではないため拒否。当該番号付き原文は未受領。過去のGO #3400を転用せず、番号を代筆せず、マージ前に停止。本復旧の本番反映・有効1件の再実行・3接続への配信は未実施。


## 13. 配信前の未サーチ・伝票剥がし跡の再解析修正（2026-09-10）

### 13.1 目的・PO合意・現状の根拠

目的は今回見つかった状態/注記2件を、原文を変えずマスタと解析処理で再現可能に訂正し、3シート配信へ進めること。PO原文「配信まで完了させてくれ」「通常カートンだがNOTE_JAに記載」を受領。後者はCaseを維持しNOTE_JAへ「伝票剥がし跡あり」を記す決定。追加質問「今回は解析ができるように判定に使用するマスタデータも精度が上がるように修正して、再解析テストまで完了させた？」に対し、マスタ/処理/再解析の未完了を説明し、既存の修正・反映・配信依頼を継続する。番号付きGOは別途正規の記録で確認し、創作しない。

根拠: recon末尾。現行tcg_analyzer_svc.py:678は状態+商品名だけ、:1145は備考だけを注記入力とする。実マスタCN0007=Unsearched packは存在、NJ041=伝票跡は存在するが「伝票剥がし跡あり」を正確に保持する札はない。注記マスタ73行、最大ID NJ078、match_typeはTEXT、追加の列は不要。item_correctionsはproduct_id以外を解析結果へ適用しないため、履歴保存だけでは修正にならない。

全有効doneの1443明細を取得し、既存の純関数と実マスタ/正規化規則で状態・注記を再現した。保存値との不一致0。状態/備考を全面結合する案は204行が変わる（状態18・注記186）ため、今回の配信前修正には採用しない。
本節の限定案では2行だけ変更、他1441行は状態/注記不変。未サーチ肯定2ケース、否定・混在・配送免責・空欄9ケースの計11対照が成功。これは単位解決済みの保存値を用いる純関数対照で、全パイプラインの試験完了ではない。実DB再解析/配信行試験を受入条件とする。証拠: /private/tmp/line-condition-note-focused-contrast.json。

### 13.2 実装契約

商品照合・作品推定・数量・価格・status・単位復旧・既存配信安全装置・原文/抽出値は変更しない。状態17件の広い備考参照とPSA誤商品15件は次周とし、本便に混在させない。

1. resolve_condition_v2に後方互換の任意keyword引数raw_memo（既定空）を追加する。既存の状態+商品名による判定を優先し、結果が厳密にR5:パック既定となる場合だけ、enabled/priorityを満たす実条件CN0007の検索/除外語で備考を照合する。通常BOX/カートン/単位不明/明示サーチ済/他の既存状態一致は変更しない。成功時はCN0007のcanonical/UUID、basis=R3:MEMO:一致語を返す。CN0007不在時は従来の既定結果を保持。
2. analyze_extraction_jobはraw_memoをCONDITION用の正規化規則で独立に整形して上記へ渡す。商品照合に渡す既存norm_memoやraw_stateを上書きしない。
3. build_note_jaへ任意keyword引数raw_state（既定空）を追加する。既存LITERAL/REGEXは従来どおりmemoだけ。新しいmatch_type=STATE_LITERALのみ、memoと状態を改行で区切った入力で既存のliteral検索/除外を使う。既存のpriority/id順、REGEX捕捉、空欄、重複発生防止を維持する。STATE_LITERALに商品名/原文全体を渡さない。memo空でもstateがあるSTATE_LITERALを早期returnで落とさない。
4. analyze_extraction_jobはraw_stateをNOTE規則で独立正規化して注記へ渡す。この変更は状態canonicalを動かさない。
5. ENGINE_VERSIONをname-first-v4-condition-noteへ更新する。APIやDB構造は変更しない。再解析は既存の履歴付きサービスを使用し、元データと変更前結果を保存する。

### 13.3 判定マスタの変更

migration候補: migrations/20260910_200000_tcg_condition_note_delivery_t004.sql。scripts/run_all_migrations.shの正規末尾に1登録。対象tenant_004のみ、既存conditions/tcg_note_master両方不存在なら0件終了、片方だけなら例外。BEGIN/COMMIT、lock_timeout5s、statement_timeout30s、対象2表への書込み競合を抑えるロック、全事前照合後に変更する。任意の解析結果を直接更新しない。

- CN0007: canonical Unsearched pack、既存search_kwを保持。exclude_kwの既存「[サーチ済み]」を保持し、未サーチではない、未サーチではありません、未サーチとは限らない、未サーチ保証なし、未サーチ保証無し、サーチ済を重複なく追加する。明示的な否定やサーチ済混在を、未サーチ肯定として増やさない。
- NJ041: label_ja伝票跡、search_keywords伝票跡,伝票痕,伝票剥がし跡、既存属性を保持。exclude_keywordsへ伝票剥がし跡ありを1回追加し、新札との二重表示を防ぐ。
- NJ079を1件追加: label_ja=伝票剥がし跡あり、label_en=Shipping label removal marks、enabled=true、search_keywords=伝票剥がし跡あり、exclude_keywords=伝票剥がし跡ありません,伝票剥がし跡ありではない,伝票剥がし跡ありではありません、category=跡痕系、priority=1、match_type=STATE_LITERAL、search_pattern/label_template=NULL。id衝突時に異なる内容を上書きせず停止する。

CN0007/NJ041は不在・別ラベル・別canonical・予期しない原値なら例外。既に期待する適用後値なら変更0件。原値/適用後値が混在しても未適用部分だけに適用し、二重追加しない。対象外のマスタ行・ID・順序・属性は不変。既存テナント以外への札配布や新規テナントへの自動複製は本便対象外（コードの追加引数は空が既定であり、旧札は動作維持）。

### 13.4 受入・検証方法

- 実PostgreSQLで正規マスタ・実analyze_extraction_jobを使用。匿名の18明細相当fixtureに今回2例を含め、再解析前後の差を比較する。製品/作品ID・価格・数量・status・単位は全件不変。状態変化1、注記変化1、残16明細の状態/注記不変。
- 「※未サーチ品」が備考にあるPackはUnsearched pack。memo不在/否定/サーチ済混在/配送後破損免責を未サーチにしない。明示サーチ済を備考だけで覆さず、BOX/Case/未知単位・他状態優先も保持。
- Case＋raw_state伝票剥がし跡ありはCaseのままNOTE_JAが原文どおり。memoのみ/両欄同語でも新札1つ。伝票跡（一般語）には従来NJ041が作用。伝票剥がし跡なし/伝票剥がし跡ありません/伝票剥がし跡ありではない/伝票剥がし跡ありではありません/商品名にだけ存在する場合は新札を出さない。
- LITERAL/REGEXの既存結果、捕捉ラベル展開、exclude、priority、raw_state任意引数未指定の既存呼出しを維持。
- migration初回/再実行/部分適用/ID衝突/対象不在/片方表不在/他テナント/対象外行保持/ロック制限を実DBで検証。異常時はトランザクション全体を保持。
- 履歴付き再解析サービスを2回実行して結果が同じで履歴が残ること、既存fetch_output_rowsへUnsearched pack/NOTE_JAが反映されることを実DBで確認。実Google Sheetsへの試験書込みはCIで行わない。
- rootが実装後に1443明細全量対照を再実施し、想定外変更0を確認する。配信前に最新の件数/前提を再確認する。現時点で全体の精度100%とは扱わない。

### 13.5 自己審査・維持・反映手順

同一AIのArchitect自己審査: APPROVE（本節の限定設計）。独立した第二者レビューではない。理由: POの分類判断が確定、実マスタとソースの入力欠落を確認、1443行の既存再現不一致0と限定変更2を照合、否定11対照を確認し、残る実パイプライン/移行の実測を具体的な必須受入へ指定した。設計合格は実装試験成功・番号付きGO・本番反映完了ではない。

守り手: データ定義はconditions/tcg_note_master、処理はtcg_analyzer_svc、否定/実DB試験は既存のkeyword_matching/work_matching_integration。新たなサブエージェントは起動せず、既存のLINE実装担当だけにカードで所有範囲を渡す。rootは文書・差分確認・承認済みの本番運用を担当。

技術CI/差分確認→正規GO確認→マージ/デプロイ→マスタの2更新1追加確認→既存履歴付き再解析を対象job bfa07018-9b34-42b6-990a-017e3c1cf140に1回→18解析・2訂正・未完了0を確認→3シート再退避→run_distributionで3接続配信→各行/件数/DB履歴を照合。新たな矛盾や追加の対象が出たら配信を止めて報告する。再解析失敗をdoneへ偽装しない。


§13改訂1（実装中の追加否定照合）: 「伝票剥がし跡ありません」は検索語「伝票剥がし跡あり」に部分一致するため、新NJ079の除外3語と否定試験を上記に追加した。肯定語を増やすだけで否定まで肯定と扱わないための修正。同一AI再審査APPROVE、対象とPO分類は不変。実装担当へ改訂を通知する。


## 15. 商品追加前の判定・配信境界（2026-09-11）

目的: POの「全て完了させてくれ」を受領。日本語名先行、単独販売の商品とセットまでを登録対象とする。収録カード全枚の登録は含めない。POは解析コード・設計文書の社外秘制限を今回一時解除した。顧客原文・実データ・秘密情報は公開しない。マージ指示は受領済みだが、試験不合格を合格として扱わない。

### 観測とWhy

基準mainは7606ca9a。解析・単位補完・配信の3ファイルは前回検証c2149dfdとSHA256一致。固定1630明細のローカル試験で候補確定52、既存確定変更0、未確定化0。原文照合で根拠不足3明細を保留へ訂正し77対照成功。これは本番の全属性の正解率ではない。

新規候補52中、単位未確定33（空欄31・冊2）。末尾の単位候補選択と、明示された入数括弧だけを扱うローカル試作で4明細を回復、29未確定。商品名の途中の長い英単語が末尾のカートン選択を妨げることを実関数で確認。備考に完売だけを含む1明細がIn Stock/exclusionなし。現行配信SQLにexclusionの除外条件がない。外部導入事例は不要: 自システムの原文・実関数・マスタに再現する不具合の対策であり、外部数値を成功根拠に置き換えない。

本番マスタを16:41 JSTに読み取り再確認。商品296、検索657、除外156。商品/検索は以前と一致。除外は49行のIDが変化したが商品・語・positionの集合は同一。古いIDを本番適用・戻しへ流用しない。参照マスタ表名の初回読取は誤りで失敗、CSV登録サービスのLOOKUP_TABLESに従って訂正し成功した。書込は0。

### 限定コード対策の契約

1. 商品専用の検索/除外に限り、既存match_one_kwの成功に加え、末尾が数字の検索トークンへ英数字の境界を要求する。日本語混在は空白AND、純ASCIIは従来どおり全文句。ST-31にST-310を一致させず、通常の日本語接尾表記は残す。状態・備考マスタの共通match_one_kwは変えない。作品不明時の型番禁止、BOXのシングル共通除外を維持。
2. E3単位補完は既存の未確定・raw_unit空・商品確定条件を維持。候補は末尾一致する単位から選ぶ。末尾の入数括弧は数字＋BOX/箱/個/パック等＋入/入りだけを認め、状態・日時等を含む括弧は除去しない。英字単位の前に英数字が続く語内部を除外。ケースの前の空白条件、商品マスタ名に含まれる語の拒否、上限100/200、E3→E5→E3b→E4を維持。数量換算しない。単位が原文にない商品へ既定値を作らない。
3. resolve_status_v2に任意raw_memo引数を追加。備考全体が有効なEXCLUDEのLITERAL語に一致する場合だけ、そのマスタのcanonicalとexcludedを採用。通常の前後空白だけ除去する。否定文・複合文・発送予定を含む備考の部分一致は追加しない。raw_stateの既存優先順とOUTPUT判定を維持。解析の呼出し側でSTATUS正規化した備考を渡す。
4. 配信SQLにexclusion='excluded'の行を除く条件を追加。NULL/空文字の従来行は維持。価格・単位・商品・Conditionと実行中jobの安全条件は緩めない。完売フラグを持つ行が商品登録によって配信可能になることを防ぐ。

### 対象・代替案・受入

変更対象はtcg_analyzer_svc.py、tcg_unit_recovery_svc.py、tcg_distribution_svc.pyと対応する試験、本文書、台帳。API/DB列・CI・運用スクリプト・Gemini・元原文を変更しない。具体的な顧客由来登録/訂正データはローカルで別管理する。商品名だけから単位を一律設定する代替案は、箱とカートンの区別を誤るため採用しない。曖昧な備考の全単語照合も否定や部分完売を誤るため採用しない。

受入: 数字続きの不一致、正しい型番一致、既存文字列THE BEST1維持、共通条件照合不変。末尾BOX/カートンと入数括弧の補完、語内部/日時括弧/未記載/マスタ名由来の不補完。完売単独備考の除外、否定/複合備考不変。実PostgreSQLで全解析→E3/E5→配信行取得を通し、同一入力2回の値一致、手動product_id訂正保持、原文/抽出値保持を確認する。既存関連試験も合格必須。DB試験・製品実装・全体検査はまだ実施前であり、設計合格を結果に代用しない。

維持担当: 商品辞書担当は登録前後の固定標本比較、開発担当は上記回帰試験を保持。取りこぼし一覧と根拠原文で未知の表記を追跡する。仕様のない単位や形態を自動追加しない。

### Architect自己審査

限定コード契約はAPPROVE（同一AIによる自己審査。独立レビューではない）。根拠は上記実関数・55原文照合・77対照・末尾単位11対照。実装後の実DB検証を必須とする。商品登録全体はREVISEを維持: 書籍/マットの分類、冊の扱い、版不明商品、単位未記載商品の確定が残る。本節はこれらの値を推測して埋める許可ではない。


### §15改訂1: 新規登録とBOX共通除外の接続

実create_product→実解析の隔離PostgreSQL試験1件で、新規PC_BOX商品のPSA10が商品確定する不具合を再現（期待0、実際1）。create_product:349はcategory_classに作品のdisplay_nameを保存し、analyze_extraction_jobはcategory_classをBox/Caseと比較していた。CSV登録仕様はcategory_class自動生成、既存create_product不変を要求しているため、この列を上書きしない。

契約追加: 商品のproduct_category_idから既存load_product_kubun_type_mapで引いた箱系/箱系大をBOX共通除外の正本とする。有効な分類参照がある場合はその分類を優先し、参照がない旧商品に限って従来category_classへフォールバックする。未知分類をBoxと推定しない。work_idの作品制約は別で維持。API/商品データの保存方式は変えない。受入は新規登録→重複登録拒否→PSA付き解析の確定0、既存Box/Singleの回帰試験。設計自己再審査APPROVE（同一AI）。本番新商品の登録前に修正・実DB試験を必須とする。


### §15 検証追記

製品3ファイルに限定対策と改訂1を実装。変更を再解析結果から識別するためENGINE_VERSIONをname-first-v6-master-safetyとする。追加試験は架空入力のみ。初回の試験モジュール参照とステータステーブルの試験定義を訂正し、既存migrationの実列で検証した。変更前の登録→PSA判定試験は1件失敗（確定1）、修正後は合格。関連110件成功。先行4対策の関連336件も成功。全体試験は実行中であり結果を先取りしない。

商品区分参照への切替は既存有効商品の29件で参照元が変わるが、固定1630明細の再比較で商品/確定/候補の変化0。実装した商品専用照合の77対照成功、同じ標本で登録案に対する候補確定52を再現。登録内容の参照コードを26候補で埋め、実CSVサービスの静的検査でblocking0（型番空の警告のみ）。これはDB登録完了ではない。59候補中33は版・形態・メーカー・書籍分類等の確認を残す。書籍区分と冊の扱いはPOへ確認中で、返答を推測しない。

ローカル根拠: /private/tmp/onepiece-research/completion-336-pass.log、completion-110-pass.log、registration-box-guard-before.log、category-reference-replay-private.json、registration-static-check-private.json。顧客由来の資料はPRへ含めない。今回の架空DB試験では本番データを使わず、Gemini実呼出し0、本番書込0、配信0。


PR #3434検証追記（実装HEAD4c5d1893）: GitHub run34577363592で2586成功/95skip、coverage62.41%、必須13検査成功。ローカル初回の読み込み依存1失敗を修正し対象39件成功。全体再試験は試験DBの7.8GBが100%・空き0（DiskFull）で2472成功/25失敗/90errors/94skip。成功扱いせず上記GitHubの新DBで確認した。他者の試験DBを削除せず、このMacの共有試験環境は容量復旧まで使わない。make lint-ci終了0、mypyは既存警告運用で型エラー0ではない。

process-artifacts gateは番号付きGO記録なしで失敗。一般的なマージ指示は受領済みだが、GO #3434の原文を代筆しない。未マージ・未配備。26登録候補は静的検査のみで未投入。書籍区分/冊の回答と33候補の確認は残る。本番再解析・配信未実施。


## 16. Geminiが商品マスタを参照して作品IDのみを判断する設計草案（2026-09-12）

### 16.1 PO合意と状態

PO原文: 「商品マスタを見て判断した方が精度が高くなると考えるが合意できるか　受け継がれる意志→マスタの名前を修正してくれ」。
続く制約の原文: 「今回geminiに判断させるのは作品IDのみ、ほかは原文抽出させる」。
設計着手への返答: 「進める」。

合意は商品マスタ参照・判断対象を作品IDだけに限定する目的と境界。下記の保存方式・試験基準は設計案であり、POが逐語承認したとは記録しない。実装担当への自動切替・migration実行・再抽出・再解析・マージ・配信のGOではない。
状態: 調査済み／設計草案作成／Architect自己審査REVISE／実装カード未発行／製品実装未着手。GO委任は未有効のまま。

### 16.2 Why・現在地

根拠は同ディレクトリrecon末尾「2026-09-12 商品マスタ参照による作品ID判断」。固定SHA 66b41766（文書作業起点。直前に調査した4afb81cから対象抽出・照合処理の変更なし）と本番読み取りで照合。

- Geminiへ渡す参照は作品名・別名だけ。商品名／型番／検索語と作品IDの対応は渡していない。
- 現行promptは作品推測とID付与を禁止し、型番のみなら作品2列を空欄にする。§9–10の旧契約に忠実な動作であり、モデル能力の不足と断定しない。
- 型番のみ19明細の1投稿では商品確定0/19。有効マスタ293商品は8作品へ紐付き、作品ID NULLは0件。
- 別投稿ではGeminiの原文作品名が「ワンピース」でも、引用行「【在庫品】 ワンピースカード」が完全一致条件を満たさず作品不明になる。
- 有効な最新取込群（source_messages.created_at >= 2026-09-11T23:00:00Z、観測時点）320解析明細の要確認120件、商品未解決114件。これは対象全件の正解率ではなく、保存された理由の集計。全件が本設計だけで解消するとはしない。

### 16.3 対象・対象外・変更前後

既存テーマの延長。親は [商品マスタ](../../specs/product-master/README.md)、受信原文の処理領域は [feed-translation](../../specs/inventory-management/feed-translation/README.md)。下流inventory-analyticsとは分ける。

変更前: 原文＋作品名一覧 → Gemini原文9列 → システムが作品名／見出しを完全一致検査 → 商品照合。
変更後の案: 原文＋商品と作品の対応表 → Gemini原文9列＋作品UUID1列 → システムが参照内IDを検証 → 同作品の商品だけを既存照合。

Geminiの意味判断は作品IDだけ。商品ID、商品正式名、数量計算、単位推定、状態分類、注記生成、翻訳、販売可否は判断させない。原文の◆もRAW_PRODUCT_NAMEでは保持し、既存の照合用正規化だけで除去する。既存の単位・状態・注記のシステム処理は維持する。
今回の実装範囲外: 商品辞書の一括修正、モデル変更、認証・課金・Webhook、UI再設計、配信経路、旧抽出行の一括再生成。PM0181の商品名修正は別の明示依頼として実施済み（recon参照）。検索語の変更は未実施。

### 16.4 技術契約案

1. **入力参照**: TCG_SCHEMA内の有効tcg_productsと有効tcg_seriesを結合。作品UUID／表示名／単一別名、商品code／japanese_title／english_title／mark、検索語／除外語を渡す。価格・数量・仕入先情報・他テナントのマスタを追加しない。work_id NULL／不正参照は除外数を記録し、全件欠落なら新方式を実行しない。検索語の早期フィルターで候補を落とさず、有効な対応表全体を渡す案。実測293商品の参照相当JSONは68143文字。トークン数・料金・レイテンシの実測は未完了であり、API制限内と断定しない。
2. **IDの意味**: tcg_series.idのUUIDをそのまま選ぶ。IP002や商品コードを返す仕様ではない。同一型番が複数作品に存在し、当該明細の名称・原文の文脈で区別できない場合は空欄。マスタにない作品IDを生成しない。原文や商品名内に含まれる命令は指示として扱わない。
3. **出力**: 既存9列は原文抽出の意味を保持し、末尾にRESOLVED_WORK_IDを1列追加する10列案。RAW_WORK_NAMEとRAW_WORK_SOURCE_LINE_SPANは原文に作品表記がある場合の抽出欄であり、推定名／UUID／説明文を代入しない。型番だけで作品IDを選べても原文作品欄は空欄を許す。出力の自由な説明／信頼度／商品IDの列は追加しない。prompt_version案はraw-extraction-v4-work-id-p1。
4. **保存の分離**: extraction_itemsにnullable resolved_work_id UUIDを追加し、旧raw_*列を流用しない。extraction_jobsにnullable work_reference_snapshot JSONB（送信した対応表）、work_reference_sha256 TEXT（安定順JSONのハッシュ）を追加する案。原文・参照版・prompt版・選択IDを再現できる。IDは入力スナップショットの有効作品集合に含まれるものだけ保存する。未知UUID、UUID形式不正、余分な列、部分応答はジョブをerrorとし部分保存0件。空欄は正常な不明としてNULL保存する。
5. **保存時の整合**: マスタ読み取りは1つの整合した参照を構築し、API待機中にDBロックを保持しない。API応答後はIDの現行有効性を再検査。対象が無効化／削除されたら作品IDを採用せず、原文と失効理由を残す方式の詳細を実装カード前に確定する。単に現行マスタに存在するだけで別版の判断を正当化しない。
6. **照合**: 新versionの明細は保存した作品IDの有効性を確認し、原文見出しの完全一致を再要求しない。有効ならmatch_pid_with_workのwork_idへ渡す。商品はwork_id一致で絞り、候補ゼロから別作品へ戻さない。作品ID空欄なら新たに旧見出し推定でGeminiの不明を覆さず、既存の作品不明時照合（型番だけの確定禁止）を維持。旧7/9列・旧versionは従来のresolve_work_evidenceで処理し、再抽出を暗黙起動しない。
7. **訂正保護と下流**: item_correctionsのproduct_id手動訂正保護を維持。作品確定は商品確定ではなく、名称・区分・除外語・複数候補チェックは継続。pid_basisに作品IDと方式を区別できる接頭辞を保存し100文字上限を守る。数量・価格・単位・状態・注記・配信判定は既存順序のまま。名称訂正だけでは検索語の誤字は変わらないことを検証する。
8. **DB導入・切替**: additive-onlyの別migration案。既存TCGテーブルのあるスキーマと将来作成経路の両方に適用し、既存行NULL・backfill0件。具体的なファイル採番、FKの削除時動作、無効IDの診断保存場所、新規テナント作成経路を再確認するまではカードを発行しない。新旧worker混在で10列を旧parserが読むことのない切替手順を用意する。旧コードへ戻しても新規行の判断を黙って失う運用は不可。
9. **他PRとの整合**: PR #3417は調査時OPEN、発送条件等の原文保持改善を含む。採用／保留の状態とHEADを実装起点で再取得し、本案の作品ID例外以外の原文保持規則を落とさない。単に現在mainの古いpromptを全文置換しない。

### 16.5 代替案と選択理由

- 現行の作品名完全一致だけ: 推定誤りを抑えるが、型番のみ19明細が未解決のまま。目的を満たさない。
- システムだけで型番→作品を決定: 一意型番には有効だが、本件のPO条件はGeminiによる作品ID判断。独断で置き換えない。
- Geminiに商品IDや状態まで決定させる: 本件の対象外。原文と解釈の境界を壊すため不採用。
- 推奨草案は作品IDのみ＋参照版保存。対応表の誤りも判断へ伝播し、入力増加に伴う費用・待ち時間・抽出列への影響がある。ID存在検査は意味の正しさを保証しないため、正解表との比較が必須。

### 16.6 受入条件と検証方法（未実行の計画）

| 基準 | 検証方法 |
|---|---|
| 作品ID以外の意味判断0項目、原文にない数量・状態・メモの追加0件 | 実APIの旧／新prompt比較で、原文の該当箇所と全RAW列を人手の正解表へ突合。単なる旧出力一致だけを原文忠実性の証拠にしない |
| 型番のみ19明細のワンピース作品ID正解19/19、誤作品0件 | reconの固定投稿を承認された検証環境で比較。商品ID確定件数とは分けて測る |
| 【在庫品】 ワンピースカード見出しを持つOP-13対象2明細の作品ID正解2/2 | 同じ原文と本番マスタスナップショットを使用して実API出力を検査 |
| EB01重複型番のみは確定0、ガンダム明示はガンダム、ワンピース明示はワンピース | 3入力を独立ケースとして比較。混在投稿・別明細の見出し持ち込みも追加 |
| 未知ID／形式不正／列不足／列過剰／応答途切れ時の部分保存0件 | parser単体＋実PostgreSQLのタスク統合試験 |
| 原文に作品名がないケースのRAW作品欄は空欄、判断した作品IDだけ別保存 | parser→DB→再解析の実PostgreSQL通し試験 |
| 他作品商品への復帰0件、手動訂正上書き0件 | 現行work matchingとitem correctionsの回帰試験 |
| 旧7/9列読み取り、旧保存行の従来解析が成功 | version別の固定入力で比較。旧データへのGemini呼出し0回 |
| マスタ無効化・テナント違い・参照版差し替え時に誤採用0件 | API待ちの前後で検証用マスタを変更する競合試験 |
| 過去結果・全3接続シートへの自動変更0件 | 検証時の書込み先と外部API呼出し履歴を照合 |
| 費用・速度をPOが判断できる | 参照文字数だけでなく実入力／出力トークン、所要秒、失敗回数を旧／新で記録。許容値は測定後にPO判断 |

数値は合格条件案であり成功実績ではない。母集団320明細の全未解決解消、精度100%、本番配信完了は約束しない。商品辞書の別課題は分離して数える。

### 16.7 影響範囲・維持の仕組み

実装候補: backend/app/services/gemini_extraction_svc.py、backend/app/tasks/tcg_extraction.py、backend/app/services/tcg_analyzer_svc.py、その既存テスト、新規additive migrationと正規登録経路。診断UI/APIに判断理由や参照版を公開する必要性と後方互換は未確定。製品コード・CI・運用スクリプトは本設計文書PRでは変更しない。

6面: 人＝POが正解と費用を確認。エージェント＝原文抽出9列と作品判断1列の境界を実装契約にする。機械＝parserとDB統合・既存Backend CI。データ＝TCGスキーマに原文と別の判断値／参照版を保持。本番＝DB先行・worker切替・旧版復帰の検証が必要。外部＝既存Geminiへ商品対応表を追加送信するが、シート配信は変更しない。

守り手: 人手で守る（本案は未実装）。設計担当が未確定項目を埋め、実装担当がbackend/tests/test_tcg_gemini_extraction.py、backend/tests/test_tcg_keyword_matching.pyと実DB統合試験を維持する。商品登録時は他作品との型番衝突を正解ケースへ追加。POが事業上の曖昧さを決定する。既存CIの存在だけで意味上の正しさを保証したとはしない。

### 16.8 Architect自己審査

判定: **REVISE（修正必要）**。同一AIによる自己審査であり、独立した第二者レビューではない。

整合: POが判断対象を作品IDに限定した目的・境界は明確。既存の原文保存と商品照合を保つ構成を提示した。
未解決: 実APIによる原文保持・正解率・トークン/時間、ID失効時の記録契約、FK/新規TCGスキーマ経路、worker混在と復帰、診断の接続を確定していない。
次の一手: 製品実装の前に、固定した原文／マスタ／正解表で旧／新promptの比較試験を行い、上記境界を実物で確定する。その結果を反映して再審査し、合格後に正式カード検査へ進む。今回カードを発行しない。方式草案の文書保存を実装承認とみなさない。

### 16.9 外部・過去事例の参照と我々への応用

外部導入事例は該当なし。今回の受入判断は自社の商品辞書と原文でしか検証できず、他社の精度数値を成功証明に使わない。過去のガンダム→ワンピース誤確定（ADR-154 Why）を否定試験へ引き継ぐ。新しいSDK/API仕様を本案で採用せず、既存呼出しのprompt契約だけを検討した。API上限に依存する判断は未確認。


### 16.10 POの後続指示と契約確定（§16.4の保留点を置換）

PO原文:「離席するので推測は禁止して事実確認を怠らずに確実性を重視して最も効果があり、現状把握の粒度が細く、精度が高いエビデンスを確立して安全に進めてくれ、確立したならPR→マージを実行、テスト段階でのgeminiの仕様は禁止、本番へマージ後に現在の最新のインポートデータを使ってgemini抽出を実行、システムに解析させて結果を確認、施策実行前と比べて解析精度が向上している場合はそのまま採用、要確認データ以外の解析データをスプレッドシートに配信をしてくれ」。
「仕様は禁止」は文脈上、テスト段階のGemini使用禁止として扱う。事前実API比較を要求した§16.6/16.8は本指示で置換する。事前は模擬応答＋実DBで機械契約を検証し、意味の正確さは本番反映後の比較まで未確認と表示する。番号付きGOを生成しない。

- 参照欠落・無効なwork_idを持つ有効商品が1件でもあればAPI前に停止。マスタ参照は1 SQLのスナップショット、安定順JSONとSHA256を保存。API中はトランザクションを保持しない。応答後に同じ参照を再読取しSHA不一致ならジョブerror、明細保存0件。判断UUIDにFKは付けず（過去判断をマスタ削除で消さない）、入力参照と現在の有効作品集合の両方で検査する。
- DB追加3列は既存TCG表に限定する正規migration。将来スキーマは基礎TCG作成→同migrationの再実行で3列が追加される実DB試験を必須にする。新規自動テナントへのTCG導入を本便で拡張しない。
- deploy.yml:319–335ではコード切替、:444以降でmigration。この順序は変えず、抽出開始前に3列の存在を検査し、未適用時はGemini0回／jobはpendingのままエラーを返す。migration適用確認後に対象を再起動する。
- 再解析時はv4参照SHAを検証し、保存UUIDが参照内かつ現在有効な作品である場合だけ使用。v4の空欄を旧見出し推定で上書きしない。無効化時は作品不明として型番のみ確定を止める。診断は既存ジョブのerror_messageと保存snapshot/prompt_version、analysis_results.pid_basisで追跡し、新UI/APIは追加しない。
- 配信のfetch_output_rowsにはneeds_review IS FALSEを追加する。既存SQLに当該条件が無いことを実物確認。NULLも除外する。その他の配信安全装置は維持。
- 本番再抽出は実装テストと混ぜない。実行直前に取込リンク・有効原文ID・旧明細/訂正/解析結果・マスタ版・3接続先を固定退避。Geminiの新応答は最初は公開配信対象へ書かない。旧明細との1対1対応と全RAW欄の一致を照合し、差があれば上書きせず停止する。安全に対応する場合のみ同じ明細IDに新作品判断と参照版を保存し、履歴付き再解析を実行する。既存明細削除や二重ジョブ追加を用いない。精度比較の正解ラベルは現在の保存値を流用せず、原文と商品/公式根拠で確定する。19件全件の正解が確立したとはしない。
- 採用条件は固定対象で正しい作品/商品確定数が増加し、誤作品確定の増加0、RAW欄改変0、手動訂正破壊0。曖昧事例が残れば成功扱いせず配信停止。既存の全3接続先が確認でき、全解析終端と要確認除外の行照合が済んだ場合だけ配信する。これは本番結果の事前保証ではない。

Architect自己再審査: **APPROVE（機械契約の実装着手のみ）**。原文保持・参照版・不正ID・旧版互換・手動訂正・配信除外の境界を固定し、移行前にAPIを呼ばない条件を実deploy順序から追加した。根拠は本番293商品/8作品/欠落0と、列/呼出し/既存配信SQLの実物。Gemini実精度・本番適用・配信への合格判定ではない。最終ゲートは実DB試験とCI、番号付きGOが要求された場合はPO原文待ち。同一AIの自己審査である。


### 16.11 実装・検証の現在地

2026-09-12: カード機械検査は初回L29（読んだ節記載不足）1件を修正し終了0、L24長行警告のみ。新規参照モジュール、原文10列、3列migration、抽出/解析接続、needs_review IS FALSEを実装。GitHub CI前であり実DB合格とはしない。変更前の名称1件以外、本番書込/再抽出/配信0。
make lint-ciはruff/import2件の整列後に終了0。ruff/Bandit成功、mypyはローカル依存不足と既存警告を含むため型検査合格とはしない。Docker未起動のためローカルpytest未実行、隔離PostgreSQL試験はCIで行う。card-lint/check-task-state/diff --check成功。参照版が変わったv4の再解析はエラー停止し、古い判断を現在マスタに流用しない。


自己レビュー追補: 商品名そのもの／従来の厳密な作品見出しで作品が一意に確定する場合、Geminiの非空IDがそれと矛盾したら全明細保存前にerror停止する。作品IDが空欄ならシステムで補完しない。有効な別作品UUIDの誤選択も拒否する否定試験を追加。モデルの意味判断を機械の存在検査だけで保証しないという§16.5を具体化した。


### 16.12 行番号契約修正（2026-09-13）

POは再抽出失敗の報告と行番号修正・検証の質問に「進めてくれ」と回答。修正着手を承認、追加PRの番号付きGOは未受領。実API再試行は本番反映後まで行わない。
目的: 商品根拠の位置を誤補完せず、Gemini出力が既存の厳格なLine ID文法に従うよう指示を修復する。観測: 本番1回はinvalid product source span、旧promptにはL0001-L0002の例あり、新promptにはなし。実際の失敗応答は未保存なので、特定表記が原因との断定はしない（recon本番停止記録）。
変更: 商品/作品の両spanについて単行L0001、複数行L0001-L0005を明示。入力の角括弧は出力しない、空欄/不連続列挙/全角区切り等を商品spanに返さない。パーサーの文法・境界検査は維持。異常時はv4についてspan長・角括弧有無・許可文字集合への適合booleanだけをerror_messageへ記録し、raw値/応答本文はログやDBへ追加しない。
版: 新抽出はraw-extraction-v4-work-id-p2。保存済みp1も作品判断の旧版として明示的に受理し、参照SHA・UUID検査は同じく必須。版変更でp1が旧9列方式へ落ちることを防ぐ。
対象外: DB変更、行番号自動修復、商品・数量・状態の推測、シート仕様、SDK変更、CI変更。外部事例・Context7の新API調査は不要（自社文字列契約の修正のみ）。
代替案: 不正spanの角括弧/区切り自動置換は証拠位置の推測を生むため不採用。raw_responseの永続保存は秘密/顧客情報の公開範囲を広げるため本便不採用。診断は内容を含まない形状だけ。
受入条件: 正規単行/範囲で元RAW値とIDが保存される、不正spanの模擬8種がitems0、秘密文字列が診断へ出ない、p1/p2の実PG再解析で同一ID・SHA検査が動く。テスト中Gemini実呼出し0。本番では従来の固定対象・RAW一致・精度向上・誤確定増加0・訂正保持・全解析終端の条件を満たすまで配信しない。
維持: tests/test_tcg_work_id.pyと実PG統合試験を既存Backend CIで実行。POが意味の判断、本担当が契約と証跡の維持を担う。設計自己審査APPROVE（機械契約の修正のみ、独立レビューではない）。本番改善効果は未確認。実装は本節に限定する。


## 17. 保存済み原文を固定した作品ID比較（2026-09-13 設計見直し）

この節は、再抽出の揺れを評価へ混ぜず、既存明細の作品判断だけを比較する設計である。POは「既存の原文欄を固定し、作品IDだけを比較する方式へ設計を見直してよいか」に「進める」と回答。方式見直しの承認であり、今回の新実装・結果採用・配信済みとは扱わない。

### 17.1 停止の棚卸しと目的

§16.10の全RAW一致は原文保持のガードとして正常に停止した。一方、作品IDだけの評価に全項目の再抽出を使う設計が価格/メモの出力揺れを持ち込んだ。span失敗は出力契約不足、p2のRAW差異は新事実と正常な拒否。これ以上promptの逐次修正だけを繰り返さず、入力と評価対象を分離する。価格記号の欠落を許容して停止を減らす変更ではない。
目的: 既存RAW/行位置/訂正/解析結果を変更せず、作品IDを与えることで商品特定が改善するかを測る。今回の比較は本番p2の通常インポート抽出仕様を変更しない。新規インポートのRAW忠実性は別の残課題として残す。
対象: POが選択した9:31確定済みimport536422ed-79c7-4a87-a887-09a02b97968fの44有効原文。保存明細を持つ37投稿/762明細を比較分母とし、empty6とerror1を明示して分母から区別する。error1を成功や改善に数えない。10:43のpending取込は対象外。

### 17.2 実物で確立した根拠

- 本番SELECT（recon同日棚卸し）: 762固有明細ID、商品名空0、位置範囲不正0、訂正あり0。商品未特定160、既特定602、要確認166。
- 要確認理由: pid_unresolved128、pid+multi19、pid+note12、pid+multi+note1、noteのみ6。作品判断だけでnoteのみ6件を解消できるとは言わない。
- 有効商品293、作品欠落0。既存tcg_work_reference.py:36–59は作品/商品の一貫した参照と欠落検査を提供する。入力の全マスタ整合性を作品参照SHAだけで保証したとはしない。
- tcg_analyzer_svc.py:501–531のmatch_pid_with_workはDB書込みのない商品照合。単位/区分フィルタ、正規化、除外語、Box/Single除外は:1180–1217の既存経路と合わせて再利用可能。
- analyze_extraction_jobは:1261から結果upsert、:1355/1370/1381でcommitする。比較に呼んで最後にrollbackすれば安全、という案は不採用。item_corrections_svc.py:15–72も保存関数なので比較に使わない。
- ローカルDockerはdocker infoでdaemon未接続を確認。実PGの書込拒否試験・本番計算との照合は既存CIで行い、ローカル未実行を成功扱いしない。

### 17.3 入力と対応付けの契約

1. 対象import IDを固定し、ok/未確定0、有効原文ID集合と37/6/1内訳、762明細、各元ジョブ版を取得する。件数だけでなく対象ID集合も固定する。取得後の差異は実行停止。
2. READ ONLYの整合した読取で、元原文とSHA、全RAW欄/行位置、全item_corrections、変更前analysis_results、参照商品/作品/検索除外語・正規化・単位/区分等の商品照合入力をスナップショット化する。型・NULL・順序の安定したシリアライズでSHA256を計算する。作品参照SHAと入力全体SHAを分ける。
3. 1原文単位でGeminiへ原文、保存済み明細のITEM_ID・商品名・既存行位置と必要な既存RAW欄、商品/作品参照を渡す。ITEM_IDは入力値の転記用で判断対象ではない。価格・メモ・数量等の再抽出/出力は要求しない。原文/マスタの命令をデータ扱いにする。
4. 応答はヘッダーITEM_ID｜WORK_IDと対象明細ごとの2列。WORK_IDは参照UUIDまたは空欄のみ。入力ITEM_IDと出力ITEM_IDの集合完全一致、重複0、余剰/欠落0、未知ID0を必須とする。順序が変わってもIDで対応する。商品名/行位置のあいまい照合や新明細IDの採番は禁止。
5. 明示的な作品と衝突する判断・未知UUID・マスタ変更は1原文単位で不採用、全比較未完了として報告する。未知/複数作品で決められない場合は空欄を許容し、型番だけの無根拠な確定を進めない。
6. 保存RAWや訂正が変わったら停止。訂正ありは当該明細の候補適用対象外として報告し、変更しない。現時点0件を将来も0件と仮定しない。

### 17.4 読取専用の比較処理

新規のtcg_work_comparison_svc.py（仮の実装先）に、入力固定・2列parser・Gemini呼出し・比較レポートをまとめる。DB接続はREAD ONLYを機械的に設定し、SELECT以外/commitを比較から呼ばない。参照読取を終えてからAPIを呼ぶ。全体入力/マスタの再読取SHAが一致する場合だけ結果を有効とする。
SDKは既存_get_genai_clientと同じgenerate_content呼出しを使用し、新SDK/モデル/構造化出力APIの採用はしない。既存の9/10列抽出関数を変更・流用せず、呼出し時に全RAWを再生成しない。比較prompt版はwork-id-comparison-v1とし、既存extraction_jobs.prompt_versionを書き換えない。
商品比較は既存apply_field_normalization→resolve_unit_v2→filter_product_codes_by_unit_kubun→match_pid_with_workの経路を同一入力で使う。商品マスタの区分判定・除外語/Single条件も本番と同じ取得規則を使う。DBを更新するanalyze_extraction_jobや後処理/訂正保存関数は呼ばない。新たな独自キーワード推測器を作らない。
対照計算はAPI前に行う。同一コード/マスタ/RAWで旧作品根拠を用いた対照結果をまず計算し、保存結果と照合する。旧作品根拠は旧版なら既存resolve_work_evidence、p1/p2なら保存判断と参照版の検査を用いる。保存商品IDと対照結果の不一致は過去版/マスタ差等の独立問題として原因確定まで停止し、Gemini改善に数えない。再現できた場合だけ新WORK_IDで同じ商品照合を実行する。レポートは保存結果/同一処理での旧判断対照/新判断候補の3列を必須とする。API前に再現性を検査することで、比較不能な入力に実API費用を使わない。
読み取り比較の出力: import/source/item ID、入力SHA/マスタSHA/コードHEAD/prompt版、旧商品/旧作品根拠、新WORK_ID/新商品候補・一致根拠・候補集合、未確定/拒否理由、証拠ラベル、集計。本文や仕入先名を公開ログへ出さず、SSH経由でローカルの非公開レポートへ保存する。失敗応答は非公開メモリ/レポートの中で診断可能にし、公開error/logは内容を含まないエラーコードだけ。
重要な限界: これは商品照合の比較であり、状態・単位復旧・備考等を含む本番の全再解析ではない。needs_review全体や配信行数をこの結果だけで改善済みとは宣言しない。

### 17.5 効果判定と次段階の停止条件

| 指標 | 合格条件と検証 |
|---|---|
| 原文/既存データ保持 | 全RAW・行位置・訂正・解析結果の前後SHA一致、DML/外部シート書込0 |
| 対応付け | 762固有明細IDに対し余剰/重複/欠落0。対象外44以外の原文をAPIへ送らない |
| 対照の再現性 | 同一処理の旧作品判断で保存商品IDを再現。不一致0、未確認差異があればAPI前に停止 |
| 未特定160の改善 | 原文と商品参照で正解を裏付けた新規正確定が1件以上。単なるpid_resolved増を成功にしない |
| 既特定602の保持 | 商品IDの変更/未特定化0。既存が誤りと思われる場合も勝手に訂正せず別の不一致として報告 |
| 不確かな候補 | 新しく確定した候補について誤商品0・正解未確認0。曖昧な型番は空欄/未確定を維持 |
| 分母の明示 | ラベル確立数/未確認数、37解析投稿/6空/1エラーを区別。762件すべての正解率を捏造しない |

正解根拠は元投稿の作品/商品名・型番・限定語と商品参照の整合を記録し、Gemini自身の説明や旧解析値を正解ラベルの代用にしない。必要な公式資料の確認ができない明細は未確認へ置く。全体成功を先に決めて不都合な明細を除外しない。
採用/配信は今回の比較の後段。現行analyze_extraction_jobはジョブ全体の複数回commitと後処理を持ち、現在の保存prompt版に作品判断を結び付けているため、既存ジョブをp2へ偽装して判断だけ書き込む案は禁止する。作品判断専用の履歴/参照版・訂正保護・競合検査・原子的な反映と全再解析の方式を別途設計/実PG検証してから正式採用カードを出す。未完了error1の扱いと全3接続の配信安全条件もその段階で確認する。本比較の合格をそのまま結果更新/配信GOにしない。

### 17.6 代替案・リスク・維持

- 旧/新RAW値の一致を都度prompt修正で追う案: 作品ID比較と無関係な抽出揺れが続くため不採用。
- 新RAWを無視し商品名/行位置で対応を推定する案: 同名複数行や境界変化による取り違えのため不採用。最初から保存ITEM_IDを渡す。
- 本番再解析→rollback案: 内部commit3か所があるため不採用。比較では書込APIを呼ばない。
- 純粋な照合呼出しの組立てが本番とずれるリスク: 合成入力の実PG試験で、本番analyzerの商品結果との一致を検証。全状態再解析の同一性までは主張しない。
- 比較専用処理が本番p2へ未反映の場合: 実APIを実行しない。模擬/実PG試験合格、正規PR/PO GO/本番反映後にのみ実Geminiを呼ぶ。
守り手は比較専用のparser/readonly/対応付け試験と既存Backend CI。POは意味の不明点と結果採用、実装担当は機械検査、設計担当は指標と入力根拠を維持する。外部成功事例は不要で、自社の対応付けと読取専用性が直接の根拠となる。新規ライブラリ/APIの仕様確認は行っていない。

### 17.7 Architect自己審査

判定: **APPROVE（読取専用の作品ID・商品照合比較の設計範囲）**。根拠は762固有ID/欠落0、保存値固定の構成、既存DB非依存match_pid_with_work、既存書込関数を比較から排除する契約。設計の自己審査であり独立した第二者レビューではない。実装・実PG合格・実API精度の証拠を作ったとは称さない。
**採用/本番再解析/配信の設計はREVISE**。判断専用の保存/反映方式と内部commitを含む訂正保護を確定する必要がある。未解決部分を比較実装へ押し込まず、採用カードは発行しない。
状態: 方式見直しPO承認済み／設計案・自己審査済み／比較実装未着手／実API追加0／結果更新・配信0。今回のユーザー依頼は設計見直しであり実装役への自動切替をしない。


### 17.8 本番読取比較の実行手順（2026-09-13）

POのGO3465により比較実装はマージ/配備済み（recon同日節）。後続「進める」に基づき本番比較手順を具体化。採用/全再解析/配信への承認とは区別する。対象は§17.1の確定済み取込を維持し、現在最新という理由だけで未確定取込へ切り替えない。

API前の実測: 本番配備済みサービスのread_snapshot/old_work/match_itemをREAD ONLY接続で実行。44投稿、37done/6empty/1error、762明細、訂正0、保存商品確定602。旧判断対照の商品ID/確定flag不一致0。前後の全入力SHA一致26923e04d50d83c4649c2b833fd71b6f6a48bbe3181cb42b67e79972abf7e3ec。モデル0/DB書込0。これを実API比較に進む機械条件とする。

実行担当は本セッションの本番読取確認として、配備済みcompare_snapshotに既存call_work_modelを明示して呼ぶ。入力SHAは上記と一致する場合のみ開始。対象37ジョブにつき最大1回、最大37呼出し、再試行なし。各API前後の再読取で入力/マスタ/訂正/解析/リンク変更を検知したら停止。モデル応答不正・作品矛盾・例外は停止し、候補を採用しない。入力/出力/エラーはローカル非公開ファイルへ保存し公開ログに出さない。明細ID完全一致・RAW固定はサービスの既存ガードで検証する。

返却は常にadoptable=false。保存/対照/候補を分けて、既確定602の商品変更/未確定化、未確定160の候補増、空work、未確認ラベル数を集計。新規候補の正解は原文/作品/型番と商品参照の実物で個別に確認し、未確認や矛盾を推測で埋めない。候補数増加を精度向上と称さない。既存解析/訂正/配信テーブルへのDML、再抽出、全再解析、シート更新は禁止。

自己審査APPROVE（読取比較の実行手順のみ）。根拠は本番対照762件不一致0、実装CI2759成功、配備HEAD/ファイル一致、固定SHAによる前後検査。独立レビューではない。外部事例や新規SDK仕様に依存せず既存本番SDK呼出しを再利用するため、新たな外部仕様調査は不要。維持担当は本設計担当、機械契約は比較サービスと既存CI。失敗時は当該実APIを止めて原因と未確認を記録する。


### 17.9 診断付き単一投稿の再試行案（実行承認待ち）

§17.8は本番初回callbackでMODEL_CALL_FAILEDとなり停止。商品精度判定には未到達。既存データ保持と対照再現は成功しているが、実APIエラーの診断根拠が不足したため、残り36投稿へ続行しない。

再試行案: 停止した同一投稿の同一2明細に対象を限定し、固定全入力SHAが一致する場合のみ、既存call_work_modelをアプリ側1回呼ぶ。独自の自動再試行なし。残り36投稿を自動開始しない。SDK内のHTTP再送回数は未確認であり、アプリの呼出し1回と区別する。APIの前後で全入力をREAD ONLY再検査し、応答は既存2列parser/作品矛盾検査で確認。成功しても2/762件の診断的比較として扱い、全体精度向上や採用を宣言しない。

例外捕捉はcompare_snapshotが包み直す前のcallback境界で行う。例外クラス名、整数の場合のみcode/status_code、発生stageとUTC、アプリ試行回数を非公開証跡へ記録。キー/URL/ヘッダー/任意例外本文は公開しない。コード取得不能はunknownとして残す。元応答が返った場合だけ非公開保存する。欠けた原因を推測しない。失敗時に追加実行しない。製品コード/DB/CI/secretsの変更は不要で、限定した実行手順の問題として扱う。

自己審査: 診断手順案APPROVE、原因確定・モデル比較完了は未達。本番再試行は、POの検証失敗時停止・質問指示に従い承認待ち。独立レビューではない。次にPOへ求める判断は同一投稿の診断付き再試行1回の可否のみ。


### 17.10 対象更新による停止（13:41）

§17.9の再試行はPO承認後、旧対象18/44原文の無効化・置換によりAPI前停止。旧全入力SHAと762明細を現在有効な対象へ流用しない。無効チェック解除・26投稿だけへの分母縮小・置換先IDへの推測対応は禁止。新確定取込c0d933e7-d9f2-48b8-ac88-66c62da1b1b0は44有効、観測時7ジョブ未終端。対象切替はPO確認待ち。承認後も全ジョブ終端を読み取り確認し、新明細ID/RAW/訂正/解析/マスタと保存対照を固定して診断対象と上限を明示する。旧602/160を新分母へ転用しない。前回モデルエラーの原因は未確定を維持。根拠はrecon同時刻節。


### 17.11 新確定取込への切替（PO合意）

POは13:36確定取込への対象切替に「合意」と回答。新取込c0d933e7-d9f2-48b8-ac88-66c62da1b1b0は44有効投稿、done38/empty5/error1で全終端。error1は保存ログSoftTimeLimitExceededで、成功扱いにしない。新729明細/商品確定552/未確定177/訂正0、保存商品ID・確定flag対照不一致0、全入力前後SHA719788e246589c708f66ead2d74040f6125cb254c576e3e6e53584fe823c72cc一致。旧762/602/160は新基準へ流用しない。

診断は§17.9のアプリ1呼出し上限を維持。旧対象原文のsuperseded_byが示す新原文cced581f-7368-4767-8927-710a86c0b4c1を新取込内で確認。新job df42518d-711a-48aa-afc4-e41c5c8960f1はdone、保存明細13。旧2明細との1対1対応は推測しない。新13明細ID全件を固定し、この1投稿だけで診断付き比較を行う。他投稿へ自動続行せず、結果は未採用/ラベル未確認。前後全入力SHA一致・応答ID完全一致・明示作品との矛盾拒否を維持。自己審査APPROVE（更新対象の単一投稿比較手順のみ）。エラー1投稿の再実行/DB書込/配信は対象外。


### 17.12 比較用Client寿命の修正設計（実装承認待ち）

目的: 比較専用のGemini接続を応答取得まで保持し、通信前に閉じる不具合を除く。POに見える変化は比較処理がモデル応答を受け取れる入口を直すこと。応答の正確さや精度改善の保証ではない。Whyはrecon「新対象の診断結果・通信クライアント寿命の不具合」の本番実物、公式2.8.0ソース、通信なし4確認true。外部事例は不要、直接の不具合再現で根拠を確立した。

変更対象はtcg_work_comparison_svc.call_work_modelのみ。`with gemini._get_genai_client() as client:`で応答取得とtext読み出しまで保持し、正常/異常時ともcontext終了でcloseする。モデル名/temperature/プロンプト/比較ルール/読み取り専用/例外外側契約を変更しない。DB/migration/CI/secrets変更なし。診断記録は§17.9の呼出し境界で継続し、任意例外本文を公開しない。

代替案: client変数への代入だけでも早期破棄を防げるが、例外時を含め終了時の解放を明確にできるwithを採用。SDK版固定・モデル変更・自動再試行は実証した原因への対処ではないため不採用。SDK Clientにcontext管理があることを公式資料と本番実物で確認済み。

| 基準 | 検証方法 |
|---|---|
| 送信処理中にclientが未close | 破棄でcloseする寿命感知FakeClientを毎回生成し、実call_work_modelを試験。修正前は失敗、修正後は成功 |
| 正常/例外どちらも解放 | close状態を別stateで記録し、応答取得後/例外伝播後のclose=Trueを確認 |
| 出力/引数の契約維持 | fakeの受信model/contents/temperatureを検算。空textは空文字、例外は従来どおり伝播 |
| テスト中Gemini実呼出し0 | fake以外のclient生成は失敗にする。外部通信を使わない |
| 既存比較保護維持 | 既存単体/実PG・全表不変・訂正検知を既存CIで再確認 |

Mockを外側で保持してClient寿命を延ばす試験は禁止。fake factoryが新規Clientを返し、ModelsはClient本体への強参照を持たない構成を使う。SDK自体の通信テストとは称さない。守り手は既存比較テストとBackend CI、設計担当が本番手順と境界を維持する。

Architect自己審査APPROVE（限定修正設計）。根拠は既存式の早期close再現と保持案の正常/異常時解放4確認true、公式仕様一致。独立レビューではない。実装と番号付きGO・再配備は未承認/未実施。本番再比較は修正CI成功/正規マージ/配備確認と最新固定入力再検査の後に別手順。採用/全再解析/配信REVISEは維持する。

## シンソク抽出の時間制限見直し（2026-09-13）

長い投稿を途中で打ち切らないための限定草案。親: [商品マスタ](../../specs/product-master/README.md)。根拠: [recon.md](./recon.md) 同名節。ADR-154/ADR-113。mode: handoff。

### 目的・Why（ADR-154 Why転記用草案）
PO方針は精度が良いなら現方式を維持し時間制限を解消すること。固定原文2回は99.842/136.291秒、解析0.554/0.683秒。原文1176照合一致。Geminiの抽出量上限でなくCelery soft100/hard120による終了が実測されたため、時間制限だけの変更を選ぶ。全体精度やマスタ全項目の正しさは保証しない。

### 対象・変更前後・契約
- backend/app/tasks/tcg_extraction.py:299–300、登録task tcg.extract_source_message のsoft_time_limitを100→300、time_limitを120→330とする。
- 300秒は実測最大136.291秒の約2.20倍を取った暫定運用値。分位点を推定した値ではない。hardとの差30秒は既存エラー記録/後処理の猶予案で、DB障害時の完了保証ではない。
- モデル/prompt/temperature/参照、抽出・解析・統合・配信、SDK再試行、Celery再試行と同時数を変えない。全taskのグローバル値は触らない。無制限化しない。
- DB/migration/UI/CI/deploy.yml/本番scripts/secretsと3商品の型番修正は対象外。既存stale診断はcreated_atから10分で待機時間も含むため、待ち増加時の誤検知は監視し本便で自動修正しない。
- 新値の実装はPO明示承認後に実装役が担当。設計セッションは製品コードを変えない。設計合格はマージ/本番反映/再抽出/配信GOではない。

### 受入条件と検証
| 条件 | 方法 |
|---|---|
| 対象taskだけ有限soft300/hard330 | 登録task属性とworker有効設定を読取。CLI上書きも確認 |
| 抽出・解析内容の不変 | 両サービス/prompt差分0、既存抽出/作品IDのBackend CI成功 |
| 正常終了とsoft超過の既存done/error契約 | 既存エラー試験を確認。不足時だけ既存抽出テスト内に模擬例外試験。CIの実Geminiは禁止 |
| 顧客情報の非公開 | 原文/マスタ/応答/対照表はrepo外、PRは数値とSHAのみ |
| 本番の完了 | 承認済み通常配備後に有効値確認。別途承認した長文1件のdone/明細/解析/原文一致/重複0を確認するまで解消済みとしない |

### 代替・弊害・維持
180秒案は2回だけでは変動の尾を評価できない。無制限は異常占有を止められない。分割・モデル/思考設定変更は品質への別影響を増やすため選ばない。300/330も将来失敗0の保証ではない。
最大占有が100/120→300/330秒へ増えるため待ち時間/API消費増の可能性がある。実装役は既存CIを維持、PO/運用担当は反映後の最初の取込でdone/error・待ち件数・実行時間を確認。追加失敗/滞留/精度悪化時は該当採用・配信を止め、job/時刻/例外/所要時間をreconへ残して再設計する。

### 外部・過去事例
外部企業事例は該当なし。自社実測の限定是正で、他社の精度を成功証明に使わない。Context7は利用できず、PO許可の代替として本番SDK2.8.0の型説明と公式資料を確認。
https://docs.celeryq.dev/en/stable/userguide/workers.html#time-limits
https://ai.google.dev/gemini-api/docs/troubleshooting

### Architect自己審査・状態
APPROVE（時間制限2値に限定した設計案）。原因・境界・有限停止・受入が明確。3商品の型番不整合と統合配信未検証は分離して残す。同一AIの自己審査で独立レビューではない。
300/330秒の数値と限定実装・実装役への委任は、2026-09-13 14:44 JSTの受領記録でPO原文「進める」により承認済み。CARD-LINE-EXTRACTION-TIMEOUT-05を発行。マージ・本番反映は承認対象外。設計担当が実装役へ自動転換するものではない。

限定実装の成果物確認: 実装役が2定数だけを変更し設計担当が差分を直接確認。登録値soft300/hard330は実装役報告。PR/CIへ進む段階であり、本番反映は未実施。


## 連続半角空白の吸収（2026-09-13・追加設計草案）

親は[商品マスタ](../../specs/product-master/README.md)、根拠は[recon](./recon.md)の「連続空白対応の導入依頼と並行作業確認」。既存テーマの延長、mode: handoff、ADR-154/ADR-113。

目的: POの「連続する空白は1つにする仕組みを導入して様子見」を、保存原文を変えず商品照合で実現する。導入方針は受領済み。全商品への適用は作品を限定しない共通の商品照合入口を意味し、全データの改善保証ではない。

草案の境界: まず連続半角空白U+0020が2個以上の部分だけを1個へまとめる。全空白削除・全角空白/タブ/改行の置換・前後trimは追加しない。商品照合に用いる商品名・検索語・除外語と、商品除外に使う状態/メモの局所コピーだけに対称適用。共通normalize_enやmatch_one_kwを変更せず、状態判定・単位・数量・価格・作品判断・原文/マスタ保存を維持する。DB/migration/CI/secrets/本番操作は対象外。Gemini呼出しは不要。

選択理由/ADR Why向け根拠: 固定729明細の設計検算552→562、既特定552の商品変更0。単一スペースを単語境界として残すため、全削除より変更範囲を限定できる。検索語の表記追加は2/3個等を個別管理する必要がある。外部事例は不要（当該入力と実物の局所処理で原因を再現）。本検算は正式pytestや正解率の実測ではない。

受入案: (1) 合成例で半角空白2/3個の名/検索語/除外語が片側・両側にある場合を照合、複数候補は未確定維持。(2) 空白1個と空白なしを同一視しない、新たに改行/タブを連結しない。(3) 作品不一致・作品不明型番・数字接尾境界・Single除外を維持。(4) 実analyzer/読取比較が同じ候補を返し入力の全RAW/訂正を保持。(5) 既存Backend CIの合成/隔離PGで全成功、実Gemini0。(6) 配備後の最初の通常取込を読取確認し対象件数/誤特定/要確認内訳を記録、疑わしい商品は配信対象にしない。過去取込の一括更新・採用・配信は別手順とする。

接触面: POは改善と誤判定を確認、実装担当は既存3473担当との重複を避ける。機械は既存Backend CIを使う。データは読取用コピーのみ変更し原文/辞書は保持。本番は別PRの正常配備後に確認、外部API設定/シート変更なし。

Architect自己審査: REVISE。原因と最小案は検算済みだが、同じ関数のPR3473が反映準備中で最終基点を未確定。新候補選択/R5品質検査との整合、basis表記5件への影響をどう扱うか、解析版の識別、回帰期待値を3473反映後の実物で確定する必要がある。未解決のまま実装可能カードを発行しない。同一AIの自己審査で独立レビューではない。推奨は3473反映後に追加する順番。導入の再承認ではなく並行作業順序をPOへ確認する。

維持の仕組み: 商品照合実装担当が既存 .github/workflows/test.yml のBackend Testsと照合回帰を維持する。PO/運用担当が最初の取込で誤特定0を確認。自動採用や誤一致の自動修復は加えない。


### 連続空白案の最終契約（PR3473反映後）

PO原文「進める」は、3473反映後に追加する順序への承認として受領。導入方針の再承認は求めない。PR3473は8d5aa581でMERGED、Deploy34743294988成功、後続62e22f8cのDeploy34744293217も成功。本番analyzer SHA3b674c3ee92ce333161fbedd02db3f5ea94a69bdb24ca1c83b108e579e704d52は3473マージ版と一致。前節の実物未確定を解消し、以下を優先する。

1. 商品専用helper collapse_product_spaces(text)をtcg_analyzer_svc.pyへ追加し、U+0020の2個以上だけを1個へ変換する。全角/タブ/改行/先頭末尾の単一空白を追加処理しない。原文/辞書オブジェクトを書き換えない。
2. match_product_keywordの入口でkwとnormalized_textの局所変数だけにhelperを適用。その後の単語境界/数字接尾の既存処理は保持。match_pid_with_workの検索・除外の双方がこの関数を通るため、状態/メモの除外照合も同じ扱いになる。通常の状態/単位判定は変更しない。
3. 辞書リスト自体は変換しない。matchedへ返す元検索語、その長さ、候補選択関数と順序を維持する。新たに先頭の語が成立する場合のbasis変化は許容し、商品ID変更と区別して比較する。PR3473の日本語商品名全体一致fallbackは関数/優先順とも変更しない。
4. 品質検査tcg_keyword_lint.pyのR3/R6同語比較はnormalize_enとhelperの合成値を使う。R4とR5の通常検索/除外照合は、既存match_one_kwへhelperを通した両辺を渡す。R5同語skipも同じ比較値。R5の日本語全体一致fallbackは元の引数/関数を保持。R1/R2/R7と重大度・停止方針は変更しない。数字ガード追加等の別仕様をlintへ混ぜない。
5. 保存結果の識別のためENGINE_VERSIONをname-first-v8-product-space-runsへ進め、期待する版文字列の試験を更新する。旧RAW/作品参照/prompt版は変更しない。過去の保存行を版更新だけで再解析しない。

最新版の純関数を用いた固定729明細の検算は商品特定553→563、既特定553の商品ID/確定boolean変更0。前版552→562との差1件は3473側の効果で、本案の改善に加算しない。全戻り値差分11件は未特定10件と、同商品PM0264のbasisがFUTURISTIC BOX→30th CELEBRATION FUTURISTICとなる1件。元辞書の先頭成立語という既存規則による。入力保持true、DB/Gemini0。これは現本番保存件数ではなく固定コピーの試算。

実装対象はanalyzer、keyword_lint、既存test_tcg_keyword_matching.py、test_tcg_keyword_lint.py、test_tcg_product_guards.py（ASTローダーへhelperを追加）、test_tcg_work_matching_integration.py（版期待値とPG例）、test_tcg_work_comparison_pg.py（比較と実解析の同候補）に限定。設計上の本番後観測は最初の通常取込1回、PO/運用担当が空白差対象・商品ID・要確認理由を確認。誤特定または既存商品判定の悪化を発見した対象は採用/配信を止め、元入力IDと根拠を記録して戻す。自動修正/自動ロールバックを追加しない。

受入試験は前節に加え、元検索語basisと入力不変、R3/R6の空白差重複検出、R4自己除外、R5両方向と除外による抑止、3473既存293名称/96設計例、旧tuple、単位/作品/Single境界を含む。期待は既存試験全成功・連続空白10例再現・商品誤特定0。合成の否定例と隔離PGで確認し、実Geminiは使わない。CI未実行を合格扱いしない。

Architect自己審査: APPROVE（上記の限定実装設計）。根拠は3473実物/配備確認、対称処理の呼出関係、729件の局所検算と既特定維持、品質検査の整合契約、範囲/受入の明確化。同一AIによる自己審査で独立レビューではない。設計合格は製品実装完了・本番改善・マージGOではない。本セッションは設計担当を維持し、カードを実装役へ渡す。サブエージェントや別AIセッションは起動しない。


## カードセットの通常商品への誤一致防止（2026-09-13）

親: [商品マスタ](../../specs/product-master/README.md)。mode: handoff、ADR113/154。PO原文「9種セット」誤判定1件も対策」を追加の限定対策依頼として受領。GO3481は空白修正だけの承認であり本変更のGOに流用しない。

目的/受入: 原文「MEGA 30th CELEBRATION カードセット (9種セット)」を通常商品PM0263へ誤確定しない。該当する集合商品をマスタで確認できないため商品未特定/要確認とする。9明細へ分割したり数量を複製したりしない。個別カードセットの正式名9種は対応するPM0276〜PM0284へ確定し、通常商品/プレミアムデッキ/FUTURISTICは維持する。

既存の辞書修正経路を使い、tenant_004のPM0263（30th CELEBRATION、作品IP001、分類PC_BOX）の除外語へ「カードセット」を1行追加する。商品検索語/他商品/他テナント/原文/既存解析結果は変更しない。新商品登録・モデル変更・作品判断・新しい照合機能・状態/価格/数量/単位/配信変更は対象外。

Why: 稼働READ ONLY確認では該当明細はactive/done、PM0263へ確定しneeds_review=false。PM0263の除外語3個はFUTURISTIC/プレミアムデッキセット/エーフィのみ。固定729件をv8で再照合して除外語だけを足すと、対象集合1件がNONE、個別9種の重複候補が正しい各1候補になり、総特定563→571。全変更10件、残る719件の照合結果不変。既存19正解ラベルでは18→19（1件を適切に保留）。個別9種は原文dd423a78の共通見出し19行と各種類21/25/29/33/37/41/45/49/53行を読み、マスタの9種と一致を確認。完全名が単一行に存在するとの主張はしない。

代替: 「9種セット」だけの除外では個別9種との候補競合が残る。「セット」全体を除外するとプレミアムデッキ等を巻き込む。「カードセット」は該当商品群を区別する原文の語であり、通常商品だけから除外する最小の辞書修正を選ぶ。新しい集合商品を推測登録しない。外部事例は不要、自社原文/マスタ/実matcherが直接の根拠。

実装契約: migrations/20260913_200000_tcg_cardset_exclusion.sqlをBEGIN/SET LOCAL lock_timeout5s/statement_timeout30sで実行。必要5表0なら変更0、一部不足/対象のcode-title-work-category-active不一致/対象除外語重複なら変更前停止。必要表をSHARE ROW EXCLUSIVEでロックし、既存3除外語は保持、新語の位置はMAX+1。既登録1行なら変更0。SQLは対象1行追加のみ。既存scripts/run_all_migrations.shにrun_sqlの登録1行を追加し、他行は保持する。これは既存の辞書修正カードと同じ配備経路に限る。CI/deploy設定変更なし。

検証計画: 使い捨てCI PostgreSQLで追加1/再実行0、他tenant/他商品/既存語保持、対象identity各不一致/部分構造/重複で原状保持、表0で変更0を検証。固定期待例は通常/FUTURISTIC/プレミアム/9個別/9種集合/状態メモ除外。CIで実Gemini禁止。既存のkeyword qualityと商品照合回帰も検査。ローカルDocker不在なら正式PGはPRのCIで実行し、未実行を成功扱いしない。

接触面: POは集合商品を誤確定しない結果を確認、本人実装担当が本カードだけ実装。機械は既存migration runner/Backend CI。データはtenant_004の1除外語、配備前backupとtransactionで保護。本番マージは新PR番号付きGO後、外部API/シートは変更0。保存済みの誤結果1件の修正は配備後の別読取確認/対象限定再解析手順とし、本migrationで履歴を上書きしない。

Architect自己審査: APPROVE（除外語1行の限定設計）。原因と選語理由、719結果不変、個別9種の原文照合、同一性ガード/原子性/再実行性/対象外が明確。独立レビューではなく同一AIの自己審査。実装・試験結果は後続に記録する。マージ/本番データ更新/再解析の承認を兼ねない。

維持: 商品辞書担当が既存 .github/workflows/test.yml と実PG回帰を維持、run_all_migrations.shの登録をCIで確認。商品名称/作品/分類の変更で同一性検査が失敗した場合は例外を緩めず再設計する。


## カードセット登録と相互除外・改訂2（2026-09-13）

位置付け: 商品マスタ既存テーマの延長。親 docs/specs/product-master/README.md、ADR113 mode handoff / ADR154。PO最新依頼は個別3商品と9種セットを検索/除外語で識別し正答率100%を目指すこと。新規登録の指示を、同じ商品の重複登録や本セッションの自動実装担当化/新エージェント起動/マージGOには読み替えない。

事実: 本番READ ONLYでPM0276/PM0277/PM0278は指定のフシギダネ組/チコリータ組/キモリ組として存在し有効。先頭「ポケモンカードゲーム MEGA」を省いたPO表記も既存検索語が拾う。9種セットに該当する商品は、CELEBRATIONを含む名称/9種・９種の検索で0。PM形式最大PM0296。既存9個別はDIV01/IP001/MK001/PC_BOX、category_class Box。同系列の先行集合PM0275も既存migration上Box。分類は既存の販売単位と整合させ、箱入数/パック数/重量/発売日/英名を推測追加しない。

### 最終変更契約

- 個別3商品の新規複製は0件。PM0276〜PM0278のUUID/正式名称/検索語を維持して対応する。残り6個別も既存UUIDのまま。
- 新規は「MEGA 30th CELEBRATION カードセット（9種セット）」1件。予定コードPM0297、DIV01/IP001/MK001/PC_BOX、Box、有効。コード/同一名称の既存有無は実行直前とtransaction内で検査する。別商品がPM0297を使用済みなら停止し、別番号へ独断変更しない。同一商品が他コードにある場合も重複作成せず停止して設計へ戻す。
- PM0297の検索語は `30th CELEBRATION カードセット (9種セット)` 1本。既存の日本語混在語のAND照合と英数/括弧の幅正規化を使う。`9種セット` 単独は19種等の部分一致を生むため採用しない。名称の括弧なしや任意の略称まで自動特定を保証しない。未実測の表記は正解確認後に別途追加する。
- PM0263/PM0264/PM0265（通常/FUTURISTIC/プレミアム）の除外語へ `カードセット` を追加。PR3483のPM0263追加は維持し重複しない。
- 個別9種PM0276〜PM0284の除外語へ `種セット` を追加。集合名に個別名が含まれても個別へ誤確定させない。複数の個別名があり集合数の記載がなければ推測で9種セットにしない。
- PM0297の除外語に `FUTURISTIC` / `プレミアムデッキセット`。矛盾する商品系列名が併記された場合は未特定とし、無理に確定しない。
- 保存原文/価格/数量/状態/単位/手動訂正/既存解析結果は更新0。9倍/9行への分割0。商品を選ぶ仕組みは既存のまま、Gemini/prompt/model/API/CIの変更0。

### 安全な保存方法と影響範囲

既存CARD07のSQLを無断で書き換えず、追加migration `migrations/20260913_210000_tcg_cardset_bundle_registration.sql` とrunnerの登録1行、既存PG統合試験へ追加する。未マージPR3483を改訂して一体で審査する経路とし、旧HEADのテスト合格を改訂後へ流用しない。
BEGIN、局所lock_timeout/statement_timeout、参照/商品/検索/除外の表状態検査、SHARE ROW EXCLUSIVEロック、対象12既存商品のコード/正式名/分類/有効状態検査、参照マスタ有効確認を実装する。構造不在の空CIテナントはno-op、部分構造は例外。同一性/重複/コード競合時はtransaction全体をrollback。既存語は追加のみ、位置MAX+1、2回目は新規0。別tenant/別商品不変。新集合の英名・発売日・入数はNULL、同系列から発売日等を転記しない。

### 成功条件と検証

1. 固定729明細のうち原文と商品正解を確認した関連28件（従来19+個別9）で商品ID正答28/28、誤商品0、未特定0。旧ラベルの集合1件「未特定」は、POの新しい商品登録依頼により新集合商品へ変更したと明記する。歴史的な19件評価を書き換えない。全729件・状態/価格/数量・未知投稿の正答率100%を主張しない。
2. 対照v8/変更前辞書から変更10件（集合1/個別9）、他719件の全商品照合戻り値不変。入力コピー元不変、DB接続0/実Gemini0。これは設計検算、正式PG/本番反映とは分離する。
3. 境界58例を既存matcherで直接照合。2表記×1〜20種=40、全角9括弧/◆2、個別9、通常3、別作品/種数なし/系列矛盾4。期待58/58。58例には適切な未特定が含まれ、58商品特定成功ではない。追加略称の安全性は未確認。
4. 実装後の隔離CI PGで商品追加1・再実行0・個別3UUID維持・除外/検索の追加本数・別tenant/無関係商品/既存語保持・別商品コード衝突・同一名称の別コード登録・参照欠落/無効・対象改名/無効・部分構造・transaction rollbackを検証する。58期待例を実PGの保存辞書で再確認する。
5. make lint-ci、migration全件/登録点検、Backend CI、card-lint、task-state、ADR索引、diff検査。実Geminiをテストで呼ばない。Docker不在時ローカルpytestは禁止。旧PR3483の3295成功は旧版だけの根拠。

代替と理由: 4件を全て新規にすると個別3件を複製し、候補競合と在庫分散を増やす。9種セットを未特定に止めるCARD07だけではPOの新しい登録意図を満たさない。単独9種検索語は別商品や19種に当たり得る。既存3件再利用＋集合1件追加＋系列/個別の相互除外が最小で目的に合う。外部事例は不要（自社の原文・DB・実照合関数を直接検算）、外部公式商品名/英名/入数を新規に断定しない。

### Architect自己審査

APPROVE（設計合格、同一AIによる自己審査）。根拠: 既存3件の実在、9個別と分類整合、商品新規1件のPO依頼、28ラベル/58境界例の直接検算、追加のみ/衝突停止/冪等の既存経路。既存カードの「集合未特定」契約は今回改訂が優先。未解決: 実装後の正式PG/CIと番号付きGOは未実施、括弧なし/未知表記の安全性は未確認で適用対象外。設計合格は実装/マージ/配備/再解析/配信の完了を兼ねない。

維持の仕組み: 守り手 backend/tests/test_tcg_work_matching_integration.py、.github/workflows/test.yml、.github/workflows/migration-guard.yml。商品管理担当は新しい略称追加時に全同系列の正例/誤一致例を確認し、コード衝突/分類不明時はPOへ戻す。実装担当への明示的な割当て後にカード08を実行する。


## Gemini判断の記録方法・検討草案（2026-09-14）

状態: PO依頼「記録方法を検討してくれ」に対する未承認草案。親は本書§16/17・商品マスタ仕様、ADR113 handoff/ADR154。現行の「作品IDのみ判断」を変更する承認ではない。製品実装・Gemini実行・本番更新は対象外。

目的: POが1明細について「送った原文とマスタ → 返ってきた行範囲と作品ID → 機械検証 → 正解」を確認できる。Gemini内部の思考は復元できない。追加で答えさせる根拠はモデルの申告であり、当時の内部思考の証明として表示しない。

### 観測事実と不足

2026-09-13の読取基準は作業枝6e9e637c、origin/main 313d7796。対象2サービスはmainと差分0、taskは制限時間のみ差分。本番の現状や翌日のmainと同一とは断定しない。

- gemini_extraction_svc.py:58–84,198–200: 商品名・型番・検索/除外語を含む参照から作品IDのみ判断、原文9項目との10列。商品ID出力は禁止。
- tcg_extraction.py:152–164,209–228,242–266: マスタ固定コピー/ハッシュ、抽出項目、作品ID、行範囲、prompt版、状態/エラーを保存。通常taskにraw_responseの保存処理はない。
- gemini_extraction_svc.py:352–393: 成功時raw_responseを返すが、解析例外時は空文字。形式不正応答の調査にも不足がある。
- tcg_work_comparison_svc.py:23–32,193–261: 固定明細IDごとの作品IDだけを受け取り、非採用の比較レポートを返す。商品候補の回答・永続履歴機能ではない。固定明細比較では行分割精度は測れない。

### 推奨する二段階

A. 通常抽出の観測記録を先行。現行指示と10列を維持し、送受信した事実を記録する。作品IDと行のまとめ方を検証できる。
B. 別の比較実験で商品候補IDと根拠の回答を求める。指示が変わるため別prompt版で区別し、販売データ/配信へ自動採用しない。商品名・型番は候補IDに対応する固定マスタから表示し、生成させない。Bは追加の判断範囲への承認後に実施する。

| 単位 | 記録するもの | 確認目的 |
|---|---|---|
| 試行 | テナント、取込/投稿/抽出ID、試行ID、親試行ID、通常/比較区分、開始/終了/失敗段階 | 再試行を区別する |
| 入力 | 実際に送信した行番号付き原文、マスタ固定コピー、指示文と各ハッシュ | 当時の入力を確認する。ハッシュだけでは代用しない |
| 条件 | 要求モデル名、prompt版、コード版、明示設定、取得できた応答メタデータ | 条件の異なる実行を混ぜない。未取得は未取得とする |
| 応答 | 加工前の返答、受信有無、形式検証結果、エラー分類、所要時間 | 応答なしと形式不正を分ける |
| 明細 | 応答上の明細番号、原文行範囲、原文抽出値、作品ID、保存後の明細IDとの対応 | どの行を1商品にまとめたかを見る |
| 比較候補のみ | 商品IDまたは保留、根拠の原文行/引用、参照マスタの該当箇所 | 候補の根拠を確認する |
| 機械検証 | ID実在、作品整合、引用一致、現行matcherの検索/除外結果 | モデルの申告と機械で確かめた事実を分ける |
| 評価 | 人が確認した正解・確認者・版、作品/商品/行範囲別の正誤、未評価、反復一致 | 正確さと安定性を別々に測る |

保存先は既存TCGデータ領域に紐付く専用履歴を推奨。抽出IDに加え試行IDを開始前に確保し、再試行は別記録として過去を上書きしない。返答は構文解析前に保存する。強制終了は「開始のみ・終了未確認」として検出可能にし、応答未受信と保存失敗を同一視しない。参照コピーの再利用はテナント権限と削除整合を保つ。原文全文をGit/PR/一般ログへ出さず、/tmpだけを恒久保存先としない。物理スキーマ・保持期間は未決。

画面案: 原文の該当行を強調し、横に抽出値/作品名、比較時だけ候補商品、機械検証、正解を並べる。「モデル申告」「システム確認」を区別する。過去の候補や理由を後付け生成して当時の記録としない。短い根拠参照を使い、全候補を検討したとは表示しない。

### 検証案と限界

- 偽応答と隔離DBで正常・形式不正・応答なし・強制終了・再試行の5種類を5/5識別。開発試験で実Gemini呼出0。
- 未知ID・作品矛盾・範囲外行・原文にない引用の4種類を4/4不合格として記録。引用一致だけで商品正答とは認定しない。
- 比較実験は同じ固定入力/マスタ/設定でまず3回を提案。人が正解を定めたN件について作品/商品それぞれ正答数/N、誤特定、保留、API/形式失敗を記録。予定対象を分母にし失敗を除外しない。未評価は別掲。
- 安定性は3回有効かつ同一商品IDとなった件数/N、3回とも保留、変動、失敗を分ける。同じ誤答もあり得るため正答率の代用にしない。3回だけで将来の安定性を保証しない。
- 行分割は投稿全体を入力した通常抽出で商品数と商品/数量/価格の対応を人の正解と比較する。固定明細の商品比較とは別評価。
- 過去の28/28、再解析15/15はシステムの商品照合結果。Gemini商品候補の正答率として転用しない。

代替/Why: 理由の自由作文だけでは検証しづらく、全候補列挙は返答量/遅延を増やす。送受信の事実と機械検証を優先し、商品候補は別実験とすることで現行契約と比較条件を守る。外部事例は不要（自社処理の記録不足が直接の根拠）。SDK新機能に依存する仕様は今回断定していない。

### Architect自己審査

REVISE（正式な実装設計として未完成。同一AIの自己審査、独立レビューではない）。未決: 履歴スキーマ、保持/削除/バックアップ整合、既存閲覧権限との接続、記録失敗時の本処理、応答容量上限、実際に取得可能なAPIメタデータ。次はAの保存経路を実物で詰め、必要なAPI仕様をContext7または許可済み公式資料で確認する。維持担当/Backend CIの具体的割当ては正式設計で確定する。Bの商品候補判断はPOの範囲決定後。カード発行・製品変更・モデル実行は未実施。


### 通常記録Aの具体化・改訂1（2026-09-14、草案）

追加調査: tcg_diagnostics.py:67–82はrequire_super_admin付きの固定SQL診断。main.py:588–605はTCG診断/取込の登録。tcg_config.py:20–29はサーバ側の検証済みTCG_SCHEMA。tcg_extraction.py:74–102は例外をエラー戻り値に変え、118–164はpending選択後にrunningへ更新するため、履歴の存在だけで二重実行防止や自動再試行を保証できない。runbooks/tenant-deletion-operations.md§2/4/5は論理停止時データ保持、物理削除は個別GOとbackup必須。scripts/backup.sh:19,27–38はDB全体dumpとRETENTION_DAYS=30による世代整理、backup_tenant_before_drop.sh:52–57はschema全体dump。これらはコード/手順の確認であり本番スケジュールや復元試験の成功証拠ではない。

#### 保存構造案

同じTCGスキーマに抽出試行履歴を追加する。既存extraction_jobs/itemsに過去分を補完しない。初版は別の共有参照テーブルを増やさず、1試行の入力JSON内にマスタ固定コピーと実際の送信本文を保存する。参照重複による容量増を許容する案であり、容量実測後に重複排除を検討する。

必須列案: id UUID、extraction_job_id UUID、source_message_id UUID、parent_attempt_id nullable UUID、started_at/response_received_at/finished_at timestamptz、phase text、input_payload JSONB、input_sha256 text、requested_model text、prompt_version text、code_version nullable text、response_text nullable text、response_sha256 nullable text、parsed_items JSONB nullable、validation_result JSONB、error_code nullable text。入力/応答のbyte数と明細数を併記し容量を集計可能にする。要求本文/設定は実際に送信する変数そのものを記録し、別の再構成関数で代用しない。code_version未取得は明示し推測しない。APIが返したモデル版・token数等はA初版の必須要件にせず、SDK変更なしで取得できた事実だけ後続設計の候補にする。

既存抽出項目との対応はparsed_items内に応答明細番号・原文行範囲・生成済み明細UUIDを保存し、明細INSERTと同じtransactionで確定する。形式不正時はresponse_textを残し、parsed_itemsはNULLとする。NULL（未受信）と空文字（空応答）を区別する。モデルの理由/商品候補欄はAに作らない。

親とのFKは同一スキーマ内に限定する案。抽出job/原文の削除で意図しない永久保持を作らないため親と同時削除を候補とするが、親の個別削除経路の確認が残る。保持期間は初版「原文と同じライフサイクル、独自の自動期限削除を追加しない」を推奨案とし、正式な既存保持規定とは断定しない。DBの記録削除とbackupからの消去は同時ではない。通常backupの設定値30日を全backupの即時消去保証や履歴保存期間へ転用しない。テナント削除前backupの保持規定は未確認。

#### 保存順序と失敗時の案

1. 開始: pendingからの取得は条件付き更新の成否を確認し、成功した1実行だけがrunningと試行開始記録を同じtransactionで確定。API待機中はtransaction/ロックを保持しない。開始記録を保存できなければGeminiを呼ばない。
2. 受信: 応答本文を構文解析より前に短いtransactionで保存する。失敗した場合は記録未完了として当該試行の明細確定/自動解析を開始しない。DB障害中にDBへエラーが必ず保存できるという保証はしない。一般ログには試行IDと固定エラーコードのみ。
3. 検証: 既存10列/行範囲/作品UUID/参照変更の検証を維持。不合格なら受信本文と検証コードを残し、明細保存0。合格なら新明細と試行完了状態を同じtransactionで確定後に既存解析へ進む。
4. 中断: 開始または受信のみ残った記録は「終了未確認」と表示する。経過時間だけでAPI未実行や安全な再試行を断定しない。自動再抽出を今回追加せず、既存の対象限定復旧経路で実行中でないことを確認する。再試行は新idと親試行idで接続する。

この停止案は「採用した新しい抽出結果には送受信記録がある」を優先する。DBへの記録障害でその投稿の完了が遅れるため、POへ停止方針を1件の判断として提示する。既に配信済みの旧結果を取り消す機能や配信全体の停止を意味しない。該当新結果は未確定のため配信へ渡らない。

#### 閲覧案

初版は管理者限定の読取APIとして既存tcg_diagnostics routerに追加する案。例: GET /tcg/diagnostics/extraction-jobs/{UUID}/attempts（概要、上限100件・既定25件）と GET /tcg/diagnostics/extraction-jobs/{UUID}/attempts/{UUID}（1試行の詳細）。require_super_adminを各endpointで必須とし、サーバ設定のTCG_SCHEMAのみ使用。URLでschemaやSQLを指定させない。詳細はjobとの対応も照合し、未存在/別jobは404。未認証/一般利用者は既存認証規則で拒否、本文を返さない。応答はCache-Control: no-storeを指定する案。初版は管理者の調査用、一般利用者への権限拡大は別設計。

PO向け行強調画面は記録APIを使う後続便とし、初版でも管理者が対象IDから入力/出力/検証を取得して比較表を作れることを受入にする。UI実装・全候補の理由表示・商品候補比較実験はこの保存便の対象外。

#### 成功条件案・引継ぎ境界

正規応答を返す偽Geminiで従来の10列抽出値/作品IDが完全一致、開始記録失敗時API呼出0、応答記録失敗時新明細/自動解析0、形式不正時本文保存ありかつ明細0、2回試行の記録2件、同時開始2要求でAPI呼出1を検証する。未認証/一般利用者/別job参照で本文返却0。既存のマスタ参照変更ガードは維持。PG transaction試験と既存test_tcg_gemini_extraction.py/test_tcg_work_id.py/test_tcg_work_matching_integration.pyを守り手候補とする。CIで実Gemini0。適用前後の1試行あたり保存byte数/DB追加時間は測定し、実測前に性能合格を宣言しない。

実装予定の責務は追加migrationと既存runner登録、抽出サービスの送受信境界、taskの保存transaction、専用履歴service、既存診断router、関連PG/権限回帰試験。製品コードはまだ変更しない。migration採番、将来TCGスキーマ作成経路、容量上限時の挙動、応答保存中のsoft timeout、親削除/backup保持の整合、POの停止方針決定を確定してから正式なADR113カード検査へ進む。

Architect自己審査: REVISEを維持。閲覧経路、backupに含まれる構造、記録順序、モデル判断を変えない境界は具体化済み。保存障害時の完了遅延は事業判断としてPOへ提示し、残る技術調査を合格扱いしない。Context7 MCPは今回の公開ツール一覧に存在しなかった。新しいSDK/API機能を必須としないため今回は外部仕様への依存を増やしていない。削除処理を探す読み取り検索はPreToolUseの禁止語検知で拒否された。許可スクリプトや制限解除は実行せず、当該検索結果を根拠にしない。


### 通常記録A・実装引継ぎ版（2026-09-14）

承認境界: 直前の質問「記録保存に失敗した場合は、その投稿の新しい解析結果を確定せず、配信へ渡さない方針でよいですか？」に対するPO原文「進める」を受領。この停止方針の合意として記録し、製品実装担当への割当て・マージGOとはしない。以下は設計側の具体化であり、POが全項目を逐語承認したとは記載しない。Aに関して本節を過去の未確定案より優先する。

#### 追加の実物根拠

GitHub mainをAPI確認し313d7796bac62c45067c61dab23180acabc4b979。専用作業枝はmain比20 ahead/83 behindの文書枝であり、実装はこの枝の古いtaskを流用せずorigin/mainから開始する。mainのsoft/hard 300/330秒を維持する。

本番READ ONLY（接続時default_transaction_read_only=onを照合、statement_timeout=10000）でDB jarvis_db、実行salesanchor_app、TCG_SCHEMA=tenant_004。source_messages/extraction_jobs/extraction_itemsはtenant_001とtenant_004に各3表、所有者jarvis、extraction_attemptsは0表。work_reference_snapshot JSONB/sha256 TEXTとresolved_work_id UUIDは実在。DBサイズ94,567,447 bytes。このサイズを空き容量や余力とはみなさない。秘密値・原文は出力していない。

固定入力snapshot SHA f8e3b354284b91aa2be374e8fde57e2d5e5d8c1f2382f58bc7da2bf4745a1385、44投稿/729明細。参照のcompact JSON 83,610 bytes、原文最大9,244 bytes/合計74,648 bytes。実装のannotate_lines/format_prompt_input純関数と現在promptで再構成した送信本文は最小85,939、最大99,099、合計3,886,828 bytes。参照コピーも各試行に重複保持する場合の入力側合計7,565,668 bytes（応答/JSONエスケープ/DB索引/WALは含まない）。最初の手書きsplitlines検算を、実物のsplit("\n") helperで再検算し同値を確認。これらは容量検算で実送信記録/本番保存性能の測定ではない。

Context7 MCPは公開ツールに存在せず、許可された公式代替 https://docs.celeryq.dev/en/stable/userguide/workers.html#time-limits を2026-09-14確認。soft limitは捕捉可能な例外、hard limitは捕捉できず強制終了する。requirements.txt:17はcelery>=5.4.0で厳密固定ではないため、今回の設計で新しい版限定機能を使わない。強制終了時の最終保存成功を保証しない。

#### 確定する保存境界

1. 表名extraction_attempts、通常抽出だけを記録。行は開始→応答受信→完了/失敗と更新するが、終了済み行を再利用しない。監査改ざん防止機構とは称さない。ハッシュは内容照合用で真正性の署名ではない。
2. job FKは同一schemaのextraction_jobs.idに接続し親削除と同時削除。source_message_idは開始時jobから取得する値を保存し、入力から自由指定させない。source親→jobの既存連鎖で履歴も消える。parent_attempt_idは同一jobの直前試行としてサービスで照合し、初回はNULL。過去の親不明を推測補完しない。indexは(job_id,started_at,id)、phase列。既存job/itemsのbackfill0。
3. 本文の保持は原文/jobと同期間。独自の定期削除機能を追加しない。テナント物理削除はsuper_admin_tenants.py:98–178のschema単位処理に含まれる。通常backupはDB全体、削除前backupはschema全体なので新表も対象。バックアップの既存保持/個別削除承認を変更せず、完全削除の保証も追加しない。これは新しい永続的な顧客データ保存規定を決定するものではない。
4. 1試行の入力JSONシリアライズ上限8,388,608 bytes、応答UTF-8上限8,388,608 bytes、parsed_items JSON上限8,388,608 bytesとする設計上の防御値。APIの上限や実測最大値ではない。入力超過はAPI呼出前停止、応答/parsed超過は新明細0・自動解析0。超過本文の先頭だけを全文扱いで保存しない。超過時はbyte数/ハッシュ/固定コードを残せた場合のみ記録し、本文未保持を明示。DB保存自体が失敗したら終了未確認として残る。上限ちょうど/1byte超過を試験する。
5. 正常時は送信contentsと明示config、参照固定コピー、モデル要求値を保存する。HTTPヘッダ/APIキー/クライアント内部設定は保存しない。APIが返すtoken/モデル実版は初版の対象外。要求モデル名から内部の実モデル版を推測しない。
6. 各DB保存transactionは局所lock_timeout=1秒、statement_timeout=5秒で設計する。既存API待機上限は変更しない。性能受入はCIで保存I/O合計の最大5秒以内（偽API待機時間を除く、44固定規模の匿名化データで測定）を目標とし、超過時は理由を調査し閾値を独断緩和しない。将来の本番反映前は新表の権限/スキーマ、DBディスク余力、旧worker停止と新worker版を確認する運用ゲートを置く。実装試験前に性能成功とはしない。

#### 状態契約と中断

phaseはstarted / received / completed / failed。startedはAPI成功/送信済みを意味しない。response_received_atがNULLでも、API未実行と断定しない（受信後・保存前に落ちる場合がある）。一覧では非終端を「終了未確認」とし、経過時間だけで失敗確定/再試行しない。error_codeはAPI_ERROR、SOFT_TIME_LIMIT、INVALID_RESPONSE、REFERENCE_CHANGED、INPUT_TOO_LARGE、RESPONSE_TOO_LARGE、PARSED_TOO_LARGE、RECORD_WRITE_FAILEDなど固定分類。原文を例外文へ埋め込まない。

soft limitは送信ラッパー/解析ラッパーで一般例外へ変換する前に分類し、進行中transactionをrollbackしてから1回だけ短い失敗保存を試みる。保存できなければstarted/receivedのままとする。hard limitやプロセス消失も同じく未完了記録を調査対象にする。失敗保存を無制限リトライしない。明細transactionがcommitした後の中断では完了記録と明細が両方残り、解析実行の完了は別状態として扱う。

二重実行防止はpending条件付き更新と試行INSERTを同一transactionにし、更新1件の実行だけAPIへ進める。成功の直前にはjobが当該試行によるrunningであり、後続試行がないことを再検査する。照合条件が違えば保存/解析を停止する。旧workerとの混在下の排他は保証しないため切替後の新worker開始分を対象とする。記録表だけ追加して旧workerが動く期間を完全記録済みとしない。rollbackで旧コードへ戻す場合も新規の完全記録保証は失われるので、再開の前に運用判断へ戻す。

#### migrationと新規スキーマ

予定migration migrations/20260914_010000_tcg_extraction_attempts.sql（実装開始時に衝突再確認）。DOブロックでtenant_%を走査し、source_messages/extraction_jobs/extraction_itemsが全てあるschemaだけ追加。全0はno-op、一部不足は例外で全体rollback。既存表の不正構造をCREATE IF NOT EXISTSで黙認せず必要列/型/FK/索引を検査。所有者は既存と同じjarvis、実行者salesanchor_appへ新表のSELECT/INSERT/UPDATEを明示し、親削除による履歴削除以外の新しい削除APIは作らない。migration transactionの局所lock_timeout5秒/statement_timeout30秒、2回目変更0、他schema不変をPG試験する。

run_all_migrations.shのTCG基礎表/作品参照migrationより後、末尾に登録する。backend/app/services/tenant.py:1620以降の一般テナント作成は抽出3表を作らない（当該3表名の検索0）。一般テナントにTCGを勝手に追加しない。将来TCGを追加したschemaは基礎表作成後に本migrationを含む公式runnerで拡張し、work_schema_readyと履歴schemaチェックが通るまで抽出を開始しない。新schemaに模擬基礎表作成→本migration適用→必要構造/権限を確認するPG試験を追加する。tenant_001基礎migrationの27表検算は名前リスト限定のため、新表を追加しても旧27表検算を変更しない。

#### 受入試験・審査

正式PG試験: 正常/空応答/形式不正/作品UUID不正/参照変更/開始保存失敗/応答保存失敗/明細commit失敗/soft中断/終了未確認/再試行/同時開始/旧完了行保護/親削除連鎖/他schema不変/新schema適用/再適用/部分構造不正/権限/3上限境界を含む。API権限試験は未認証/一般ユーザー/管理者/別jobとページ境界を検証。既存原文/作品判定/配信の回帰結果を確認し、実Gemini呼出0で実行する。実装後にBackend CIとmigration guardを通す。新コード・新SQL・新テストは本セッションで未実装。

Architect自己審査: APPROVE（通常記録Aの上記限定設計。同一AIによる自己審査で独立レビューではない）。根拠: 本番の対象schema/所有者/実行者/列、既存管理者認証、44投稿の容量検算、停止方針合意、API中transaction非保持、失敗時に採用しない状態契約、追加migrationの既存/将来経路、受入の○×判定。残る実装/性能/CI/復元試験は受入作業として明示し、実測合格に読み替えない。商品候補判断BとPO向け専用画面は別設計で合格対象外。実装担当の割当て・実装開始承認・マージGO・本番反映・再抽出の承認を兼ねない。
