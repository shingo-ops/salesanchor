# 引き継ぎ書（2026-07-02〜03 セッション → 次セッション）

作成: Planner（設計パートナー）／承認: Shingo。正本は docs/STANDARD-WORKFLOW.md・docs/ai-agents/design-partner.md。
検証前に必ず `git rev-parse origin/main` を取り、本書記載のSHA以降のmainと突合すること（EV-20260703-001の作法）。

## 1. 完了済み（すべてMERGED・実測・EV登録済み）
| 便 | 中身 | PR / EV |
|---|---|---|
| reaper修正 | 完了机の未push誤検出を解消 | #2705 / EV-20260701-002 |
| 第1便 | 動線11ファイルを main へ付替・不要WF3削除 | #2715 / EV-20260702-001 |
| 第1.5便 | 守りの移設（worktree検問 main宛化＋main必須チェック10→12件） | #2724＋ruleset 15777895 / EV-20260702-003 |
| 現在地スナップショット | 並行セッション誤報告の防波堤（SHA照合基準） | #2742 / EV-20260703-001 |
| 第1.7便 | エージェント案内書4枚を main 一本化に整合＋正本に接触面分析欄 | #2745（記録 #2748）/ EV-20260703-002 |
| hooks検証 | 住所化け仮説の棄却・検知健在・阻止力に疑い | EV-20260703-003（**PR #2754・本書と同じPR**） |

## 2. いま開いているもの
- **PR #2754**（EV-20260703-003＋本引き継ぎ書・docs-only）: POのマージGO待ち。

## 3. 次テーマ（優先順）
1. **B+C便（agent設定整合）** — スコープは方向承認済み・KGI未設定（①KGIから入る）:
   - B-1: .claude/agents/generator.md に「Plannerカードが存在する便ではカードの許可・禁止が優先」の但し書き
   - B-2: 同 台帳の行削除命令→「DONEに更新（行は残す）」へ訂正（PARALLEL_TERMINAL_GUIDEとの矛盾解消）
   - B-3: 同 「カード実行時の応答様式」節を新設（3部構成: 結論1行→生ログ全文→補足2行以内・要約禁止・承認待ちステップは返信を待つ・同一カード再実行禁止）
   - B-4: docs/ai-agents/design-partner.md §6 に「カード設計の規約」を追加（冒頭上書き宣言／禁止の名指し／停止条件は肯定形「前段が全緑の場合のみ次段実行」／承認待ちは独立カードに分離／消す範囲もアンカー完全一致・広域正規表現禁止／応答様式は generator.md を参照）
   - C: .claude/hooks/check-freshness.sh の鮮度基準を origin/develop → origin/main（現状は永久沈黙）
2. **フック阻止力の修理** — 検知は健在だが「BLOCKED表示後も実行が継続」（PR #2748 実戦ログで確認）。~/.claude/ 配下＝リポ外につき、バックアップ→修正全文提示→PO承認→適用→阻止の実測、の二段構え必須。
3. **dangling-route gate 誤検知対策** — 古い土台のPRで偽赤（実例 #2744）。merge-base 比較 or docs-onlyスキップ。main必須チェック昇格済みのため誤検知＝マージ物理不可。独立テーマ（索引 docs/specs/README.md で類似確認のうえ）。

## 4. develop廃止の残工程（本線・変わらず）
しんご実地確認（次の実務PRが兼ねる）→ 第2便（develop鍵外し・ruleset 16619490・PO画面操作見込み）→ 第3便（撤去・唯一の危険操作・自筆GO）→ 第4便（後片付け: runner-label-lint.yml削除・deploy.yml stamp・残コメント）。
- develop は意図的に存続（SHA 1b9a93b7・ロールバック用・新規作業での使用禁止）。
- main ruleset の変更前控え: ~/ruleset-main-before-batch1-5.json（恒久保存）。

## 5. その他の申し送り
- E2E必須化は skip時判定検証込みの独立テーマ（意図的除外・EV-20260702-003）。
- 案内書lint（実在しないファイル名参照の機械検出）は独立テーマ。
- §3-2 守り正本リスト化は別セッション分担（衝突なし）。
- 綺麗な作業起点は ~/reaper-fix-clone（worktree作成の親）。KGI-3実発火の追認は次に active-work.md を触るPRで。
