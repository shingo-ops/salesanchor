# 設計 — sop-pr-template-hint

**対象ADR**: 対象外（テンプレート文書変更のみ）  
**recon**: docs/handoff/sop-pr-template-hint/recon.md  
**日付**: 2026-09-05  
**担当**: shingo-cc

---

## 外部・過去事例の参照と我々への応用

- 該当なし：PR テンプレートの HTML コメント（ヒント文言）更新のみ。コード変更・DB変更なし。外部事例参照は不要と判断。

---

## 受け入れ基準

| 基準 | 検証方法 |
|------|---------|
| `標準ワークフロー確認` セクションの構成（欄数・順序）が変わらない | `git diff` で行の追加・削除がコメント内のみであることを目視確認 |
| `触るファイル:` 欄のコメントが「半角コロンと同じ行にカンマ区切り。改行して箇条書きにしない」旨の文言を含む | テンプレートの当該行を目視確認 |
| `削除するファイル:` 欄のコメントが「1行でも変更・削除した行があるファイルをすべて列挙」旨を含む | テンプレートの当該行を目視確認 |
| CI `check-process-artifacts` が通過する | `gh pr checks` 緑 |

---

## 技術 How・KPI

- KPI: CI の process-artifacts ゲートが PASS（コメント除去後の構造に変化なし）
- 技術選択: HTML コメント追記のみ。パーサーは `.replace(/<!--[\s\S]*?-->/g, '')` で除去するため影響ゼロ

---

## 弊害・トレードオフ

- なし。コメントはパーサーに無視される設計であることを `check-process-artifacts.js:214,222` で確認済み

---

## 計画票

| ステップ | 内容 | 担当 |
|---------|------|------|
| 1 | `.github/PULL_REQUEST_TEMPLATE.md` の5行のHTMLコメントを更新 | Generator |
| 2 | recon.md / design.md 作成 | Generator |
| 3 | commit → push → PR 作成 | Generator |

---

## 継続

- 完了後の監視: CI pass を確認して終了
- 次フェーズへの引き継ぎ: なし
