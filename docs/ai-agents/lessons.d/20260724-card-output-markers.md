分類: 6-2
出所: （2026-07-24 PR #3082）

- カードで複数コマンドの出力を続けて出させるときは、判定に使う出力を `=== JUDGE_START ===` / `=== JUDGE_END ===` のマーカーで囲み、「マーカーの外は判定に使わない」と明記する。ラベルのない連続出力は直前のコマンドの結果と混同され、正常な状態でも誤停止を生む（2026-07-24 PR #3082 で、`git merge` が取り込んだmain側ファイルを `git diff --stat` の結果と読み違えて誤停止したことを実測）。
- ブランチ間の差分検算は `git diff --name-only origin/main...HEAD`（三点）で行う。二点の `origin/main HEAD` は照会中にmainが進むと相手側の新規ファイルが削除として現れ、判定が揺れる（2026-07-24 実測）。
