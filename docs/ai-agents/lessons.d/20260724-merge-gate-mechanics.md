分類: 6-1
出所: （2026-07-24 PR #3072/#3079）

- `mergeStateStatus` は照会のたびに算出される揺れる値。同一SHA・追加コミットなし・必須チェック全緑のまま CLEAN → UNSTABLE → CLEAN → UNKNOWN → BEHIND と変化した。カードの関門条件にこの値のスナップショットを使わない。関門は「必須チェックが全pass」で置き、マージ可否の最終判定は `gh pr merge` そのもの（サーバー側）に委ねる（2026-07-24 PR #3072 で実測）。
- PR本文の編集は `edited` を購読するワークフローを再発火させ、同一SHAに新しい採点セットが走る。「本文編集だけでは新runは出ない」は本リポジトリでは成り立たない（2026-07-24 PR #3072・run 30048094xxx 系の発火を実測）。GO転記後は再採点の完了を待ってからマージへ進む。
- process-artifacts gate の宣言見出しは `### 標準ワークフロー確認`（h3）。`##`（h2）で書くと「セクションがありません」で落ちる。判定は `check-process-artifacts.js` の正規表現 `/###\s*標準ワークフロー確認/`（2026-07-24 実測）。
- 書類のみの変更でも正本（docs/ai-agents/ 等）を含むと宣言照合へ進む。lessons.d のみの PR #3072 は「書類のみの変更 — 自動スキップ（pass）」で通ったが、evidence-registry.md を含む PR #3079 は 対象ADR / recon / 設計 の3欄が必須になった。記録便でも免除は使えず、そのテーマの既存 recon・design を指す（2026-07-24 対照で実測）。
- GO記録の欄名は `GO発行者:` `日時:` `GO原文:` `バックアップ確認:`。`発行者:` `原文:` では書式違い（`check-process-artifacts.js` の parseGORecord コメントおよび PR #3050 の実例で実測）。

分類: 6-2
出所: （2026-07-24 PR #3079）

- 完了待ちループの判定に「未完了の実行数 = 0」を使わない。push直後は実行がまだ1件も登録されておらず0件が返るため、偽の完了になる。判定は「必須チェックの一覧が空でなく、かつ非passが0件」で行う（2026-07-24 PR #3079 で pending=0 の直後に必須5件が pending だったことを実測）。

分類: 6-3
出所: （2026-07-24 作業台作成の沈黙診断）

- 既知事象への当てはめを実測より先に行わない。作業台作成の沈黙を EV-20260720-001（reaperの排他ロック）と同型と断定したが、実測ではフォルダもworktree登録も存在し、失敗していたのは後続のシェル起動だった。症状の類似は原因の同一を意味しない（2026-07-24 実測）。
