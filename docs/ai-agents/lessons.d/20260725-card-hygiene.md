分類: 6-2
出所: （2026-07-25 現況棚卸し便）

- カードの禁止事項リストに書く語と、実行コマンドに書く語は、字面が衝突しないようにする。多義語は完全な形で書く。「prune 禁止」と書いた便に `git fetch --prune`（追跡枝の掃除・ローカルは無変更）を入れたところ、実装役が禁止語 prune への抵触と読んで誤停止した。禁止は `git worktree prune` のように一意な形で書き、リモート確認は fetch を伴わない `git ls-remote` を使う（2026-07-25 実測。同一メッセージで2回停止）。
- 同じカードで同じ停止が2回続いたら、設計側でカードを作り直す前に、実装役に「いま届いているカード本文をそのまま返して」と依頼し、入力側を逐語で検算する。設計側の手元と実装役の受信は別物で、一致は未検証。実際、2回目の停止は前便のカードに引きずられたもので、当便のカードは正しく届いていた（2026-07-25 逐語返送で確定）。
