分類: 6-1
出所: （2026-07-20 PR #3002）

- 実測カードのgrepは探索先をrevisionで明示する（`git grep <パターン> origin/main -- <パス>`）。ワーキングツリー依存のgrepはworktree状態で偽エラー（path not in the working tree）を出す。またgit grepはBRE制約があり `.*?`・`\d` は使えない（2026-07-20 process-hardening reconで2回停止を実測）。
- 引継ぎ書・要約の「ミスN回」類の記述は、現物PRの痕跡（gateのfailコメント・本文追記履歴）と突き合わせてから設計根拠にする。作業途中で解消された事象はPRに残らず、原因の断定に使えない（2026-07-20 宣言漏れ4回を40PR実測で痕跡ゼロと確定。同日、教訓の記帳先も引継ぎ写しの§6直書きでなく現行lessons.d方式と実測で確定＝古い写しより現物ルール）。
