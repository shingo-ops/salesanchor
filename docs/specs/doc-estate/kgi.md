# KGI（○×で測る・v2.1・PO承認 2026-07-03）

前提: 数値の分母はすべて実測（推測禁止）。現状値の測定基準SHA: b4a1ced。守り手は原則「PRごとの関所（即時検知）」、関所化できないものだけ定期再測定（バックストップ）。

| # | 合格条件 | 測り方 | 合格ライン | 現状値（実測） | 守り手（予定含む） |
|---|---|---|---|---|---|
| K1 | 索引から辿れない文書0・索引リンク切れ0 | docs配下実ファイルと索引の突合＋索引内リンクの実在検査 | 0件/0件 | リンク切れ0（全文書755本） | 関所に検査追加（子便A）。それまで人手 |
| K2 | 置き場ルール（§1.5三ファイル構成）違反の新設0 | 関所で新規specファイルの置き場・構成を機械判定 | 0件 | 未実装（0からの開始） | 置き場SSOT＋増産防止便（循環§5-5予約）＝子便A |
| K3 | 索引の状態語は凡例の定義語のみ | 索引全行の状態セルを凡例5語と突合 | 違反0セル | 違反3セル（注記混在2＋区切り1） | 状態語正規化（バックログ済）＝子便B→以後関所 |
| K4 | 量が索引1枚で読める | 索引冒頭の集計行（テーマ総数・状態別内訳）が実数と一致 | 在=1・一致=1 | 集計行なし（0からの開始） | 子便Bで新設→関所で一致検査 |
| K5 | specs外に散在する正本宣言0 | 正本宣言の逐件台帳と残数 | 0個 | 宣言含む文書17本・うちspecs外15本（grep初期値。宣言単位の確定は棚卸しで） | 正本宣言棚卸し便（循環§5-5予約）＝子便C |
| K6 | K1〜K5すべてに機械の守り手と検知遅れ上限 | 本表の守り手欄の空欄数＋関所化された数 | 空欄0・検知遅れ明記 | 本表で名指し済み（関所化は子便A/B） | 本表自体＝process-artifacts gateの維持欄検査対象 |
| K7 | 収める人が迷わない | (a)「収め方ガイド1枚」（どこに=置き場／何を=3ファイル構成／どれぐらい=§1.8振り分け、の3問に回答）が索引から1クリック＋エージェント案内書から参照 (b)新規収納の初回関所通過率 | (a)2経路とも在 (b)満数 | (a)未作成 (b)測定未開始 | ガイド作成便＝子便D。差し戻し発生時はガイド側を直す（人を責めない） |

KPI: 達成KGI数 ◯/7（未実装項目は n/a 表示。0/0を満数と偽らない＝循環KGIと同規約）。

## 設計送り事項（子便への分解・実装順）
- 子便A: 関所拡張（K1リンク検査・K2置き場検査）— 蛇口優先。
- 子便B: 索引整備（K3語彙正規化・K4集計行）。
- 子便C: 正本宣言棚卸し（K5・重い床掃除。蛇口確立後）。
- 子便D: 収め方ガイド1枚（K7a）。
- FEATURE-INDEX補充（ADR-148/110/091・バックログ済）はK1の導線整備として子便Bに同乗。

## 弊害・トレードオフ（空欄不可）
- 関所実装（子便A/B）までは人手運用の空白期間が残る（規律で守る期間を実装順で最短化）。
- K7の「理解」は代理測定（ガイド実在＋初回通過率）であり、人の理解そのものは測れない。
- K5の実数はgrep初期値（17/15）と過去の概算（約30）が不一致のまま記録されている。棚卸しの逐件確認で確定する（数字を寄せに行かない）。
- 中身の質は本テーマの守備範囲外（PO目視・原理的限界）。
- 索引が将来数百行に達した場合の分節（章立て）は本KGIでは制約しない設計の自由（必要時に子便で対応）。

## 外部・過去事例
内部: 循環テーマのKGI規約（n/a表示・分母実測・守り手名指し）と、#2735の関所実装型（dry-run実証→逐語検収）をそのまま再利用。索引・凡例・接点行の運用は#2741/取引フローとの境界相談の実績に準拠。外部設計の輸入なし。

## 受入基準（本起票PRの○×）
| 基準 | 検証 |
|---|---|
| 3ファイルが上記全文と逐語一致 | diff全文の目視＋grep |
| 索引に1行追加・削除0行・状態語は凡例語「KGI承認済」のみ | numstat＋状態セル目視 |
| 見出し重複検査PASS | 関所（実行出力転記） |
| GO記録4欄PO自筆・merge commit | 2点検算 |
