# テーマ: 台帳と本店の守り — 定点観測台帳

表紙: [README.md](./README.md) ／ 記帳ルールは docs/specs/agent-complete-design/track-record.md に従う。

## 記録（1便1行）
| 日付 | PR | a 指示消化 | b 関所一発通過 | c 実装中停止 | d 本番不具合 | e 5W2H記帳 | 備考 |
|---|---|---|---|---|---|---|---|
| 2026-07-04 | #2783 | ○ | ○ | ○ | − | − | テーマ器＋第1弾設計＋配布原本v2。手戻りゼロ |
| 2026-07-04 | —(手作業) | ○ | −(関所対象外) | ○ | − | − | 第1弾適用: 配布原本cp＋diff逐語検収＋G1実測6項目全○（本店commit拒否/status許可/worktree内許可/develop化石0）。.bak-20260704確保 |
| 2026-07-04 | #2785 | ○ | × | △ | − | ○ | 起因:設計(プランナー) — G2便。初回実測が偽合格(worktree内で試験)→PO指摘でG2.1改修(本店を直接測定)・実測5点全合格。カード欠陥4件→[5W2H-G2]。関所はGO転記前の採点で一度fail、再実行で緑 |
| 2026-07-04 | #2787 | ○ | ○ | △ | − | − | 起因:環境 — design-phase2の記録便。1回目の実行結果が空転送で届かず未実行と誤認→再実行が改良3行の積み増しとして機能し最終形は完全版（書庫側で独立検算） |
| 2026-07-05 | #2788 | ○ | × | △ | − | ○ | 起因:設計(プランナー)＋未特定 — 便1（窓口3本＋単票検証）。第一走で検査官105行が消失も合格基準がexit値のみで素通し→原本復元＋生存検証で回復・最終形は170行追加/削除0→[5W2H-BEN1]。関所×はGO転記前採点（仕様どおり再実行で緑） |


## 5W2H詳細

### [5W2H-G2] G2便のカード欠陥4件（2026-07-04）
- What(何が): ①実測をworktree内で実行し警告が設計どおり抑止され偽合格(T1=0を見逃し)
  ②期待値不一致でも先へ進むSTOP条項の欠落 ③測定と判定を別ステップに分け変数が
  消えて誤STOP ④絵文字入り長文アンカーが輸送中に写し崩れANCHOR ERROR(count=0)
- Who(誰が): 起因はすべてプランナー(Fable・カード設計)。実装役は全件指示どおり
- When/Where: 2026-07-04 PR #2785便・実測手順とカード本文
- Why(なぜ): 挙動条件(場所依存)と試験条件の不一致を見落とし。実測手順の標準型が未定義だった
- How(検知/回復): PO指摘(worktree内なら黙るのは抜け穴では)が①の根本を暴き、G2.1で
  測定対象を本店そのものへ変更(git-common-dir)。ANCHORガードが④を書き込み前に阻止。
  v3カードで測定と判定を同一ステップに束ね実測5点全合格
- How much(規模): 往復4回の手戻り。main無傷・誤マージゼロ
- 教訓: §6へ4行還流(本番同条件の実測/同一ステップ判定/崩れないアンカー+STOP内蔵/
  危ない変更はGO転記後に関所再実行)


### [5W2H-BEN1] 便1の検査官消失と合格基準の甘さ（2026-07-05）
- What(何が): ①第一走完了時、check-active-work-format.sh が18行（先頭2行＋追加16行）に
  なり既定の7列検証105行が消失 ②カードの合格基準がexit値と新モードのテストのみで、
  既定機能の喪失を検出できず一度は「合格」と誤判定
- Who(誰が): ②は起因プランナー(Fable・基準設計)。①の消失機構は未特定
  （一括実行中の複合汚染疑い。証拠: 18行＝shebang＋set -e＋追加ブロックのみ）
- When/Where: 2026-07-05 PR #2788便・worktree release/ledger-guard-ben1
- Why(なぜ): 「exit=0＝既存挙動不変」と短絡した。空の検査官もexit=0を返せる
- How(検知/回復): mainとのdiff --statの「102行削除」が異常を露呈→原本をorigin/mainから
  復元→再注入時に行数レンジ(15-25)＋原文4箇所の生存＋正常出力文言の実在を機械検証→
  単体9件ALL PASS＋純差分「170行追加/削除0」を書庫側で独立検算
- How much(規模): 手戻り2往復。main無傷・誤マージゼロ（GO前に検出）
- 教訓: 書き換え系の合格基準は「行数レンジ＋正常時出力の文言＋削除0の差分」まで含める
