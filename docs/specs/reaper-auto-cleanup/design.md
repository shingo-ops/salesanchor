# design（技術How）

親: ./README.md
recon: ./recon.md
KGI: ./kgi.md

> この文書は何か（専門用語なしの1行）:
> 掃除機を直すための具体的なやり方と、その副作用を書いたもの。

## 1. 設計（3かたまり）

### かたまり① 配線を本店に向ける（K1・K2）

.github/workflows/reaper-schedule.yml の reaper 実行ステップに env を注入する。

    env:
      REAPER_WORKTREES_DIR: /Users/tanizawashingo/worktrees/salesanchor

採用理由: 2026-07-22 の実測で、この指定により本店の87件を正常走査できることを実証済み。checkout 廃止案・reaper 本体自衛案は未実測のため採らない（実測で効くと分かっている最小の変更を採る）。
併せて「なぜ本店指定が必要か」をコメントで明記する（将来の可読性確保）。

### かたまり③ 見張りの自動化（K7・K8）

reaper-schedule.yml の掃除ステップの後に scripts/ghost-count.sh を呼ぶステップを追加する。
ghost-count は exit 値を変えない見張り専用設計のため、掃除本体の成否に影響しない。

### かたまり② 異物の名指し報告（K3・K9）

scripts/reaper-worktree.sh:90 の「ブランチ名が空なら無言で continue」を、SKIP_NOT_WORKTREE 欄に名前を記録してから continue する形に変更する。
異物は SKIP_ 欄に入るため削除ルートに載らない（既存の保護と同じ仕組みに乗せ、削除判定は一切変更しない）。

## 2. 弊害・トレードオフ（空欄不可）

- 即時掃除は Mac がオンであることが前提。留守中のマージは即時起動しない。定期がバックアップするが、定期が回るまで最大1日は溜まる。これはタイミング方式（二段構え）を選んだ時点で受け入れた弱点。
- かたまり①③は .github/workflows 変更＝危険操作。PO の GO 必須・関所の再採点必須・全緑確認後にマージ。
- かたまり②は番人（reaper 本体）に手を入れる。「報告欄を足すだけ・削除判定は不変」を厳守し、配布原本との逐語 diff で検収する。
- かたまり①の弱点: 「なぜ本店指定が効くか」がコメントなしでは読み取れず将来壊れやすい。コメントで理由を明記して補う。
- 接触面17ファイル（recon 観点7）のうち、動線・手順を変える場合は案内書（docs/PARALLEL_TERMINAL_GUIDE.md・docs/ops/runner-commands.md）の同時更新要否を判断する。

## 3. 外部・過去事例

該当なし＋理由: 本件は自リポジトリ固有の self-hosted runner 配線問題であり、外部事例の応用対象ではない。

## 4. 受入基準と検証方法

kgi.md の K1〜K10 を受入基準とする。各Kの測り方欄が検証方法に対応する。
K4・K5・K6 は消せる worktree が1件以上出現してから実測する（2026-07-22 時点で削除対象0件のため実測不能）。

## 5. 維持の仕組み

- 守り手: scripts/tests/test-reaper-safety.sh
- 対象: 異物欄の存在と、異物が削除対象に載らないこと（K3・K9）が壊れると、自動掃除が異物を巻き込む事故になる
- 上記テストに異物欄のケースを追加して機械で守る

## 6. 既知の弱点（K10・別テーマ切り出し）

即時掃除・定期掃除とも self-hosted runner（PO の Mac）上でのみ動作する。Mac 電源オフ中はどちらも動かず、あるべき姿の「毎回もれなく」を完全には満たさない。
本テーマの合格ラインは「Mac がオンの間は確実に動く」とし、電源依存の解消は別テーマとして索引に登録する（2026-07-22 PO決定）。
