# KGI — process-hardening（開発ループの自動ガード化）

> この文書は何か（専門用語なしの1行）:
> AIエージェントの手順ミスを機械が自動で止める/正すための、○×で測る合格条件の正本。

親（表紙）へのリンク: [README.md](README.md)
あるべき姿: [ideal-state.md](ideal-state.md)（PO自筆・2026-07-19）

## エビデンスの層別（正直な現在地・2026-07-20 PO承認）

- 実測済み（強）: 5ミスの現在地・既存ガードの抜け道・ペアテスト型の既存実在。出典は末尾§出典のSHA群。
- 設計判断（中）: B段階方式・gh-pr-merge-safe.sh連携等の方式選択。論拠ありだが動作未検証。
- 未確立（弱）: 本KGI群の再発防止効果。効果の証拠は実装後のペアテスト実測で初めて生まれるため、設計段階では存在し得ない。ゆえに全柱に「欠落版で落ち・充足版で通るを実測して合格」を組み込み、効果のエビデンスを完了条件として強制する。

## ②の回路（テーマ横断・新しい穴も塞がり続ける仕組み）

| # | 合格条件 | 測り方 | 合格ライン |
|---|---|---|---|
| ②-a | 新しい手順ミスが実測されたら、対応ガードのPRが立つまで機械が「未対応」警告を出す | 未対応ミスを1件仕込み、警告が出る=1 | 1 |
| ②-b | 本テーマで新設・補修する全ガードがペアテスト（欠落版で落ちる・充足版で通る）を持つ | ペアテスト完備ガード ÷ 全ガード | 満数 |
| ②-c | 欠落版が実条件で現に赤で止まることを実測した | 実測済みガード ÷ 全ガード | 満数 |

## 柱1 再採点漏れ（既存gateの補修・#2979の残穴のみ）

前提実測: editedトリガーは#2979で導入済み。古い完了runの無効化・最新HEAD照合は無い（gate内に該当0件）。

| # | 合格条件 | 測り方 | 合格ライン |
|---|---|---|---|
| 柱1-a | gateが結論採用前に最新HEADに紐づく検査か照合する | 照合処理が在る=1 | 1 |
| 柱1-b | 古いHEADに紐づく結果を無効として採用しない | 古い結果を仕込み不採用=1 | 1 |
| 柱1-c | 欠落版・充足版のペアテストで実測した | 両方実測=1 | 1 |

## 柱2 宣言漏れ（既存gateの補修・#2969の残穴のみ）

前提実測: #2969はexecutor-preambleへの手順書追記＝機械強制でない。hasDocsOnly前段スキップ（check-process-artifacts.js:92, 671-672）は未対処。

| # | 合格条件 | 測り方 | 合格ライン |
|---|---|---|---|
| 柱2-a | 書類のみ変更でも正本ファイルを触る場合は前段スキップさせず宣言照合に到達させる | 正本含む書類PRを仕込み到達=1 | 1 |
| 柱2-b | 宣言照合が機械（CI）で強制される | 宣言外を検出して落とす=1 | 1 |
| 柱2-c | 欠落版・充足版のペアテストで実測した | 両方実測=1 | 1 |

注: 柱2-aの正本ファイル一覧（canonical docsパス集合）の鮮度は②の回路で維持する。

## 柱3 テストスキーマ複製（B段階: 出血停止→段階集約）

前提実測: conftest.pyに共通定義64テーブルが在るが、31ファイルが独自DDLを持つ。leadsは最低3ファイルで別定義（IF NOT EXISTS有無の変種込み）。#2956で列追加が179件連鎖。

| # | 合格条件 | 測り方 | 合格ライン |
|---|---|---|---|
| 柱3-a | テストの独自CREATE TABLEの新規増加を機械が検出して止める | 新規複製PRを仕込み赤=1 | 1 |
| 柱3-b | 検出パターンが変種（IF NOT EXISTS有無・引用符違い）を取りこぼさない | 全変種を仕込み検出 ÷ 変種数 | 満数 |
| 柱3-c | 既存複製一覧（31ファイル・テーブル名つき）を固定化し進捗を数値管理する | 一覧在り=1、以後 残複製 ◯/31 | 1＋単調減少 |
| 柱3-d | 本番スキーマ変更PRに複製側テストの同伴変更が漏れたら機械が警告する | 同伴を故意に落とし警告=1 | 1 |
| 柱3-e | 欠落版・充足版のペアテストで実測した | 両方実測=1 | 1 |

## 柱4 run取り違い（B案: ヘルパー新設＋gh-pr-merge-safe.sh連携）

前提実測: 正手順はdesign-partner.md:239,298に文書化済み・機械強制なし。既存check-runs参照は通知専用。実装順序は PR #2994 決着後（同一ファイル衝突回避）。

| # | 合格条件 | 測り方 | 合格ライン |
|---|---|---|---|
| 柱4-a | ヘルパーが最新HEAD SHA→紐づくcheck-run結論・run IDを一体で返す | SHAとrun IDが揃う=1 | 1 |
| 柱4-b | SHA不一致の過去run結論を不採用と明示する | 古いrunを仕込み不採用=1 | 1 |
| 柱4-c | gh-pr-merge-safe.sh がマージ前に必ずヘルパーを経由する | 経由せず到達できる経路数 | 0 |
| 柱4-d | 欠落版・充足版のペアテストで実測した | 両方実測=1 | 1 |

## 柱5 本番SQL（危険領域・ラッパー方式＋限界の明文化）

前提実測: scripts/に強制現物0件。SSH直書きの引用符剥がれは段階①dry-runで実測済み。

| # | 合格条件 | 測り方 | 合格ライン |
|---|---|---|---|
| 柱5-a | ラッパーがインラインSQLを拒否しファイルパスのみ受け付ける | 直書きを渡し拒否=1 | 1 |
| 柱5-b | 読み取り実行にread-only自動付与＋隔離レベル自己検証 | 読み取りモードで書き込みSQLが失敗=1 | 1 |
| 柱5-c | バックアップ・dry-run証跡なしの書き込みを拒否する | 証跡なし実行を試み拒否=1 | 1 |
| 柱5-d | ペアテストをテスト環境（tenant_006相当）の実DBで実測した | 両方実測=1 | 1 |
| 柱5-e | ラッパー外の直書き経路が残る限界を正本に明記し、ラッパー経由を常設ルール化した | 明記あり=1 | 1 |

注: 本柱の実装・本番配置は危険操作。確認→退避→dry-run→PO自筆GO→実行→検算のフル手続き。KGI確定と実装GOは別。

## 予約（recon後にKGI化・新規テーマとしては立てない）

- 柱7 妨害注入の標準化（②-cの全ガード横展開。2026-07-20 PO採用・転送受領）
- 柱8 証拠照合ゲート（実装役報告値と機械独立実測の照合関所化。同上）

## 保留

- CI詰まり対策（子候補#6。別セッションのrecon共有待ち）

## 実装順序（PO承認済み・各便は個別カード＋該当時PO GO）

1. 柱2→柱1（同一関所ファイル群・危険操作＝各便PO自筆GO）
2. 柱3-a/b/c（検出＋一覧固定。集約は小分け便）
3. ②の回路（柱1〜3の実物ガードを対象に張る）
4. 柱4（#2994決着後）
5. 柱5（最後・フル手続き）
6. 柱7・8のrecon

KGI確定＝実装開始ではない。各柱の実装前にdesign.md（技術How・接触面分析6面）を作成する。

## KPI

達成KGI数 ◯/24（②3項＋柱1〜5の21項）。

## §出典（実測SHA・すべて本セッション実測）

- 7観点recon・5ミス対照: dd338e2c4cec84 / d9a73243db3cc4
- 宣言漏れ経路特定・40PR痕跡調査: 94fdbfd870d036 / bdb8e4c8476430
- #2979/#2969残穴・柱3複製構造: 16cfc54ba1fbe4
